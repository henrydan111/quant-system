# Codex Operating Rules

This `AGENTS.md` file is the canonical Codex instruction layer for this repository. The files under `.agents/rules/` remain the detailed human-readable reference set and must stay aligned with these rules, but Codex should treat `AGENTS.md` as the always-on contract.

## 1. Mandatory Context Refresh

Before any non-trivial implementation, investigation, refactor, or data operation, read these files in order:

1. `project_state.md`
2. `config.yaml`
3. `src/system.md`
4. `data/data_dictionary.md`
5. `data/data_tracker.md`

Use them to re-establish current architecture, data coverage, known issues, and active priorities before making recommendations or edits.

For formal quantitative research, also follow `.agents/rules/research-integrity.md`, especially Section 10. That section explains the 10-stage lifecycle, the 5 human gates, pre-registration, sealed OOS, and multiple-testing rules.

Operationally, v3.1 formal non-audit runs now insert `gate_evaluation -> gate_concern_scoring -> gate_review` before publication. `gate_concern_scoring` is the `pause_for_input` step and now uses a typed `PauseForInputPayload` in `src/research_orchestrator/dag.py`; `gate_review` is the `pause_for_gate` step that writes the final gate report and accepts `approved`, `rejected`, or `quarantined`; seal-aware backtest execution flows through `src/research_orchestrator/sealed_backtest_runner.py`; the first pre-load date clamp lives in `src/research_orchestrator/window_enforcement.py`; and cache/window safety is then reinforced by `src/research_orchestrator/cache_manifest.py` plus `src/research_orchestrator/qlib_windowed_features.py`. `workspace/scripts/hypothesis_cli.py verify-seal` is now safe for automation: exit `0` means untouched, `1` means OOS already touched, and `2` means malformed design hash. The `--expect-claims N` flag (added 2026-04-28, plan `jolly-seeking-lollipop`) provides exact-count assertion mode (exit 0 only if claim count equals N).

**Discovery vs validation profiles (added 2026-04-28, plan `jolly-seeking-lollipop`; 8th profile added Phase 5):** the orchestrator now has 8 built-in profiles (the 8th, `factor_lifecycle`, is the IS-only `draft→candidate` factor gate — see §2a Phase-5 entry). The original 5 (`factor_screening`, `theme_strategy`, `event_driven_signal_research`, `ml_signal_model_research`, `strategy_improvement`) are **discovery** profiles — they auto-search a recipe space and pick the empirically best variant. The new `hypothesis_validation` profile (the 7th, alongside `benchmark_audit`) is a **validation** profile — it runs a fully-prescribed recipe (universe + components + weights + topk + rebalance + cost model) verbatim through IS+gate+OOS+publish with no auto-search. Use validation when you have a specific recipe to test (e.g., from prior discovery research); use a discovery profile when you want to find the best recipe within a search space. The validation profile requires `hypothesis.prescription` to be set (a `PrescribedRecipe` defined in `src/research_orchestrator/hypothesis.py`); discovery profiles ignore the prescription field. Register validation hypotheses with `hypothesis_cli.py register --profile-id hypothesis_validation` to opt into profile-aware floor validation (default validates against ALL profiles' floors — strategy_improvement is the strictest). **Factor lifecycle (start-to-finish overview):** how a factor moves `draft → candidate → approved` (status ladder, no-lookahead label boundary, the 4-step IS-only gate, phase map 1–7, current registry state, invariants) — see `src/alpha_research/factor_lifecycle/README.md`.

## 2. System Awareness

Always keep the full six-module architecture in mind:

- `src/data_infra/`: Tushare ingestion, Parquet storage, Qlib backend construction, verification
- `src/alpha_research/`: factor operators, catalog, evaluation toolkit, ML models, MLflow tracking
- `src/backtest_engine/`: vectorized screening engine and event-driven A-share simulator
- `src/portfolio_risk/`: portfolio optimization, transaction-cost, and risk-model tooling
- `src/result_analysis/`: standardized performance metrics, trading analysis, and reporting
- `src/research_orchestrator/`: DAG-based formal research entrypoint, built-in research profiles, typed registries, and run-artifact governance

When unsure where logic belongs, prefer the existing module boundary over adding new top-level scripts or ad-hoc helpers. The orchestrator's scope is “data is ready -> results are published”; raw downloads, normalization, and Qlib backend builds remain in `data_infra`.

### 2a. Hard invariants (must not break)

These correspond 1:1 with the `CLAUDE.md §3` hard invariants list. Both contracts agree on substance.

- **Tushare ↔ Qlib code format**: Tushare `000001.SZ` ↔ Qlib `000001_SZ`. Use `ts_code.replace('.', '_')` before every join. Wrong format silently returns 0 matches.
- **Benchmark codes** use the underscore form: `000300_SH`, never `SH000300`.
- **Trading calendar**: `data/reference/trade_cal.parquet` is the single ground truth.
- **ST authoritative source**: `data/qlib_data/instruments/st_stocks.txt`.
- **PIT for fundamentals**: align on `ann_date`, apply `shift(1)` after `merge_asof`, forward-fill across calendar gaps. Use provider-side `pit_*` fields for derived growth metrics.
- **PIT visibility anchor**: `effective_date > disclosure_date` STRICTLY (via `strictly_next_open_trade_day` in `src/data_infra/pit_backend.py`).
- **`f_ann_date` coverage is dataset-specific**: statement families (`income`, `income_quarterly`, `balancesheet`, `cashflow`, `cashflow_quarterly`) use `max(ann_date, f_ann_date)`; event/indicator families (`indicators`, `dividends`, `forecast`, `holder_number`) use `ann_date` only.
- **Delist / IPO-lag contract**: enforced at the instruments sidecar (`all_stocks.txt`) via `provider_metadata.build_all_stocks_universe()`. Direct PIT ledger readers must apply `provider_metadata.stock_basic_bounds(ts_code)` themselves.
- **Cumulative→quarterly late restatement**: `derive_single_quarter_value` retroactively changes derived quarter values when a prior quarter's cumulative is restated. Research code caching quarter values must invalidate on every ledger rebuild.
- **MultiIndex order**: Qlib `D.features()` returns `MultiIndex(instrument, datetime)` — not `(datetime, instrument)`.
- **Negation in Qlib expressions**: `-Operator(...)` does not parse. Use `0 - Operator(...)`.
- **Qlib expressions for predictive factors**: same-day leakage is the most common bug. See below for the factor-library specific enforcement.
- **Factor library PIT-safety (post follow-up plan #1, 2026-04-11)**: every `$field` reference inside every Layer 1 operator in `src/alpha_research/factor_library/operators.py` MUST be wrapped inside a `Ref(...)` frame. The correct pattern is `Mean(Ref($close, 1), 20)`, NOT `Mean($close, 20)`. Use the `ADJ_*_T1` module constants for shifted adjusted prices. `forward_return` is the ONE allowlisted exception. Enforcement lives in `tests/alpha_research/test_factor_library_pit_safety.py` (parser-based static analysis), `test_operator_expressions.py` (per-operator lock tests), and `test_operator_behavioral_pit.py` (tiny-Qlib-fixture behavioral proof).
- **Adjusted vs raw prices**: adjusted prices for cross-day returns/momentum; raw values for PIT accounting ratios. Document the choice in any new factor.
- **Publish atomicity**: `StagedQlibBackendBuilder.publish()` hard-fails on cross-volume rename. Move the staged build onto the target volume before publishing.
- **Deterministic rebuild**: `_normalize_periodic_dataset` injects `_src_file` / `_src_ordinal` hidden columns so `collapse_duplicate_versions` and `canonicalize_report_variants` produce bit-identical output across machines.
- **Exchange cost source of truth (post follow-up plan #2, 2026-04-14)**: stamp tax, commission, and transfer fee flow through `exchange.compute_sell_cost_breakdown()` / `compute_buy_cost_breakdown()`. Do NOT duplicate rate checks in the engine or portfolio. `_STAMP_TAX_CHANGE_DATE` in `exchange.py` is the single boundary.
- **CostConfig defaults are JoinQuant (changed 2026-05-22)**: `CostConfig()` matches JoinQuant `OrderCost` (close_tax 0.001 constant, no 2023 cut, no transfer fee). For realistic Chinese exchange rules use `CostConfig.realistic_china()`. Enforcement: `tests/backtest_engine/test_exchange_costs.py::CostConfigPresetTests`.
- **Exchange default slippage is JoinQuant (changed 2026-05-22)**: `Exchange()` defaults to `FixedSlippage(0.0003)` (matches JoinQuant `FixedSlippage(3/10000)`). The prior 10-bps default is the named constant `CONSERVATIVE_SLIPPAGE_10BPS`. Zero-cost fills still require explicit `NoSlippage()`. `PctSlippage(0.0003)` ≠ `FixedSlippage(0.0003)` — prefer named constants.
- **Limit prices use round-half-up**: `exchange.compute_limit_prices()` uses `Decimal.quantize(ROUND_HALF_UP)`, not Python banker's rounding.
- **Event-driven suspension wiring**: `EventDrivenBacktester` must pass `data/market/suspension/suspension_ranges.parquet` into `Exchange` when the file exists. If absent, it must log the fallback and `Exchange.is_suspended()` uses the legacy `vol == 0` proxy.
- **Event-like daily endpoint namespacing (2026-04-20)**: `_materialize_daily_dataset` in `src/data_infra/pit_backend.py` writes per-column `.day.bin` files AFTER `_run_dump_bin` writes the canonical kline bins (`$open/$high/$low/$close/$vol/$amount`). Some event-like daily endpoints ship payload columns that collide with those names — `top_list` has `close`/`amount`, `block_trade` has `vol`/`amount`. Every dataset in `EVENT_LIKE_DAILY_DATASETS` (`top_list`, `top_inst`, `block_trade`, `cyq_perf`) MUST have an entry in `EVENT_LIKE_DAILY_FIELD_PREFIX`, and its payload columns are written as `{dataset}__{column}.day.bin` (e.g., `$top_list__close`, `$block_trade__amount`, `$cyq_perf__winner_rate`). Enforcement: `tests/data_infra/test_event_like_daily_namespace.py`.
- **No hedge words in quantitative analysis (2026-05-20)**: when analyzing a strategy result, a discrepancy between backtests, or any quantitative claim, do NOT use "likely", "possibly", "probably", "appears to", "seems to", "could be", "might be", or any other epistemic hedge. Either cite the exact data/script/output that establishes the claim with certainty, or explicitly mark the claim "HYPOTHESIS:" with a stated falsification plan and run that test. Banned failure mode: presenting plausible-sounding guesses as conclusions. Mirror lives in `CLAUDE.md` §7 item 10 and `.agents/rules/research-integrity.md` §8a.

- **Provider self-attestation (PR 1 of 2026-05-26 freeze plan)**: every host that publishes a Qlib provider MUST also emit `data/qlib_data/metadata/provider_build.json` — this file is the single source of truth for `provider_build_id`, `calendar_policy_id`, `event_endpoint_namespacing.status`, and calendar bounds. The Qlib provider tree itself is gitignored, so this manifest is what lets formal artifacts prove which provider produced them. Schema lives at `schemas/provider_build.schema.json`; loader at `src/data_infra/provider_manifest.py`; builder emission is wired into `StagedQlibBackendBuilder.publish()` in `src/data_infra/pit_backend.py`. The current 2026-04-21 build has a `retroactive_manifest=true` manifest with an evidence array; future builds emit non-retroactive manifests automatically. **Hard rule for formal runs**: missing manifest → fail; namespacing != enforced → fail; calendar mismatch → fail unless the calendar policy explicitly allows it. Enforcement: `tests/data_infra/test_provider_manifest.py`.
- **Artifact provenance contract (PR 1)**: every formal `BacktestResult.config`, validation-step output, and registry artifact MUST carry an `artifact_provenance` block (PR 1 = `provider_build_id` + `calendar_policy_id`; PR 3 will extend with `execution_profile_*`). Older artifacts missing this block are read back as `legacy_artifact=true` — they remain viewable for historical comparison but cannot pass the formal release gate. Schema: `schemas/artifact_provenance.schema.json`. Reader: `src/research_orchestrator/artifact_provenance.py`. Gate: `evaluate_artifact_provenance` / `assert_formal_artifact_eligible` in `src/research_orchestrator/release_gate.py`. Enforcement: `tests/research_orchestrator/test_artifact_provenance.py`.
- **Calendar policy contract (PR 1)**: the 2026-02-27 trading-calendar freeze is recorded as an explicit policy YAML at `config/calendar_policies/frozen_20260227_system_build.yaml`. Formal runs that operate against a frozen calendar must pass `calendar_policy_id` explicitly (default sandbox behavior is `legacy_artifact=true`). When the freeze ends, flipping `frozen: false` and adding `max_calendar_lag_days` in the same YAML is the only change required — the loader at `src/research_orchestrator/calendar_policy.py` already branches on `frozen`. Enforcement: `tests/research_orchestrator/test_calendar_policy.py`.
- **Governance file home rule (PR 1)**: governance registries (calendar policies, field-status registry, execution profiles) live under `config/` and committed schemas under `schemas/`. Do NOT place governance files under `data/` — that directory is gitignored, so a registry placed there would silently become un-versioned. Runtime artifacts (the actual `provider_build.json`, registry status caches) DO live under `data/` because they are local per-host state, not shared governance.
- **ENGINE_REQUIRED_FIELDS contract (PR 2 of 2026-05-26 freeze plan)**: the canonical 8-field tuple (`$open`, `$close`, `$high`, `$low`, `$vol`, `$amount`, `$pre_close`, `$adj_factor`) lives at `src/backtest_engine/event_driven/constants.py`. `EventDrivenBacktester.run()` ALWAYS unions caller-supplied `preload_fields` with this tuple when `should_preload` is True, so the engine path never falls back to per-day `D.features` for OHLCV. Adding a new engine-required field means adding it here, not in `engine._fetch_day_data` alone.
- **`should_preload` condition (PR 2)**: `EventDrivenBacktester.run()` preloads when ANY of `preload_required=True`, `preload_fields is not None`, or `run_mode in FORMAL_RUN_MODES` ({`formal`, `oos_test`, `joinquant_replication`}). Passing `run_mode='formal'` auto-enables both `strict=True` and `require_preloaded=True`. The old `if preload_fields:` condition silently skipped preload when a formal run had no strategy-specific factor fields — that bug is fixed. Reference: the PR 2 negative-test suite at `tests/backtest_engine/test_preload_hardening.py`.
- **No-op preload removed (PR 2)**: `QlibDataFeeder.preload(start, end)` was a silent no-op that left `_cache_df=None` and forced the engine into a per-day fallback (the ~100x slowdown discovered in plan `snappy-buzzing-meerkat` v5). The method now raises `NotImplementedError`; the previous `BacktestEngine.run()` call site has been removed. Use `preload_features(index_name, fields, start_time, end_time, strict=True)` exclusively.
- **`require_preloaded` engine contract (PR 2)**: `BacktestEngine(require_preloaded=True)` makes the engine call `feeder.assert_preloaded(required_fields=ENGINE_REQUIRED_FIELDS, start=prev_trading_day(backtest_start), end=backtest_end, require_zero_fallback=True)` before the day loop starts. Auto-set to True when `EventDrivenBacktester.run(run_mode='formal'|'oos_test'|'joinquant_replication')`. Formal runs cannot silently degrade into per-day fallback.
- **`ExecutionProfile` is the formal contract (PR 3 of 2026-05-26 freeze plan)**: every formal backtest passes `execution_profile=<profile_id>` to `EventDrivenBacktester.run()` or `VectorizedBacktester.run()`. The profile lives in `src/backtest_engine/execution_profiles.py` as a frozen dataclass aggregating `fill_mode`, `cost_config_factory`, `slippage_preset`, `volume_limit`, `allowed_for_formal`, `deployment_target`, `backend`, plus a date-stamped `profile_version`. Built-in profiles: `joinquant_daily_sim` (formal, jq_daily_avg fill), `joinquant_open_close_replica` (formal, open_close fill), `realistic_china_stress` (event-driven but `allowed_for_formal=False`), `vectorized_screening_close` (vectorized, screening only). Adding a new profile means a new entry in `_BUILTIN_PROFILES` + a bumped `profile_version`. Enforcement: `tests/backtest_engine/test_execution_profiles.py`.
- **`profile_hash` is computed and self-excluding (PR 3)**: `ExecutionProfile.profile_hash` is a `@property` (NOT a dataclass field) that computes `sha256(json.dumps(execution-relevant-fields, sort_keys=True))`. The hash excludes itself — there is no recursion. Two ExecutionProfile instances with identical hashed fields produce identical hashes; changing `fill_mode`, `slippage_preset`, `cost_config_factory`, `volume_limit`, `notes`, or `profile_version` changes the hash. Every formal artifact records `execution_profile_hash` alongside `execution_profile_id` so reviewers can diff by hash without re-reading the profile registry.
- **Formal-no-override default (PR 3)**: when a caller passes `execution_profile=<formal_id>` AND also passes explicit `fill_mode=...` / `slippage=...` / `exchange_config=...` / `volume_limit=...`, the wrapper computes an `override_diff` and rejects the run with `OverrideRequiresReasonError` unless `override_reason='<text>'` is also supplied. Formal artifacts that DO carry overrides record `manual_override=true` + `override_reason` + `override_diff` in `artifact_provenance`; the release gate then surfaces those fields so reviewers can decide whether the override was justified.
- **`ArtifactProvenance` schema v2 (PR 3)**: bumped from PROVENANCE_SCHEMA_VERSION=1 to =2. v2 formal-eligible artifacts MUST carry `provider_build_id`, `calendar_policy_id`, `execution_profile_id`, AND `execution_profile_hash`; when `manual_override=True`, also `override_reason` and `override_diff`. v1 artifacts (PR 1 era, no execution_profile fields) remain readable but `is_formal_eligible()` returns False with reason `missing_execution_profile_id`. The release gate at `src/research_orchestrator/release_gate.py` cross-checks `execution_profile_id` against the registry — unknown profile → fail; profile with `allowed_for_formal=False` → fail.
- **Cache + seal critical sections are file-locked (PR 4 of 2026-05-26 freeze plan)**: `HoldoutSealStore.claim_holdout_access` and `CacheManifestStore.record_cache_write` both wrap their **entire read-check-write sequence** in `src/research_orchestrator/file_lock.py`, not just the final `_atomic_write_dataframe`. Without lock coverage of the read-check window, two concurrent processes could each pass the `frame.empty` check (seal) or each `_load` a baseline frame (cache) and silently overwrite each other. Lock files: `<seal_root>/holdout_events.lock` and `<cache_root>/cache_events.lock`. Enforcement: `tests/research_orchestrator/test_lock_concurrency.py` — 8 concurrent processes claiming the same `design_hash` produce exactly 1 success + 7 raised; 8 processes writing distinct cache rows produce 8 persisted rows. Touch either function without preserving the `with file_lock(...):` scope and the concurrency tests fail loudly.
- **Field-status registry is the formal data gate (PR 5 of 2026-05-26 freeze plan)**: every Qlib `$field` referenced in a factor expression is resolved through `config/field_registry/field_status.yaml` (committed governance, NEVER under `data/`). Each dataset declares one of 4 statuses (`approved` / `pending_review` / `quarantine` / `deprecated`); each status declares per-stage allowed flags (`sandbox_screening`, `vectorized_screening`, `formal_validation`, `oos_test`, `registry_publish`). The runtime API lives at `src/data_infra/field_registry.py` — `FieldStatusRegistry.resolve_field(token, stage)` and `validate_expression(expr, stage)`; `extract_qlib_fields(expr)` parses tokens out of nested operators (`Mean(Ref($close, 1), 20)`). Schema lives at `schemas/field_status.schema.json`. Enforcement: `tests/data_infra/test_field_registry.py` and `tests/research_orchestrator/test_field_dependency_gate.py`.
- **Unknown-field conservative-fail policy (PR 5)**: when a referenced field matches NO registered dataset, the registry's `unknown_field_policy[<stage>]` decides. Default policy: `warn` in `sandbox_screening` / `vectorized_screening` (exploration not blocked); `fail` in `formal_validation` / `oos_test` / `registry_publish` (new untracked fields cannot quietly enter formal artifacts). Bumping a new provider field into formal use requires either listing it explicitly in `field_status.yaml` or letting it ride a registered `field_prefixes` entry (e.g. `$pit_*` is approved via the `pit_fundamentals` prefix).
- **Seed status assignments (PR 5)**: `approved` = `market_daily` + `daily_basic` + `pit_fundamentals` + `reference`; `quarantine` = `moneyflow` + `hk_hold` (northbound) + `margin_detail` + `stk_limit` (downloaded but pending anomaly review per project_state 2026-05-26); `pending_review` = `top_list` + `top_inst` + `block_trade` + `cyq_perf` (post-2026-04-20 namespacing fix; anomaly review WIP). Status transitions require an append-only entry in `config/field_registry/field_approval_log.jsonl` AND a per-promotion YAML under `config/field_registry/approvals/` — diffable in Git, never binary.
- **Release-gate field-dependency check (PR 5)**: `evaluate_field_dependencies(fields=..., expressions=..., stage=...)` and the strict `assert_field_dependencies_eligible(...)` in `src/research_orchestrator/release_gate.py` are the central choke point for refusing formal artifacts whose factor expressions touch any disallowed field. Returns a `FieldDependencyGateResult` with `disallowed_fields`, `unknown_fields`, and per-field reason strings.
- **`ResearchAccessContext` is the data-layer OOS enforcement primitive (PR 6 of 2026-05-26 freeze plan)**: a `contextvars.ContextVar` carrying `(run_id, step_id, stage, design_hash, allowed_start, allowed_end, provider_build_id, calendar_policy_id, holdout_context_id, holdout_seal_claimed, allowed_fields)`. Set via the `research_access_context(...)` context manager or via `SealedBacktestRunner.run_workspace_pipeline()` which builds the context from `time_split + holdout_context`. Lives at `src/research_orchestrator/research_access_context.py`. The wrapper-level OOS backstop (`EventDrivenBacktester.run`) is preserved; this context closes the bypass path where a pipeline could read data via `qlib_windowed_features` directly.
- **`qlib_windowed_features` is the mandatory data-access chokepoint (PR 6)**: every Qlib `D.features` call in formal research must route through `src/research_orchestrator/qlib_windowed_features.py`. Before the `D.features` invocation, the wrapper calls `get_research_access_context()` and invokes `ResearchAccessContext.validate_read(start_time, end_time, fields=...)`. Violations raise `HoldoutWindowViolation` (read outside window), `HoldoutSealViolation` (OOS without seal), or `FieldAccessViolation` (field outside `allowed_fields`). Sandbox / no-context calls are unchanged.
- **`SealedBacktestRunner.run_workspace_pipeline` signature is tight (PR 6)**: `time_split` and `pipeline_args` are required keyword args; the runner constructs a `ResearchAccessContext` from them and the holdout context, then wraps `pipeline_fn(...)` in `research_access_context(...)`. Pipelines receive `time_split` and `holdout_context` explicitly. The pre-PR-6 hole (pipeline could claim a seal and then request a broader window internally) is closed.
- **AST lint bans bare `D.features` outside the canonical wrapper (PR 6)**: `scripts/lint_no_bare_qlib_features.py` detects `D.features`, aliased `from qlib.data import D as X; X.features`, `qlib.data.D.features`, and the `getattr(D, "features")` trick. Allowed: `src/research_orchestrator/qlib_windowed_features.py` (the wrapper itself) and any line with the `# noqa: bare-qlib-features` marker (privileged admin calls like `provider_manifest.compute_canonical_kline_hash`). The lint runs inside `scripts/run_daily_qa.py` as the `no_bare_qlib_features_lint` audit block. Enforcement: `tests/research_orchestrator/test_lint_no_bare_qlib_features.py`.
- **Workspace `SCRIPT_STATUS` header contract (PR 7 of 2026-05-26 freeze plan)**: every Python script under `workspace/scripts/` that the PR 2 audit classified carries a machine-readable header block. Keys: `script_status` (`formal_candidate` / `historical_investigation`), `formal_research_allowed`, `deployment_target`, `execution_profile` (when formal), `requires_provider_manifest`, `requires_preload_strict`, `pr2_audit_class` (A/B/C/D). Greppable via `rg "# script_status:"`. Enforcement: `tests/architecture/test_dormant_module_boundaries.py` `test_workspace_scripts_outside_archive_carry_script_status_header`.
- **Class-D scripts live in `workspace/scripts/archive/` (PR 7)**: 14 superseded JoinQuant mimic ladder scripts (v2-v15) were archived to `workspace/scripts/archive/p1_jq_g5a2_investigation_2026_05/`. `src/` must NOT import or path-reference them — the archived scripts are historical investigation only. Enforcement: `tests/architecture/test_dormant_module_boundaries.py` `test_archived_workspace_scripts_not_referenced_from_src`.
- **Dormant `portfolio_risk` import boundary (PR 7)**: `src/portfolio_risk/` is dormant per the 2026-05-26 audit — its `predict_portfolio_risk()` returns a hardcoded `0.05` and `MultiFactorRiskModel.fit()` is a no-op. No formal-path module (`release_gate`, `validation_steps`, `event_driven/`, `factor_library/`, `result_analysis/`) may import the dormant symbols `MultiFactorRiskModel`, `predict_portfolio_risk`, or `MarketImpactModel` until portfolio_risk is implemented for real (P2 → P0 promotion). Enforcement: `tests/architecture/test_dormant_module_boundaries.py` `test_formal_path_does_not_import_dormant_portfolio_risk` (parametrized across all formal paths).
- **`is_formal` considers BOTH run_mode AND profile.allowed_for_formal (PR 8 of 2026-05-26 freeze plan)**: `EventDrivenBacktester.run()` computes `is_formal = mode_is_formal or profile_is_formal` exactly once, before any preload decision. A caller passing `execution_profile='joinquant_daily_sim'` with `run_mode=None` is treated as formal — strict preload, `require_preloaded=True`, and `require_provider_manifest=True` all auto-enable. The pre-PR-8 bug computed `is_formal` twice and the second computation overwrote the profile-aware version with run_mode-only.
- **`strict_cache_only` mode fails on first mid-loop cache miss (PR 8)**: `QlibDataFeeder.set_strict_cache_only(True)` makes `get_features()` raise `PreloadCoverageError` on the first cache miss instead of silently falling back to per-day `D.features`. `BacktestEngine` flips this on automatically when `require_preloaded=True` so a formal run that passes the pre-loop `assert_preloaded` cannot then silently degrade mid-loop on a non-preloaded field. Enforcement: `tests/backtest_engine/test_pr8_runtime_enforcement.py` `TestFix2StrictCacheOnly`.
- **Formal-runtime provider/calendar validation (PR 8)**: `EventDrivenBacktester.run()` calls `_validate_provider_at_runtime(manifest, calendar_policy_id, run_mode)` whenever formal mode is active. For a `frozen: true` policy, the observed Qlib calendar end-date MUST match BOTH `policy.calendar_end_date` AND `manifest.provider.calendar_end_date`; the pre-PR-8 `allow_mismatch = policy.frozen` blanket was too loose. Disallowed run_modes (mode not in `policy.allowed_modes`) also raise. Enforcement: `tests/backtest_engine/test_pr8_runtime_enforcement.py` `TestFix3ProviderRuntimeValidation`.
- **Release gate flags `execution_profile_hash` mismatch (PR 8)**: `evaluate_artifact_provenance` now compares the artifact's stored `execution_profile_hash` against the current canonical `get_profile(id).profile_hash`. Mismatch → `execution_profile_hash_mismatch` added to reasons, `ArtifactGateResult.profile_hash_matches_current=False`. This catches the case where a profile was bumped (version+hash) after the artifact was produced; the artifact is no longer bit-for-bit reproducible against the current registry. Enforcement: `tests/backtest_engine/test_pr8_runtime_enforcement.py` `TestFix5ProfileHashMismatch`.
- **Replayable override_diff for object overrides (PR 8)**: when a caller passes `exchange_config=CostConfig(...)` or `slippage=FixedSlippage(...)` on top of an execution profile, the provenance override_diff now records `{"from": {"factory": "joinquant_default"}, "to": {"class": "CostConfig", "params": {...}}}` so the override is reconstructible from the artifact alone. Pre-PR-8 we stored opaque `"<caller-supplied CostConfig instance>"` strings. Helpers: `_serialize_cost_config(cfg)` + `_serialize_slippage(slip)` in `src/backtest_engine/event_driven/__init__.py`.
- **`require_research_access_context(stage)` for formal stages (PR 8)**: formal validation handlers should call this BEFORE touching feature data. Raises `MissingResearchAccessContextError` if `stage in FORMAL_STAGES = {formal_validation, oos_test, registry_publish}` and `get_research_access_context() is None`. Sandbox stages tolerantly return None. Lives at `src/research_orchestrator/research_access_context.py`. Per-handler installs in `validation_steps.py` are tracked as the remaining follow-up after PR 8.
- **Formal runs MUST supply `calendar_policy_id` (PR 8a of 2026-05-26 freeze plan)**: `EventDrivenBacktester.run()` raises `RuntimeError` BEFORE doing any backtest work if `effective_require_provider_manifest=True` AND `calendar_policy_id is None`. The pre-PR-8a behavior silently skipped `_validate_provider_at_runtime` and stamped legacy provenance — formal-mode in name only. The policy is what authorizes a formal run to operate against the current calendar window; without it the manifest validator cannot run and the artifact cannot be formally attested.
- **Frozen policy allowed_modes include profile deployment targets (PR 8a)**: `config/calendar_policies/frozen_20260227_system_build.yaml` lists `joinquant_daily` and `joinquant_open_close_replica` in `allowed_modes` so a profile-driven formal run with `run_mode=None` passes `policy.assert_run_mode_allowed(profile.deployment_target)`. Pre-PR-8a a `joinquant_daily_sim` profile + frozen policy + `run_mode=None` was rejected even though the profile is faithful to JoinQuant.
- **Daily QA enforces frozen-policy calendar equality (PR 8a)**: `scripts/run_daily_qa.py` no longer uses `allow_mismatch = policy.frozen`. Frozen policies require strict equality between `manifest.calendar_end_date`, `policy.calendar_end_date`, and the live `calendars/day.txt[-1]`. Mismatch raises with a precise diagnostic. Daily QA semantics now match the formal-runtime validator.
- **Formal runtime validator raises on Qlib read failure (PR 8a)**: `_validate_provider_at_runtime` in `src/backtest_engine/event_driven/__init__.py` now raises `RuntimeError` if the Qlib `D.calendar()` read fails. The pre-PR-8a code logged a warning and returned silently — too permissive for a formal validator.
- **Slippage serialization captures all `__dict__` attributes (PR 8a)**: `_serialize_slippage(slip)` uses `vars(slip)` filtered to JSON-safe types instead of a hardcoded attribute allowlist. Previously `FixedSlippage.spread` (the only field that class stores) was missed by the `(rate, value, bps, fixed_amount)` allowlist, producing an unreplayable `{"class": "FixedSlippage", "params": {}}`. Now `FixedSlippage(0.0005)` serializes as `{"class": "FixedSlippage", "params": {"spread": 0.0005}}`.
- **Strict cache-only raises on all-missing instruments (PR 8a)**: in `QlibDataFeeder.get_features`, when `_strict_cache_only=True` and the requested instruments have empty intersection with the cache's instrument index, the feeder raises `PreloadCoverageError` instead of returning an empty DataFrame. Closes a coverage-gap blind spot that the pre-PR-8a cache-hit short-circuit silently hid.
- **BacktestEngine restores strict_cache_only after run (PR 8a)**: `BacktestEngine.run()` snapshots the feeder's `_strict_cache_only` value before the day loop and restores it on the success path so test harnesses and direct-engine reuse don't see leaked formal-mode state.
- **Calendar policy + manifest validation fires BEFORE feeder/preload (PR 8b)**: `EventDrivenBacktester.run()` performs the `calendar_policy_id` presence check AND the full `_validate_provider_at_runtime` call BEFORE creating `QlibDataFeeder` or invoking `preload_features`. Pre-PR-8b the check fired after preload, so a formal run without a policy still touched Qlib and burned cache work before failing. Enforcement: `tests/backtest_engine/test_pr8b_ordering_modes.py`.
- **Frozen policy `allowed_modes` covers canonical FORMAL_RUN_MODES (PR 8b)**: `config/calendar_policies/frozen_20260227_system_build.yaml` `allowed_modes` includes `formal` and `oos_test` (the canonical strings from `src/backtest_engine/event_driven/constants.py`) alongside the profile deployment targets. A run with `run_mode='formal'` and the frozen policy now passes `policy.assert_run_mode_allowed` literally — no string normalization shim required.
- **Provider manifest's own `calendar_policy_id` must match the run's policy (PR 8b)**: `_validate_provider_at_runtime` enforces `manifest.calendar_policy_id == calendar_policy_id`. Without this check, a caller could pass a different policy id with matching dates and silently stamp a mismatched policy into the artifact. Enforcement: `tests/backtest_engine/test_pr8b_ordering_modes.py`.
- **Strict cache-only catches PARTIAL missing instruments (PR 8b)**: in `QlibDataFeeder.get_features`, when `_strict_cache_only=True` and ANY requested instrument is absent from the cache (not only the all-missing case), the feeder raises `PreloadCoverageError`. Pre-PR-8b the partial-missing path silently sliced and returned the subset present in cache, hiding a real coverage gap.
- **strict_cache_only restored on exception path (PR 8b)**: `BacktestEngine.run()` wraps the day loop in `try: ... finally: feeder.set_strict_cache_only(_prev_strict_cache_only)`. A module-level `strict_cache_mode(feeder, enabled)` context manager is also exported from `src/backtest_engine/event_driven/data_feeder.py` for callers that want the same guarantee in their own code. Pre-PR-8b restored only on the success path.
- **Runtime validator reads `calendars/day.txt` directly (PR 8c Blocker 1)**: `_validate_provider_at_runtime()` no longer depends on `qlib.init(...)` having run. PR 8b moved the validator BEFORE feeder construction (which is what calls `qlib.init`), so on a fresh process the prior `D.calendar(...)` call could fail with "Qlib not initialized" even when the provider on disk was fine. The new helper `_read_provider_calendar_end(qlib_dir)` reads `day.txt` directly, mirroring `scripts/run_daily_qa.py`. Enforcement: `tests/backtest_engine/test_pr8c_validation_wiring.py` explicitly does NOT patch `qlib.data.D` and the validator must still succeed.
- **Validation handlers drive EventDrivenBacktester through the formal contract (PR 8c Blocker 2)**: `handle_validation_event_backtest_is` and `handle_validation_event_backtest_oos` in `src/research_orchestrator/validation_steps.py` now pass `execution_profile='joinquant_daily_sim'`, `calendar_policy_id='frozen_20260227_system_build'`, `run_mode='formal'`/`'oos_test'`, `preload_required=True`, and `require_provider_manifest=True`. `run_event_driven_window` in `workspace/research/alpha_mining/event_driven_strategy_research.py` gains those kwargs and forwards them. Pre-PR-8c the formal-runtime guardrails added in PR 8 / PR 8a / PR 8b worked but the validation path never invoked them; the release gate could reject a published artifact later, but the actual runtime had already executed without formal enforcement. Override reason is supplied verbatim because the prescription always provides an explicit `exchange_config` + `slippage_rate` that overrides profile defaults.
- **strict_cache_only enabled BEFORE warmup + strategy.initialize (PR 8c Blocker 3)**: `BacktestEngine.run()` flips `self.feeder.set_strict_cache_only(True)` immediately after `assert_preloaded(...)`, BEFORE `prev_day_data = self._fetch_day_data(prev_date)` and BEFORE `self.strategy.initialize(init_context)`. Pre-PR-8c the strict-mode enable happened right before the day loop, so warmup + initialize could still silently fall back on a non-preloaded field. Once `require_preloaded=True`, NO feeder access after the assertion may bypass the cache.
- **OOS validation handler claims the holdout seal explicitly (PR 8d Blocker 1)**: `src/research_orchestrator/validation_steps.py` `handle_validation_event_backtest_oos` now calls `_claim_holdout_access_if_needed(context)` from `src/research_orchestrator/steps.py` BEFORE invoking `run_event_driven_window`. Pre-PR-8d the docstring claimed the seal claim happened "inside SealedBacktestRunner._claim_if_oos" but the handler called `_run_with_cache_context(... run_event_driven_window ...)` directly without going through `SealedBacktestRunner`. The `EventDrivenBacktester` OOS backstop would correctly refuse the run with "no seal claim found" — failing safely, but leaving the formal OOS validation path broken in production. The helper uses `design_hash=hypothesis.design_hash()`, `run_dir=str(context.run_dir)`, `step_id=context.step.step_id` — the exact identifiers the engine backstop later cross-checks.
- **Strict-cache try/finally covers benchmark + warmup + initialize (PR 8d Blocker 2)**: `BacktestEngine.run()` `try:` starts immediately after `set_strict_cache_only(True)`, so the `finally:` restoration fires even if benchmark load, warmup `_fetch_day_data`, or `strategy.initialize` raises. Pre-PR-8d the `try:` started at the day loop, so an init-time `PreloadCoverageError` (a strategy reading a non-preloaded field during initialize) leaked strict-mode state on the feeder. Combined with PR 8c Blocker 3, the engine now guarantees: (a) strict mode is on for every feeder access after the assertion, AND (b) restoration is guaranteed on every exit path.
- **Field-registry resolver enforcement (PR 9 of 2026-05-26 freeze plan)**: `src/research_orchestrator/validation_steps.py` `handle_validation_object_resolver` now calls `_validate_factor_field_dependencies(factor_names, stage='formal_validation', artifact_label=hypothesis_id)` AFTER the resolver succeeds. The helper looks up each prescribed factor's Qlib expression in `get_factor_catalog()` + `get_industry_relative_defs()`, then calls `assert_field_dependencies_eligible(expressions=..., stage='formal_validation')` from `src/research_orchestrator/release_gate.py`. Any factor whose expression touches a `quarantine` / `pending_review` / unknown `$field` raises `FieldApprovalError` BEFORE the IS leg starts — pre-PR-9 such candidates would only be caught at release-gate time after spending the full IS leg's compute budget. The `field_dependency_report` is also persisted into the resolver step's outputs for audit. Enforcement: `tests/research_orchestrator/test_pr9_validation_field_gate.py`.
- **Resolver helper fails closed on lookup gaps (PR 9a of 2026-05-27, GPT 5.5 Pro round-2 review)**: pre-PR-9a `_validate_factor_field_dependencies` recorded a `no_expression_found` source-tag and continued when a resolved factor lacked any factor-library expression, then submitted a possibly-empty expression list to `assert_field_dependencies_eligible` — the strict assert would return eligible because no `$field` tokens were extracted, silently defeating the gate. PR 9a raises `FieldApprovalError` at any formal stage (`formal_validation` / `oos_test` / `registry_publish`) in three previously-fail-open cases: (a) factor name in NEITHER `get_factor_catalog()` NOR `get_industry_relative_defs()`, (b) industry-relative composite whose base is missing from the catalog, (c) empty `factor_names` input. Sandbox / vectorized screening stages preserve the lenient note-and-continue behavior. PR 9a also reverses the lookup order: industry-relative composites are checked FIRST so a same-named stray catalog entry cannot bypass the base-inheritance PIT-safety contract. Enforcement: `tests/research_orchestrator/test_pr9_validation_field_gate.py`.
- **Behavioral resolver-handler test (PR 9a)**: `handle_validation_object_resolver` is now driven end-to-end with a mocked `ResolverHub.resolve_assets` to prove (a) `_validate_factor_field_dependencies` IS invoked after a successful resolver pass, (b) `field_dependency_report` is persisted into both step outputs and `registry_resolution.json`, and (c) a `FieldApprovalError` raised by the helper propagates out of the handler BEFORE any dataset/IS work begins. Enforcement: `tests/research_orchestrator/test_pr9_validation_field_gate.py`.
- **Formal-factor / field-registry compatibility test (PR 9a)**: a parametrized test iterates `get_factor_catalog(include_new_data=True)` (147 base factors; the 4 industry-relative composites + 20 Layer-2 composites inherit their field dependencies from their base factors and are covered transitively) and asserts every factor either passes `evaluate_field_dependencies(stage='formal_validation')` OR appears in `KNOWN_NON_FORMAL_FACTORS` (the explicit list of alpha-endpoint / quarantine-dependent factors expected to remain blocked). A new unexpected failure means a registry/catalog drift; a new unexpected pass means a dataset was promoted and the factor should leave the known-failing set. A representative basket (`val_bp`, `val_ep_ttm`, `qual_roe`, `qual_accruals`, `grow_netprofit_yoy`, `lev_debt_to_assets`, `liq_turnover_20d`, `mom_return_20d`, `size_ln_mcap`, `risk_vol_20d`) is also spot-checked separately. Pre-PR-9a a registry/catalog gap could silently block ~30 historically-formal factors without anyone noticing until production. Enforcement: `tests/research_orchestrator/test_pr9_validation_field_gate.py`.
- **Auditable KNOWN_NON_FORMAL_FACTORS contract (PR 9a round-3)**: the compatibility-test skip list is a `dict[str, dict]` mapping each non-formal factor name to `{reason, expected_status, expected_dataset, expected_fields}`, not a bare set. The companion test `test_known_non_formal_factors_still_block` iterates the mapping and verifies each entry: (a) the helper still raises `FieldApprovalError`, (b) actual `disallowed_fields` exactly match `expected_fields`, (c) the resolving dataset matches `expected_dataset`, (d) status matches `expected_status`. Catches three drift modes: dataset promoted → factor passes (`unexpected_passes`), factor expression edited → `wrong_fields`, registry coverage shifted → `wrong_dataset` / `wrong_status`. The skip list cannot silently become an escape hatch. Enforcement: `tests/research_orchestrator/test_pr9_validation_field_gate.py`.
- **Indicators-dataset PIT-lag contract (PR 9a round-3)**: the indicators approval YAML records `approved_usage_pattern: "Ref($field, 1) or stricter"` and `same_day_raw_usage_allowed: false`. `TestFormalIndicatorPITLagContract` (`tests/research_orchestrator/test_pr9_validation_field_gate.py`) locks two invariants: (a) every YAML-listed indicator field resolves to `dataset_id=indicators` + `status=approved` in the live registry, and (b) every formal factor whose expression touches an indicator field wraps that field in a `Ref(...)` ancestor — reuses the parser-based `find_unwrapped_field_references` stack walk from `tests/alpha_research/test_factor_library_pit_safety.py`. The approval YAML at `config/field_registry/approvals/2026-05-27_indicators_unlisted_to_approved.yaml` names these test IDs in its `evidence.tests` array so a reviewer can audit the live coverage from the YAML alone.
- **Per-family bare-name registry guardrails (PR 9a round-3)**: `tests/data_infra/test_field_registry.py` carries one explicit test per quarantine / pending-review family that uses bare-named columns: `test_moneyflow_is_quarantined_for_formal` (`$net_mf_amount`, `$buy_sm_vol`), `test_hk_hold_bare_field_quarantined_for_formal` (`$ratio`), `test_margin_detail_bare_fields_quarantined_for_formal` (`$rzye`, `$rqye`, `$rzmre`, `$rzche`), `test_stk_limit_bare_fields_quarantined_for_formal` (`$up_limit`, `$down_limit`), `test_stk_holdertrade_bare_fields_pending_review_for_formal` (`$holdertrade_*`), `test_indicators_bare_fields_approved_for_formal` (`$roe`, `$or_yoy`, …). Each asserts dataset_id + status_id explicitly. Locks PR 9a's prefix→bare-name fix as a permanent guardrail — a future registry regression to the unmatched `$moneyflow_` style fails these tests with a precise diagnostic rather than the vague `unknown_field` outcome.
- **Universe raw-field gate at the resolver (PR 9b of 2026-05-28, GPT 5.5 Pro round-4 review)**: `handle_validation_object_resolver` in `src/research_orchestrator/validation_steps.py` now calls `_validate_prescription_universe_field_dependencies(prescription, stage='formal_validation', artifact_label=hypothesis_id)` immediately after the factor field gate. Pre-PR-9b the factor gate covered only `prescription.components`; `handle_validation_dataset_build` then independently constructed `raw_field_exprs` and turned `prescription.universe.broad_filters.profitability_field` into `Ref(${profit_field}, 1)` without any registry check. A formal prescription with only approved factor components could pass the factor gate AND still consume a quarantined `$ratio` (hk_hold) through the universe path. PR 9b enumerates the canonical universe-side `$field` set (`$close`, `$adj_factor`, `$total_mv`, `$amount`, plus `$<profit_field>` if set) and runs them through `assert_field_dependencies_eligible` at the resolver, BEFORE any IS work begins. The result is persisted as `universe_field_dependency_report` next to `field_dependency_report` so reviewers can audit which non-factor `$fields` the resolver authorized. Enforcement: `tests/research_orchestrator/test_pr9_validation_field_gate.py`.
- **Dataset_build raw-field defense-in-depth (PR 9b)**: `handle_validation_dataset_build` now runs `assert_field_dependencies_eligible(expressions=list(raw_field_exprs.values()), stage=field_gate_stage, ...)` BEFORE the second `QlibFieldProvider.load_named_expressions(raw_field_exprs, ...)` call. This is a belt-and-suspenders check: if a future change adds a new field to `raw_field_exprs` (e.g. starts loading `$ratio` to honor `northbound_required`, or `$revenue_q` to honor `revenue_floor`) and someone forgets to mirror the addition into `_validate_prescription_universe_field_dependencies`, the runtime check still fires before the Qlib load. The earlier hardcoded `provider.load_named_expressions({"market_cap": "Ref($total_mv, 1)"})` call inside the industry-relative composite branch is structurally constrained to an approved field and does not pass through the gate. Mirror contract: any addition to `raw_field_exprs` MUST also be reflected in the resolver-side helper. Enforcement: `tests/research_orchestrator/test_pr9_validation_field_gate.py` (source-level ordering proof) plus the live FieldApprovalError tests in the same class.
- **Dataset_build IS-stage mapping (PR 9c of 2026-05-28, GPT 5.5 Pro round-5 review)**: `handle_validation_dataset_build` now maps the DAG-level stage to the field-registry stage explicitly — `"oos_test" → "oos_test"`, `"is_only" → "formal_validation"`, `"formal_validation"` and `"registry_publish"` pass through, anything else is treated as ungated. Pre-PR-9c the check used `if stage in {"formal_validation","oos_test","registry_publish"}` which silently skipped the IS leg because `_gate_stage(context)` returns `"is_only"` by default (`src/research_orchestrator/steps.py#L132`). The IS leg of a `hypothesis_validation` run is itself a formal stage for the field-status registry — the dataset_build defense-in-depth must fire there too or the future-proofing claim does not hold. Enforcement: `tests/research_orchestrator/test_pr9_validation_field_gate.py` pins the source-level mapping; `test_dataset_build_gate_fires_on_is_only_behavioral` proves the strict assert fires on a synthetic raw_field_exprs containing `$ratio` when the IS-leg mapping is applied.
- **`indicators` dataset is approved in field_status.yaml (PR 9a)**: PR 5 originally seeded only `$pit_*` aliases under `pit_fundamentals`, but the live formal factor catalog has always referenced bare Tushare `fina_indicator_vip` field names (`$roe`, `$or_yoy`, `$netprofit_yoy`, `$debt_to_assets`, `$current_ratio`, `$quick_ratio`, `$ocfps`, `$bps`, `$eps`, `$grossprofit_margin`, `$netprofit_margin`, `$assets_turn`, `$roa`, `$roic`, `$op_yoy`, `$basic_eps_yoy`, `$roe_yoy`, `$q_op_qoq`). These flow through the SAME PIT-anchored pipeline as `$pit_*` (per CLAUDE.md §3, the indicators family is anchored on `ann_date`). PR 9a adds an explicit `indicators` dataset entry with `status: approved` so the 30+ formal factors that depend on them (qual_*, grow_*, lev_*, val_bps_to_price, val_cftp) do not regress. The on-disk pipeline did not change — only the registry now reflects what the factor library has always consumed. Evidence: `config/field_registry/approvals/2026-05-27_indicators_unlisted_to_approved.yaml`.
- **Quarantine prefix coverage fix (PR 9a)**: pre-PR-9a `field_status.yaml` declared `field_prefixes: [$moneyflow_, $hk_hold_, $northbound_, $margin_detail_, $margin_]` that matched NOTHING in the actual Qlib provider — moneyflow/hk_hold/margin_detail/stk_holdertrade are NOT in `EVENT_LIKE_DAILY_DATASETS`, so their numeric columns ship with BARE names (`$net_mf_amount`, `$buy_lg_amount`, `$ratio`, `$rzye`, `$rqye`, `$rzmre`, `$rzche`, `$holdertrade_*`). The fail-closed `unknown_field_policy[formal_validation]=fail` preserved safety, but the diagnostic message said "unknown_field" rather than the dataset-specific quarantine reason. PR 9a replaced the unmatched prefixes with explicit `fields:` lists drawn from on-disk verification. Also added a new `stk_holdertrade` dataset (`pending_review`, same status as its alpha-endpoint siblings). No status changes; pure coverage fix. Evidence: `config/field_registry/approvals/2026-05-27_quarantine_prefix_fix.yaml`.
- **OOS seal is consumed on attempt; resume same run for recovery (PR 9, formalizing PR 8d behavior)**: `handle_validation_event_backtest_oos` calls `_claim_holdout_access_if_needed(context)` at handler entry (after the IS-gate-decision short-circuit), BEFORE reading the prescription schedule and BEFORE invoking `run_event_driven_window`. This means: (a) a failed schedule-read or provider-validation consumes the seal slot, and (b) recovery from any post-claim failure REQUIRES using the same `run_dir` + `step_id` so `allow_same_run=context.resumed` permits the second claim. Starting a fresh run with the same `design_hash` after a failed attempt is intentionally refused — the seal exists to make multiple-OOS-attempt overfitting impossible, including across crash-resume boundaries. To recover: use `research_orchestrator_cli.py resume --run-dir <same-run-dir>`, not a new run. Behavioral enforcement: `tests/research_orchestrator/test_pr9_validation_field_gate.py`.
- **PIT-lookahead prevention architecture — "one PIT-semantics layer, two front doors" (2026-05-29/30 prevention plan, PRs #18-#21 + the promotion gate)**: a string-comparison lookahead bug in the hand-rolled `workspace/scripts/sandbox_v*` loaders (dashed `effective_date` lexically compared to compact `trade_date`) inflated the v31/v32/v33 + val_heavy "champions" by ~9 months of earnings foresight (OOS 188.7%->2.0%; val_heavy +81.9%->+9.6% with negative WF). The production backend was always clean; the bug lived only in research code that BYPASSED it. Standing contract:
  1. Two sanctioned front doors only: research/sandbox via `src/data_infra/pit_research_loader.py` (`load_pit_asof_panel` lag-0 / `load_pit_signal_panel` lag-1 default); formal via `src/research_orchestrator/qlib_windowed_features.py`. Both share the datetime-correct stateful-q0 kernel `src/data_infra/pit_alignment_core.py`. NEVER hand-roll PIT alignment; NEVER read `data/pit_ledger/*` directly outside the loader/builder; NEVER string-compare date columns. The production provider is the oracle, bound by the live parity test `tests/data_infra/test_pit_loader_provider_parity.py` (in `run_daily_qa`).
  2. Loader is fail-closed: unknown/quarantined fields refused even at `sandbox_screening`; `duplicate_policy` fail-closed (`provider_stateful_q0` for Case-A; raises on Case-C incl. mixed null/non-null); `apply_provider_bounds=True` closes the delist/IPO-lag leak.
  3. PIT002 lint is a hard gate: `scripts/lint_no_unsafe_pit_dates.py` (raw-`pit_ledger`-read = hard error, docstring-aware; PIT001 = staged warning) runs in `run_daily_qa.py` over `src`+`workspace`; exemptions only via `config/lint/unsafe_pit_dates_allowlist.yaml` (no inline noqa for PIT002); only `ARCHIVE_SKIP_ROOTS` skipped (root-specific).
  4. Legacy lineage contained: the 73 invalidated `sandbox_v*` scripts are local-only (untracked, gitignored) under `workspace/scripts/archive/pit_lookahead_legacy_2026_05/` + tracked README manifest; `tests/architecture/test_dormant_module_boundaries.py` forbids live code importing `sandbox_v*` or path-referencing the archive.
  5. Promotion gate (decision layer — ENFORCED at the registry transition): promoting a `strategy_candidate` to the privileged registry status `approved` is gated in `StrategyRegistryStore.set_status` (`src/research_orchestrator/registries/strategy_registry.py`) — REQUIRES a `current_git_sha` (mandatory; binds the approval to a committed HEAD; omitting it raises) and raises `PromotionGateError` unless `promotion_evidence` passes `assert_promotion_artifact_eligible` (`src/research_orchestrator/release_gate.py`): independent PIT-correct reproduction source in `{qlib_windowed_features, joinquant_native_pit, audited_pit_source}` (a sandbox/loader panel, even parity-verified `pit_research_loader`, is INSUFFICIENT; `audited_pit_source` needs `source_name`+`audit_artifact`), PLUS `unsafe_pit_dates_lint`=="passed", `live_provider_parity` passed-or-legally-not-required (illegal if `pit_research_loader` used), clean git tree / matching `git_sha`. Forward-looking deployment LABELS (`champion`/`deployment_candidate`/`live_candidate`) have no call site yet; future promotion tooling MUST call the full `assert_promotion_artifact_eligible` (NOT the source-only `assert_promotion_eligible`, which skips lint/parity/clean-tree/git checks). Statuses split from labels per GPT review. Enforcement: `tests/research_orchestrator/test_promotion_gate.py` (incl. `set_status` wiring).
- Factor-lifecycle Phase-1 formal gates (2026-05-31, factor_lifecycle plan PRs P1.1+P1.2+P1.3; Codex round-4/5 reviewed, GPT cross-review P0-fixed): the formal factor registry enforces a writer gate, a reader gate, AND a definition-binding gate so a `draft` factor stays usable for discovery research but can NOT be silently treated as formally-approved by a formal validation, and a formal validation can NOT run against a registry row whose definition has drifted from the catalog.
  1. Writer gate (P1.1): `FactorRegistryStore.set_status("approved")` (`src/alpha_research/factor_registry/store.py`) requires a MANDATORY `current_git_sha` + a `promotion_evidence` artifact passing `assert_promotion_artifact_eligible` (mirrors the strategy-registry gate); else `PromotionGateError`. New `approval_validity` column (`valid`/`requires_revalidation`/`stale`); `_normalize_approval_validity` fail-closes a pre-existing `approved`+blank row to `requires_revalidation` on load; a successful approve stamps `valid`. `export_current(status="approved")` excludes non-`valid` rows unless `include_invalid=True`. CLI `set-status --status approved` (`workspace/scripts/factor_registry_cli.py`) requires `--promotion-evidence-json` + clean tree (exits 2 otherwise) — not an unaudited approval door.
  2. Reader gate "resolve-but-label" (P1.2, Codex round-5; SUPERSEDED the round-2 `draft→_unresolved` design, which would hard-fail the 3 discovery DAGs whose `object_resolver` raises on unresolved at `steps.py:444`, since all registry rows are `draft`): `resolver._resolve_formal_factor` (`src/research_orchestrator/resolver.py`) RESOLVES every current row (status stays `resolved`) but LABELS `source_layer` by status+validity: `approved`+valid→`formal`; `candidate`+valid→`factor_registry_candidate`; `draft`→`factor_registry_draft`; `approved`+non-valid→`factor_registry_stale`; `deprecated`→`factor_registry_deprecated`. A requested `definition_hash` is enforced as a REAL match filter (closes same-name shadowing). The SOLE formal-permission point is the explicit allow-set in `handle_validation_object_resolver` (`src/research_orchestrator/validation_steps.py`): accept `formal` (+ `factor_registry_candidate` IFF `prescription.allow_candidate_components`), reject every other layer incl. plain candidate-registry `candidate`. Safe because discovery publish writes `candidate`/`observed` (`typed_store.py:452`), never `approved`. `resolve_assets` adds a `factor_registry_hits_by_layer` summary. Enforcement: `tests/alpha_research/test_factor_registry.py`, `tests/alpha_research/test_research_orchestrator.py`, `tests/research_orchestrator/test_pr9_validation_field_gate.py` (`TestPR12FormalAllowSet`).
  3. Definition-binding gate (P1.3): `handle_validation_object_resolver` (`src/research_orchestrator/validation_steps.py`) calls `_assert_no_definition_drift` AFTER the allow-set and BEFORE the field gate / ANY compute: every resolved formal factor's STORED registry `definition_hash` must equal the CURRENT code catalog's hash, recomputed via `FactorRegistryStore.current_catalog_definition_hashes()` which REUSES `_build_catalog_snapshots` — the SAME algorithm `sync_catalog` writes (base/composite/industry-relative compare apples-to-apples, Codex round-4 parity). A mismatch (registry row stale vs `catalog.py`, or factor absent from the catalog) raises `FactorDefinitionDriftError`; a factor-registry entry with NO stored `definition_hash`/canonical_id (malformed/legacy approved row) is itself treated as drift and refused — fail-closed (GPT cross-review); only non-factor-registry layers are skipped. Enforcement: `tests/research_orchestrator/test_pr9_validation_field_gate.py` (`TestPR13DefinitionBindingGate`, incl. `test_missing_definition_hash_fails_closed`), `tests/alpha_research/test_factor_registry.py` (`test_current_catalog_definition_hashes_round_trip`).
  4. GPT cross-review P0 fixes (2026-05-31): two approval-bypass holes fixed — (a) `set_status`/`StrategyRegistryStore.set_status` force-overwrite `artifact["promotion_status"]=status` (NOT `setdefault`) so a caller-supplied `promotion_status="draft"` can't downgrade the gate; (b) `set_approval_validity` refuses `validity="valid"` on an `approved` row (re-affirm via the `set_status` gate). Enforcement: `cannot_be_bypassed_via_evidence_status` / `cannot_revalidate_approved_row` in `tests/alpha_research/test_factor_registry.py` + `tests/research_orchestrator/test_promotion_gate.py`.
  5. TestingLedger file-lock (P1.5): `TestingLedgerStore.record_event` / `record_verdict` (`src/alpha_research/testing_ledger.py`) wrap the FULL read-check-write in a `file_lock` (lazy import, cycle-safe), mirroring `CacheManifestStore.record_cache_write` / `HoldoutSealStore.claim_holdout_access`. `record_verdict` holds ONE lock over measurement lookup + prior-verdict supersedes lookup + append via an unlocked `_append_event_unlocked` helper (NOT the public `record_event`) → no self-deadlock on the non-reentrant lock (Codex round-4). Lock file `<ledger_root>/testing_ledger.lock`. Enforcement: `tests/research_orchestrator/test_lock_concurrency.py` (`TestTestingLedgerConcurrency`).
  6. `seal_key` / FrozenSelectionSet OOS-seal migration (P1.4): the holdout seal is keyed by `seal_key` (defaults to `design_hash` for back-compat) so the OOS budget is spent per FROZEN SELECTION SET, not per mutable `design_hash`. `HoldoutSealStore` (`src/research_orchestrator/holdout_seal.py`) gained a `seal_key` column; `_load()` backfills `seal_key=design_hash` for pre-P1.4 rows (no mixed write/read window); `claim_holdout_access` + `list_events` accept `seal_key`. `HoldoutContext` + `ResearchAccessContext` carry `seal_key` with an `effective_seal_key` property (falls back to `design_hash`); `SealedBacktestRunner` + the two engine backstops (`event_driven`/`vectorized` `__init__.py`) claim/check by `effective_seal_key`; `hypothesis_cli.py verify-seal` gained `--seal-key`. New `src/research_orchestrator/frozen_selection_set.py` `FrozenSelectionSet.frozen_set_hash` = sha256 over `{schema_version, sorted selected (factor_id,version,definition_hash,expected_direction), candidate_pool_hash, selection_rule_hash, eval_protocol_hash, metric, portfolio_side, universe, time_split_window, rebalance, neutralization}` — EXCLUDES pass/fail wording + provider/calendar/build IDs (cosmetic/prose edits don't change it; direction/pool/rule/protocol/metric/side/universe/window/rebalance/neutralization do). No live caller passes `frozen_set_hash` yet (defaults to `design_hash`); wiring a real run is future work. Enforcement: `tests/research_orchestrator/test_frozen_selection_set.py`.
- Factor-lifecycle Phase-2 registry evidence schema (2026-05-31, factor_lifecycle plan PRs P2.1-P2.4; GPT cross-reviewed; PR #31): the factor registry carries walk-forward/OOS + GROSS long-only evidence + signal-role metadata + provenance, populated by a definition-bound importer (`src/alpha_research/factor_registry/store.py`). BOUNDARY: Phase 2 writes EVIDENCE/metadata ONLY — never `status`/`approval_validity`/`definition_hash` — so the Phase-1 gates are unaffected. (1) Schema (P2.1): new `factor_evidence` cols (`is/oos_rank_icir`, `sign_consistency`, `oos_ls_sharpe`, `retain_pct`, `lo_*_gross`, `lo_hit`, `evidence_class`, `formal_evidence_eligible`, `source_path/hash`, `provider_build_id`, `calendar_policy_id`) + `factor_master` latest-mirrors + signal-role metadata (`long_only_viable_provisional`, `signal_role`(+`_suggested`), `expected_direction`, `requires_inverse_for_long_only`, `approved_uses`, `validation_scope`, `field_eligibility_snapshot_json`, `last_revalidated_at`, provenance); fail-closed load defaults via `_normalize_phase2_metadata`. (2) Viability (P2.2): `long_only_viable_provisional` DERIVED in `refresh_master_derived_fields` from the GROSS LO metric (fail-closed order: missing→non_viable; sharpe≥1.0 ∧ excess>0 ∧ hit≥0.60→viable; sharpe<0.5 ∨ excess≤0→non_viable; sharpe≥0.5 ∧ excess>0→review_only). GROSS proxy — formal cost-adjusted viability deferred; review_only fail-closed for formal long-only. (3) Importer (P2.3): `import_revalidation` attaches evidence ONLY to factors whose registry `definition_hash` == the current catalog hash (fail-closed; drifted/unknown SKIPPED, never by name), labels rows `historical_investigation`/`formal_evidence_eligible=False` with bound `source_hash`, evidence-only, idempotent. (4) Surfacing (P2.4): registry review HTML + `factor_registry_cli.py import-revalidation`. (5) GPT PR-#31 cross-review hardening (2026-05-31): post-implementation review (NO-GO → 5 findings verified + fixed; boundary re-confirmed) hardened `refresh_master_derived_fields`: (a) mirrors `latest_provider_build_id`/`latest_calendar_policy_id`/`last_revalidated_at` from the latest `run_type=='revalidation'` evidence ONLY (re-review fix: a plain `sync_catalog`'s `catalog_sync` row no longer stamps `last_revalidated_at`) + sets `signal_role_suggested='long_only_alpha'` for `viable` (never touches authoritative `signal_role`); (b) derives viability from the SINGLE latest LO-Sharpe-bearing evidence row's full `(sharpe,excess,hit)` tuple — partial latest tuple → `non_viable`, NO cross-row metric mixing; (c) binds the P2 mirrors to the row's CURRENT `definition_hash` — nonblank `source_hash` ≠ current hash (stale/skip-drifted) is ignored though it remains in `factor_evidence`; (d) populates `field_eligibility_snapshot_json` from the LIVE field-status registry at `formal_validation` — BASE factors `resolved:true`, COMPOSITE/INDUSTRY_RELATIVE pseudo-expressions + registry-load errors are fail-closed `resolved:false` (never a false `all_allowed=true`; empty string likewise fail-closed for Phase-3). `retain_pct` (finding 5) schema-reserved/DEFERRED — absent from all current inputs, must not be read as populated. Enforcement: `tests/alpha_research/test_factor_registry.py` (+ 5 PR-#31 regression tests; finding-2/3 + the re-review revalidation-scoping test proven to fail pre-fix; 28 passed). GPT re-review of the fixes (commit `75c2e45`) found one residual must-fix — `last_revalidated_at` set from ANY bound row let a plain `sync_catalog` stamp all 171 "revalidated"; fixed by scoping the P2 evidence-mirrors to `run_type=='revalidation'` only.
- Factor-lifecycle Phase-3 status-aware factor readers (2026-05-31, factor_lifecycle plan §4 Phase 3; design cross-reviewed → conditional-GO integrated → confirmed GO; branch `factor-lifecycle-p3`): ADDITIVE sandbox/discovery read layer over the formal factor registry (`src/alpha_research/factor_library/selection.py`, re-exported from `factor_library/__init__.py`). BOUNDARY: `get_factors`/`get_factor_selection` are NOT the formal gate — formal validation still resolves through the P1.2 resolver allow-set + P1.3 definition-binding. ZERO of the 42 `get_factor_catalog` call sites change; `get_factor_catalog` stays the authoritative computable definition source. (1) `get_factors(*, stage, status_in, prioritize=None, include_new_data=False, registry_dir=None, on_drift='skip')` → `OrderedDict{base_factor_id: qlib_expr}`, BASE only, compute-ready drop-in for `compute_factors`. Registry = status FILTER; CODE = definition SOURCE (registry `COMPOSITE(...)` pseudo-expressions never returned). `stage` REQUIRED ∈ `SANDBOX_STAGES` (a `FORMAL_STAGES` value raises `FormalStageNotAllowedError`); `status_in` REQUIRED + explicit (no default); `on_drift='skip'` default (drifted row excluded; `code_warn`/`raise` opt-in); empty registry raises `RegistryNotSyncedError` (never `{}`). (2) `get_factor_selection(...)` → `FactorSelection` (`base_expressions` + `composite_defs` + `industry_relative_defs` + per-factor `records`): selected composites'/industry-relative base DEPENDENCIES auto-unioned into `base_expressions` for compute-readiness, tagged `selection_role='dependency'`/`selected=False`/`dependency_included=True` (never confused with a status selection); `get_factors` is the strict base-status filter (deps excluded). (3) `sync_catalog_to_registry(*, dry_run=...)` — thin wrapper over `sync_catalog` + parity diff (`new_drafts`/`new_versions`/`catalog_only`/`registry_only`/`parity_ok`); never auto-run in a read; never writes `approved`. Defense-in-depth bypass guard: runtime `stage=` refusal + an AST-USAGE architecture test (`tests/architecture/test_get_factors_boundary.py`) forbidding any formal-path module from importing/referencing the readers (AST not text — formal paths legitimately import the sibling `get_factor_catalog`). Enforcement: `tests/alpha_research/test_factor_selection.py` + `tests/architecture/test_get_factors_boundary.py` (21 tests). GPT PR-#32 post-impl review (NO-GO → 2 fixes → GO): (a) `sync_catalog_to_registry` `parity_ok` now also requires `not registry_only` (an orphan current row for a code-removed factor is a parity violation the pre-fix formula missed); (b) `prescription_runtime` (hypothesis_validation Gate-C, imported by `validation_steps`) added to the AST-boundary `FORMAL_PATHS`.
- Factor-lifecycle Phase-4 walk-forward revalidation modules (2026-06-01, factor_lifecycle plan §4 Phase 4; design cross-reviewed → conditional-GO ×2 integrated → confirmed GO; branch `factor-lifecycle-p4`): legacy walk-forward revalidation SCRIPTS ported into a tested package `src/alpha_research/factor_lifecycle/` (`metrics`/`status_rules`/`revalidation`/`walk_forward_validation`/`report`). TWO strictly-separated modes (the separation IS the leakage guard): (1) `run_is_walk_forward` (FORMAL, IS-only, `draft→candidate`) is structurally bounded to `TimeSplit.is_end`. The forward-return label is FUTURE-looking (`Ref(ADJ_CLOSE, 0 - h)`), so bounding the factor date alone is insufficient — THREE belts enforce the label-realization boundary: (a) factors computed `horizons=None` over `[is_start, is_end]`; (b) label built from adj-close at the EXACT trading-calendar target `r(t)=open_days[pos(t)+h]` (NOT `shift(-h)`-over-rows — a sparse/uncapped adj-close would otherwise reach a later row past `is_end`; GPT P0 review); missing `r(t)` drops, both inputs asserted `≤ is_end` (belt 0); (c) `IsWindowedPanel` raises `IsEndLeakageError` unless `max_factor_date` AND `max_label_realization_date` (factor date `+h` TRADING days via `trade_cal.parquet`) are `≤ is_end` AND factor/label indices are aligned. IS-internal holdout = 3+1+1 (default 5+2+1 yields ZERO folds for 2014-2020); a generated factor with no heldout RAISES `NoHeldoutBlockError` (fail-closed); result carries NO `oos_*` field, labels `evidence_kind` (`generated_heldout`/`a_priori`) on result + every row. (2) `run_historical_*` (NON-formal full-window IS+OOS) is a faithful port labeled `historical_investigation`; `revalidate_*.py` reduced to thin wrappers. Split rules: `assign_historical_status` (oos-based parity) vs `assign_candidate_status` (IS-only, no `oos_*`, candidate/draft only); long-only viability stays Phase-2 metadata. `report` reproduces the exact CSV columns/rounding (round-trips through Phase-2 `import_revalidation`). Enforcement: `tests/alpha_research/test_factor_lifecycle_{metrics,revalidation,walk_forward}.py` (34 tests). GPT PR-#33 post-impl review (NO-GO → 2 fixes → GO): (P0) `build_is_windowed_panel` `shift(-h)`-over-rows could use a later adj-close row past `is_end` for a sparse/uncapped series — fixed by exact-calendar `r(t)` lookup + belt-0 input caps + index-alignment assertion; (P1) unknown `factor_origin` silently took `a_priori` — now raises. Scope: modules + tests + thin wrappers only — no new revalidation run; the formal candidate gate is not yet orchestrator-wired (Phase 5).
- Factor-lifecycle Phase-5 orchestrator `factor_lifecycle` profile (2026-06-01, factor_lifecycle plan §2.4/2.5; design cross-reviewed → conditional-GO ×2 → GO; slice-1 GPT-GO'd; branch `factor-lifecycle-p5`): a NEW formal profile (the 8th built-in) wiring `run_is_walk_forward` into the DAG as the `draft→candidate` factor gate. IS-ONLY by construction (orchestrator-level mirror of the Phase-4 leakage guard): the compiled DAG has NO `oos_test` stage, NO event/vectorized OOS backtest, NO holdout-seal claim (seal only fires on `oos_test`); `candidate→approved` OOS spend is a SEPARATE frozen-set/promotion-gate path. Handlers in `src/research_orchestrator/factor_lifecycle_steps.py`: (1) `factor_lifecycle_object_resolver` (DRAFT-accepting allow-set; reuses P1.3 `_assert_no_definition_drift` + PER-FACTOR field gate so the next step excludes-and-continues); (2) `factor_lifecycle_dataset_build` (IS-only panel via `load_is_windowed_panel` for field-eligible base factors only; disallowed `$field` never reaches compute); (3) `factor_lifecycle_walk_forward` (`run_is_walk_forward` → candidate/draft verdicts, no `oos_*`, + per-factor & batch-effective-trials ledger events; `factor_origin∈{generated,a_priori}` fail-closed); (4) `factor_lifecycle_registry_publish` (DIRECT decision matrix, NOT `_assert_gate_allows_publication`: only human `approved` writes a FORMAL idempotent lifecycle evidence row [`store.record_lifecycle_evidence`, `formal_evidence_eligible=True`, definition-bound, no `oos_*`] THEN `set_status('candidate')` non-privileged; rejected/quarantined/missing → no writes; NEVER `approved`). Gate triplet reused unchanged + a `factor_lifecycle` branch in `_collect_measured_values` maps max `|heldout ICIR|`→`rank_icir` so `criteria_results` is non-empty. Scope: profile + handlers + tests, NO actual run (Phase 6). Enforcement: `tests/research_orchestrator/test_factor_lifecycle_{profile,steps}.py` (17 tests). GPT PR-#34 post-impl review (NO-GO → 4 fixes): (1) `record_lifecycle_evidence` independently fail-closes on definition drift (skipped_drift/skipped_unknown); (2) maps `n_heldout_blocks`→`selected_fold_count`; (3) profile `consumes_types` narrowed to `("factor",)` (composite gating is a follow-up); (4) publish emits `produced_objects` (promoted factors at status `candidate`) for lineage.
- Factor-lifecycle Phase-6 FIRST OPERATIONAL RUN — 72 base factors promoted draft→candidate on the LIVE registry (2026-06-01, branch `factor-lifecycle-p6`; user-approved live write): ran the Phase-5 `factor_lifecycle` profile on the 147 base catalog factors end-to-end through the orchestrator. Result: live `data/factor_registry/` is now 72 `candidate` + 99 `draft` (171 current); the 72 carry definition-bound FORMAL lifecycle evidence (`run_type='factor_lifecycle'`, `formal_evidence_eligible=True`, `oos_rank_icir`=NA, NEVER `approved`). Pipeline: 147 base → 114 field-eligible (33 quarantine/pending `$field` auto-excluded) → 85 IS-candidates → 72 promoted = the OOS-stable subset (13 IS-candidates whose INDEPENDENT full-window 2014-2026 evidence collapses/flips OOS were EXCLUDED — 8 are the `grow_*`/`earn_earnings_momentum` earnings-LEVEL factors already known IS-overfit; they stay `draft`). `candidate` is an ADDITIVE tier: it does NOT restrict research use — `get_factor_catalog()` stays authoritative for ALL discovery/sandbox (42 call sites ignore status); status gates ONLY formal `hypothesis_validation` components (P1.2 allow-set), and `candidate` can never auto-become `approved` (separate P1.1 OOS/promotion gate). TWO real merged-code bugs found by the run + fixed (each with a regression test proven to fail pre-fix): (1) Phase-4 MultiIndex level-order — `build_is_windowed_panel` hardcoded an `(instrument,datetime)` future-price index but `compute_factors` returns `(datetime,instrument)`; positional `adj.reindex` matched NOTHING → all-NaN label → empty-panel `IsEndLeakageError` (the Phase-4 fixtures only used `(instrument,datetime)` so it was untested). Fixed: build the future index in the panel's actual level order + a loud guard if current prices match but every future price is NaN (`test_build_is_multiindex_level_order_invariant`). (2) Phase-5 publish resume-safety — `handle_factor_lifecycle_registry_publish` read verdicts from in-memory `context.state["factor_lifecycle"]`, but the gate pauses split the run across processes and `reconstruct_state_from_completed_steps` restores ONLY `step_outputs` on resume → a paused run would promote NOTHING. Fixed: read from the PERSISTED `factor_lifecycle_walk_forward` step_outputs (in-memory fallback for the no-pause path) (`test_publish_reads_verdicts_from_persisted_step_outputs_on_resume`). Run mechanics (reusable): factor_lifecycle is formal-only → needs a Hypothesis (no floor rails — not in `SUCCESS_CRITERIA_FLOORS`); set ONLY `success_criteria.min_rank_icir` (others None → no null hard rules → automated verdict honestly "accepted"); the 2 gate pauses driven by `workspace/scripts/phase6_drive_gates.py`, request by `workspace/scripts/phase6_setup_request.py`. Safety arc: compute-only dry-run (cross-checked vs the prior catalog-revalidation CSV — IS ICIR reproduces) → 3-factor temp-registry orchestrator validation → 1-factor benign drift sync (`rev_up_down_ratio_20d` Count→equivalent Sum(If), re-versioned v3 via `sync_catalog_to_registry`) → live 72-run. factor_lifecycle+registry suite: 81 passed. NOT done: the 24 composite + industry-relative factors (Layer-2 `add_composites` gating); the 6 sealed-OOS winners take their SEPARATE governed sealed-OOS/promotion path. Committed `38ba203`; PR #35 → `wave1-field-promotion`. GPT PR-#35 cross-review = GO (both fixes confirmed correct/complete, no leakage hole, 57 focused tests pass, live-registry probe matches) + one resolved governance follow-up: the 72 are an `oos_informed_backfill` — the IS-only validator was NOT contaminated (OOS never entered labels/folds/metrics/set_status), but the candidate SELECTION used pre-existing full-window OOS knowledge to exclude the 13 collapsers, so the 72 must NEVER be described as a fresh OOS-free selection, and 2021-2026 is BURNED for these 72 (candidate→approved MUST use a genuinely-sealed window). Full funnel (147→114→85→72) + label recorded in `workspace/research/factor_expansion/phase6_selection_provenance.json` + a `testing_ledger` `phase6_selection_funnel` event.
- Factor-lifecycle Phase-7 — composite + industry-relative gating + cross-sectional-helper PIT-leak hardening (2026-06-01, branch `factor-lifecycle-p7`; design cross-reviewed → GPT Conditional GO → v2 → re-confirm GO → built; NO run yet): extends the IS-only `factor_lifecycle` gate to the 20 composite + 4 industry-relative Layer-2 factors Phase 5 deferred. PIT-leak hardening (GPT main finding): `cs_rank`/`cs_zscore`/`cs_demean`/`winsorize` (`operators.py`) used positional `groupby(level=0)`; on an `(instrument, datetime)` panel that ranks each stock ACROSS TIME → `factor[t]` absorbs `t+1 … end` values, INCLUDING dates after `r(t)` → lookahead leak inside IS (same MultiIndex-order class as the Phase-6 bug). Fixed via a shared name-based `_date_level_key` resolver (prefer `datetime`-named level; dtype fallback; fail-closed — no positional-0 fallback); behavior-preserving for all `(datetime, instrument)` callers; `cs_zscore` keeps `std.replace(0, np.nan)`. Layer-2 gating: composites (`Σ wᵢ·cs_rank(±baseᵢ)`) + industry-relative (within-`(date,industry)` demean / size-industry-neutralize) are SAME-DATE cross-sectional transforms of PIT-safe base factors → `is_end` boundary inherited. New `load_is_windowed_panel_with_layer2` computes the base dependency closure over `[is_start, is_end]`, applies `add_composites` + `add_industry_relative_composites` (PIT-safe `build_industry_series_asof` L1 + `Ref($total_mv,1)`), gates ONLY named columns (dependency-only bases EXCLUDED), reuses `build_is_windowed_panel` + a `["datetime","instrument"]` assert. `handle_factor_lifecycle_dataset_build` splits eligible into base/composite/industry-rel (no `non_base_deferred`; unknown → hard error). `$total_mv` added to the industry-rel field-eligibility check; `expected_direction` (positive/inverse/undetermined from signed ICIR) on every verdict row AND (GPT impl-review must-fix) persisted to `factor_master.expected_direction` at publish via metadata-only `FactorRegistryStore.set_expected_direction` (future `FrozenSelectionSet` hash consumes it; no touch to status/approval_validity/definition_hash); evidence stores signed `is_rank_icir`. GPT impl-review = NO-GO → that one must-fix integrated + tested → otherwise correct. Tests: `test_cross_sectional_helpers.py` (10, incl. the no-future-dependence leak test proven to fail pre-fix) + Layer-2 builder tests + dataset_build split/unknown; 178 passed. OPERATIONAL RUN COMPLETE (2026-06-02, MERGED PR #36 `c587e44`): ran the Layer-2 gate on the 16 OOS-stable (12 composites + 4 industry-rel) — dry-run (15 candidate + 1 IS-draft) → temp validation → live (parity-clean). 15 promoted draft→candidate; `comp_momentum_quality` stayed DRAFT (full-window OOS-stable but IS sign-consistency 0.571<0.70 — gate filtering, not OOS). Live registry now 87 candidate + 84 draft (72 base + 15 Layer-2); `expected_direction` persisted (3 inverse predictors); `oos_informed_backfill` funnel (24→16→15) in `phase7_selection_provenance.json` + `testing_ledger` `phase7_selection_funnel` event (2021-2026 burned; candidate≠approved). Factor-lifecycle arc Phases 1-7 COMPLETE — 87 of 171 catalog factors `candidate`. Non-blocking future cleanup: `operators.neutralize` positional assumptions (Phase 7 uses `neutralize_size_industry` which normalizes).

## 3. Root, Workspace, and Data Hygiene

- Keep the project root limited to configuration, documentation, and top-level directories. Move ad-hoc outputs to `logs/` or `workspace/outputs/`.
- `workspace/` is the only place for active notebooks, experiments, prototypes, and research-only helper scripts.
- `data/` is for datasets and documentation only. Do not place Python source files inside `data/`.
- Do not use external temp directories such as `%TEMP%`, `AppData`, `/tmp`, or other paths outside `E:\量化系统\` for persistent project artifacts.

## 4. Python Environment and Path Discipline

- Use the project virtual environment at `E:\量化系统\venv\Scripts\python.exe`.
- Install packages with `E:\量化系统\venv\Scripts\pip.exe`.
- Reusable code under `src/` must derive paths from `config.yaml` or project-root-relative configuration, never hardcoded machine-specific paths.
- One-off research scripts under `workspace/scripts/` may use explicit local paths when necessary, but should still prefer config-driven paths when practical.

## 5. Logging and Operational Discipline

- Use `logging`, not `print()`, in reusable modules and production scripts.
- Route operational logs to `logs/` with rotation for long-running or recurring scripts.
- Any future script or pipeline step expected to take substantial time must include a visible progress tracker and regularly print current progress to the console. Prefer `tqdm` or periodic logging with completed/total counts and ETA when practical.
- For meaningful system changes, data syncs, milestone completions, or architecture/rule updates, record the outcome in `project_state.md`.

## 6. State Tracking and Rule-File Maintenance

### 6.1 project_state.md

`project_state.md` is the durable project memory. Update it after significant work, including:

- new datasets or pipeline entry points
- major bug fixes or architecture changes
- backtester behavior changes or new research conventions
- data sync status changes
- rule migrations such as this Codex/AGENTS transition
- **any update to `AGENTS.md`, `CLAUDE.md`, or `.agents/rules/`** — rule changes are themselves significant work

Also keep "Last Updated", active focus, and data sync sections current when they are affected.

### 6.2 Keep AGENTS.md, CLAUDE.md, and .agents/rules/ fresh (standing instruction)

This is non-optional. Stale rule files actively mislead future sessions and are worse than no rule files.

- **At the start of every non-trivial task**, during the §1 context refresh, also skim `AGENTS.md` and `CLAUDE.md` against `project_state.md`. If `project_state.md` describes architecture, entry points, registries, or workflow facts that the rule files contradict or omit, fix the rule files **before** proceeding with the task.
- **At the end of every substantive change** to `src/`, pipeline entry points, registries, research workflow, naming conventions, hard invariants, or anti-patterns, check whether `AGENTS.md`, `CLAUDE.md`, and the matching `.agents/rules/*.md` still describe reality. If not, update them in the same session as the change, not "later".
- **Drift signals to watch for:**
  - A module exists in `src/` but is not listed in `AGENTS.md` / `CLAUDE.md` / `src/system.md` (example: `src/research_orchestrator/` is the 6th top-level module — older copies of `src/system.md` may still describe a five-module layout). New file `src/research_orchestrator/prescription_runtime.py` (added 2026-04-28) is part of the validation profile and must be listed when describing the orchestrator's internals.
  - A pipeline entry point listed in the rule files no longer exists in `src/data_infra/pipeline/`, or a new one exists that is not listed.
  - A "deprecated" name in the rule files has actually been deleted, or a name marked live has been deprecated.
  - A scoped `AGENTS.md` inside a subdirectory contradicts this root contract.
  - A research convention recorded in `project_state.md` is missing from the rule files.
  - A registry was added, removed, or moved across `data/`.
  - A new built-in research profile was added to `src/research_orchestrator/profiles.py` (registered via `engine.py:_register_builtin_profiles`) but is not listed in the rule files. Current count: 7 (factor_screening, theme_strategy, event_driven_signal_research, ml_signal_model_research, strategy_improvement, benchmark_audit, hypothesis_validation).
- **Alignment contract.** `AGENTS.md` and `CLAUDE.md` must agree on substance even though they differ in tone and audience. The §1 context-refresh list, the module list, the hard invariants, the live entry points, the data ops rules, the research integrity rules, the backtest pipeline, the banned anti-patterns, and the venv path must read the same in both files. The Codex-specific subagent matrix in §8 of this file and the Claude-specific tool sections in `CLAUDE.md` are the legitimate places to diverge. If you update a rule in one file, update the matching rule in the other in the same edit pass.
- **Record rule changes in `project_state.md`** so the audit trail of how the agent contract evolved lives next to the audit trail of how the system evolved.

## 7. Scope and Overrides

More specific `AGENTS.md` files inside subdirectories refine these global rules for their subtree. When both apply, follow the more specific scoped file in addition to this root contract.

## 8. Subagent Workflow

- Prefer the repo-local custom agent names defined under `.codex/agents/` when the runtime supports direct custom-agent spawning.
- Treat the files under `.codex/agents/` as the source-of-truth agent definitions for this repository.
- If a restricted integration exposes only built-in agent types, fall back to the matching built-in `explorer` or `worker` and have it follow the corresponding repo-local role brief.
- The root Codex session remains the orchestrator. Do not spawn a child agent whose only job is orchestration.
- Start non-trivial work with `quant_context_mapper` so the affected module, entry points, and validation path are established before edits.
- Prefer parallel fan-out only for read-only work such as context gathering or review. Keep write-heavy work serialized unless file ownership is clearly disjoint.
- Use at most one writing agent per write scope. Parallel writers are acceptable only when they have explicitly disjoint file ownership and the parent agent can integrate the results safely.
- For `quant_impl_worker` and `quant_test_runner`, pass a self-contained assignment with the exact write scope, expected artifacts, and validation target. Do not rely on broad forked conversation context as the task definition for bounded write tasks.
- For `src/data_infra/` or backend/data-sync work, run `quant_data_guardian` before finalizing. Never run parallel Tushare fetchers against the same account.
- For `src/alpha_research/` or substantive `workspace/` research work, run `quant_research_guard` before accepting the result.
- For `src/backtest_engine/`, signal construction, or execution-logic changes, run `quant_backtest_auditor` before accepting the result.
- Treat `quant_test_runner` as the primary validation gate for any behavior-changing work. It may add or strengthen durable test assets and validation scripts, not just run existing checks.
- Any change to calculations, data pipelines, factors, backtests, execution logic, portfolio/risk logic, or result-analysis behavior must go through `quant_test_runner` before sign-off.
- Changes are not considered trusted until that validation `worker` either provides strong evidence of correctness or explicitly reports the remaining validation gap.
- After substantive changes, run `quant_test_runner` before a final read-only `quant_reviewer`; for high-risk numerical or methodology-sensitive work, prefer specialist guard review plus validation worker plus final reviewer.
- Keep `agents.max_depth = 1` unless nested delegation becomes demonstrably necessary. Prefer more precise top-level orchestration over recursive spawning.

### Restricted-Surface Fallback Mapping

| Custom agent | Built-in fallback | Use |
|-----------|-------------------|-----|
| `quant_context_mapper` | `explorer` | Read-only context mapping, entry-point discovery, validation-path discovery |
| `quant_impl_worker` | `worker` | Bounded implementation in an assigned write scope |
| `quant_test_runner` | `worker` | Validation ownership, test creation, and regression checking |
| `quant_data_guardian` | `explorer` | Read-only data-infrastructure safety review |
| `quant_research_guard` | `explorer` | Read-only research-integrity review |
| `quant_backtest_auditor` | `explorer` | Read-only backtest and signal-pipeline review |
| `quant_reviewer` | `explorer` | Final read-only correctness review |

### Default Spawn Matrix

| Task shape | Default spawn path |
|-----------|--------------------|
| Small local task | Stay in the root session unless the area is unfamiliar. |
| Medium single-module code task | `quant_context_mapper` -> optional domain guard -> `quant_impl_worker` -> `quant_test_runner` -> `quant_reviewer` |
| Large or cross-module task | `quant_context_mapper` plus relevant read-only guards in parallel -> one writer per disjoint scope -> `quant_test_runner` -> `quant_reviewer` |
| Data ingestion, storage, or Qlib backend work | Always include `quant_data_guardian` and `quant_test_runner`; never parallelize Tushare fetchers. |
| Factor engineering, labels, experiments, or research scripts | Always include `quant_research_guard` and `quant_test_runner` before sign-off. |
| Signal construction, masks, execution logic, or parity work | Always include `quant_backtest_auditor` and `quant_test_runner` before sign-off. |
| Write-heavy work | Prefer one writer; parallel writers are allowed only when file ownership is clearly disjoint. |
