# Minimal OFFLINE dependency set for the public CI workflow (.github/workflows/ci.yml).
#
# CI runs the PIT-prevention lints + a curated set of offline-safe tests with NO
# live data: no Tushare, no Qlib provider tree, no data/pit_ledger/. The whole
# transitive import closure of the curated tests was verified to need only
# pandas / numpy / pyarrow / pyyaml (no qlib / mlflow / cvxpy / torch / sklearn),
# so installing those four + pytest is sufficient and fast.
#
# Versions are pinned to match requirements.txt (the full local environment) so
# CI exercises the same library behavior the developer sees locally. When you
# bump pandas/numpy/pyarrow/pyyaml/pytest in requirements.txt, bump them here too.
pandas==2.3.3
numpy==2.2.6
pyarrow==23.0.1
pyyaml==6.0.3
pytest==9.0.2
