# GPT 复审 #9 prompt — chain_v2.8 八轮修复验收(自包含,直接全文发给 GPT-5.5 Pro)

```text
ROLE
You are the same senior reviewer. Verdict history: #1 (5B) -> v2.1; #2 (4B) -> v2.2; #3 (3B) -> v2.3; #4 (3B) -> v2.4; #5 (3B) -> v2.5; #6 (2B+1M) -> v2.6; #7 (4B) -> v2.7; #8 REVISE (1B: truthiness fail-open in the shared publishable predicate — schema_valid=NaN resealed loads sealed_ok; 1M: route value types unvalidated — thinking="False" silently inverts) -> chain_v2.8. This is fix-verification round 8. Re-run your round-8 reproductions, hunt for new gaps, and issue SHIP or REVISE. Do not re-litigate previously approved designs or previously accepted deferrals.

REPO (public — fetch any file to verify)
https://github.com/henrydan111/quant-system   (branch: calendar-unfreeze, fix commit 669a33d)
Raw form: https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/<path>
Key files (under workspace/research/ai_research_dept/ unless noted): engine/integrity.py, engine/analyst_chain.py, engine/llm_config.py, platform/server.py, tests: workspace/research/ai_research_dept/tests/{test_v27_routing_publishable,test_v28_strict_predicate}.py (+ the earlier v22/v25/v26 suites). Ledger: INPUT_PROMPT_AUDIT_v1.md §10.8.

DISPOSITION — your round-8 findings (your exact replacements were applied):

BLOCKER STRICT PREDICATE: verify_publishable_archive now applies your exact replacement — `a["seats"][s].get("error") is not None` -> named problem; `bear.get("error") is not None` -> named; `bear.get("schema_valid") is not True` -> "bear.schema_valid 非严格布尔真". No truthiness remains in the predicate (parse_mode stays a strict two-string membership check). Regression matrix: schema_valid in {NaN, 1, "false", "true", [1], 0, None} all rejected with archive_complete False; falsey non-null errors {[], {}, 0, "", False} on a seat and {[], {}, 0, ""} on bear all named; the literal-None compatibility case asserted (existing v2.4-v2.7 archives store literal True/None — verified live: all four historical versions still load 1/2/2/2 sealed_ok under the strict predicate). LIVE re-shot of your exact attack: three copies of a REAL v2.8 archive with schema_valid set to NaN / 1 / "false", input-fp recomputed and correctly RE-SEALED, planted into the real chain_v2.8 day dir -> ALL THREE rejected at platform load with "bear.schema_valid 非严格布尔真" (rejected=3 in integrity_status, never loaded, thus never countable by validate_full_month_status).

MAJOR ROUTE VALUE VALIDATION: integrity.py now owns the canonical ROUTE_EXEC_KEYS and a shared verify_llm_route(route): model must be a non-empty str; thinking literal bool or None (isinstance bool — "False"/1/"true" all reject); temperature _num_ok in [0,2] (bool excluded by _num_ok); max_tokens a positive int with bool explicitly excluded (True/4000.0/"4000"/0/-5 all reject); fallback None or non-empty str. It is consumed at all three boundaries: ChainContract.load validates BOTH routing legs (replacing the presence-only check), call_with_config validates after the missing-key KeyError check (type violation -> ValueError, raised BEFORE any chat call — regression test stubs chat to assert unreachable), and the platform version gate validates both legs for every non-legacy version (invalid -> whole version rejected). LIVE re-shot of your reproduction: a self-consistent manifest with routing.scoring.thinking="False" planted as a new version -> whole version rejected: "routing[scoring]: thinking 必须是字面 bool 或 None"; the same mutation on a chain_v2.8-versioned manifest is rejected by ChainContract.load (regression test). Historical compatibility verified live: all v2.4-v2.7 stored manifest routes pass verify_llm_route (they were snapshotted from real TASK_LLM values).

VERSION BUMP per your R4: CHAIN_VERSION/RENDER_VERSION = chain_v2.8 (integrity.py + llm_config.py + analyst_chain.py all inside the 7-file engine contract hash); fresh manifest (schema 2, job_spec 16x149=2384, full scoring contract, validated routing) and a fresh 2-stock live smoke: 2/2 complete on attempt 1, composites 34.3->28.6 / 47.1->34.9, 8/8 strict-parsed bear refutations, executed_contract_sha256 == manifest_sha256, archive llm_config_hash == manifest's, bear.schema_valid is literal True on both; full_month_status.json not created (smoke scope); default_chain = chain_v2.8.

SELF-REVIEW PREFLIGHT: tests 224/224 (+39 round-8 regressions as listed). Residual notes for your judgement: (a) verify_llm_route bounds temperature to [0,2] (Ark/OpenAI-compatible range) — the live routes use 0.1; flag if you want a narrower pin (e.g. exactly the frozen value — though that is already enforced by the manifest fingerprint, since routing IS manifest content). (b) parse_mode remains a two-string membership check (strict/lenient) — strings, not booleans, so no truthiness exposure; a non-string value fails membership. (c) verify_archive_semantics itself still uses _num_ok for all numeric checks (bool excluded) and key-set equality — we re-audited it for truthiness after your finding and found none load-bearing; the one remaining .get with default is kill_switches (checked via isinstance list + all non-empty str, not truthiness).

REVIEW QUESTIONS
R1: Re-run your round-8 reproductions against 669a33d: (a) schema_valid=NaN / 1 / "false" correctly resealed -> rejected at engine (archive_complete False) AND platform (never loaded, never countable); falsey non-null seat/bear errors -> named; (b) thinking="False" (and 1 / "true") self-consistent manifest -> rejected at ChainContract.load AND whole-version rejected at platform; temperature/max_tokens/fallback abuse (bool/NaN/string/zero/negative) -> rejected; call_with_config raises before any LLM call on a type-invalid route.
R2: New gaps introduced? Grep the shared predicates for any remaining truthiness on load-bearing fields; judge residual notes (a)-(c) and anything else you find.
R3: Is the completion chain now closed end-to-end under adversarial inputs: manifest (typed, validated scoring + routing) -> contract (load-validated, deep read-only, executed verbatim incl. LLM routing) -> archive (required seals + strict publishable predicate, one shared definition engine/platform) -> terminal recount -> validated month marker?
R4: Final gate — is chain_v2.8 fit to spend the 149x16 full-month replay (NON_EVIDENTIARY), with previously-agreed deferrals unchanged?
OUTPUT FORMAT
- Issues ranked Blocker / Major / Minor with offending line quoted + exact replacement.
- Answer R1-R4 explicitly.
- Final line: SHIP / REVISE, plus the single most important residual risk.
```
