# GPT 复审 #5 prompt — chain_v2.4 四轮修复验收(自包含,直接全文发给 GPT-5.5 Pro)

```text
ROLE
You are the same senior reviewer. Verdict history: #1 REVISE (5B) -> v2.1; #2 REVISE (4B) -> v2.2; #3 REVISE (3B) -> v2.3; #4 REVISE (3B: seal downgrade by field deletion / frozen prompts not actually executed / missing-input silent exit 0) -> chain_v2.4. This is fix-verification round 4. Re-run your round-4 reproductions, hunt for new gaps, and issue SHIP or REVISE. Do not re-litigate previously approved designs or previously accepted deferrals.

REPO (public — fetch any file to verify)
https://github.com/henrydan111/quant-system   (branch: calendar-unfreeze, fix commit 66a33e6)
Raw form: https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/<path>
Key files (under workspace/research/ai_research_dept/ unless noted): engine/integrity.py, engine/analyst_chain.py, engine/validators.py, platform/server.py, platform/static/archives.html, src/ai_layer/scorecard.py, tests: tests/ai_layer/test_scorecard_v2_contract.py + workspace/research/ai_research_dept/tests/{test_v21_containment,test_v22_drift_guard}.py. Ledger: INPUT_PROMPT_AUDIT_v1.md §10.4.

DISPOSITION — your round-4 findings:
B1 REQUIRED-SEAL SCHEMA: manifest now declares integrity_schema=1 + sealed_required=True. integrity.verify_archive_body(require_sealed=True) hard-fails when ANY of REQUIRED_SEAL_FIELDS = {manifest_fp, artifact_fp, archive_sha256, cards, market_context} is missing, and recomputes the body seal + stored-input fp UNCONDITIONALLY (never gated on field presence). Engine reuse (verify_existing_archive) always passes require_sealed=True. The platform derives require_sealed from the version manifest's sealed_required flag; legacy is the EXPLICIT allowlist LEGACY_CHAINS = {chain_v1.0, v2.1, v2.2, v2.3} (never inferred from missing fields); a manifest that declares integrity_schema/sealed_required without manifest_fp is itself invalid (verify_manifest_body). LIVE SHOT: delete cards + re-seal -> rejected with 缺少必需封印字段 (also parametrized regression tests per deleted field).
B2 EXECUTED FROZEN CONTRACT: read_prompt_bundle() reads prompt files exactly once; build_manifest(pv, retr, regime, prompt_bundle) embeds them; ChainContract.from_verified_manifest(manifest) re-verifies the manifest body and carries manifest_fp + effective_prompts. run_stock signature now REQUIRES the contract (the bare manifest_fp="" parameter is deleted — unbound archives cannot be produced); run_seat and run_bear receive contract.effective_prompts[...] as the complete system prompt (SYSTEM_C15 already inside; no disk re-read anywhere in the execution path — grep confirms the only prompt read is read_prompt_bundle).
B3 MISSING-INPUT + WHOLE-RUN PREFLIGHT: _run_batch preflights ALL days x codes (build_inputs + fingerprint verification of existing archives) BEFORE any LLM submission; missing inputs -> run_status.json {status: failed_preflight, missing:[...]} + exit 2; after execution a mechanical assertion complete==expected drives exit 0/2 and run_status.json persists {status: complete|partial, expected, complete, failed, days, pool_size} as the durable month-completion marker; VersionCollisionError still cancels pending futures, writes status aborted_collision, and propagates.
MAJOR-1 ATTEMPTS: state machine started -> attempt_completed / attempt_failed -> published, all in an append-only day ledger written under cross-process file_lock + flush + os.fsync; attempt allocation under file_lock(attempts_root/.allocate.lock) as max(parsed attempt numbers)+1 with mkdir(exist_ok=False) (sparse {0001,0003} allocates 0004 — regression test); unexpected exceptions write status.json + attempt_failed to the ledger BEFORE propagating (no orphan attempts); the published ledger event is written only AFTER os.replace succeeds; a version-level .batch.lock (timeout 1s -> BatchInstanceError) mechanically blocks a second batch instance.
MAJOR-2 CONTRACT FILES: engine_contract_sha256 now covers 7 files (+integrity.py, +llm_config.py, +src/ai_layer/ark_client.py) hashed with project-relative path prefixes; PIT loaders excluded per your ruling (cards already bound by artifact_fp).
MAJOR-3 TOTAL FUNCTIONS: claim/reason must be non-empty str (else per-entry dropped.text — the 10**10000 claim that exploded _norm(str()) and killed valid siblings is a regression test, as are list/dict claims); _valid_final is a total function (bool/str/NaN/inf/-1/101/10**10000/list -> False, never raises); archive_complete additionally requires every seats[seat] and records[seat] to be a dict.
MAJOR-4 PLATFORM: verification state externalized to Data.archive_verification (the archive body is never mutated — writing _verify had invalidated the very seal being displayed); /api/dept returns verification as an external field; /api/meta exposes integrity_status per version {loaded, rejected, reasons[:10]} plus render_version; default_chain is RENDER_VERSION only — if the current version's manifest is damaged the platform does NOT silently fall back to an older version (default becomes empty while legacy versions stay browsable, and integrity_status shows the rejection); JSON parse failures are caught and counted as rejections.
MINOR-1: archive_sha256 and the new manifest_sha256 are FULL 64-hex sha256 digests (manifest_sha256 covers created_at; the 16-hex manifest_fp remains the short semantic identity and excludes it).
MINOR-2: archives.html gained a chain selector (days filled from archive_days_by_version) and its analysis links propagate chain.

SELF-REVIEW PREFLIGHT: tests 111/111 (+19 round-4 regressions). LIVE BATTERY on the persisted chain_v2.4 smoke: manifest schema=1/sealed_required=True/manifest_sha256 length 64/contract holds 4 effective prompts; SMIC complete on attempt 1 (bear 7 validated refutations, strict parse, composite 42.8 -> adj 34.4, seal length 64); fast-path reuse 0.02s; delete-cards+reseal downgrade attack rejected; ledger event sequence [started, attempt_completed, published]. Platform live: 5 versions indexed (149 legacy structural + sealed v2.4), integrity_status zero rejections, verification=sealed_ok served externally, contract_source=frozen_manifest. Residual concerns for you: (a) ChainContract carries manifest_fp + effective_prompts only — scoring params still execute from module constants (bound by the 7-file code hash; changing them changes engine_contract_sha256 hence manifest_fp) — acceptable, or must the judge also read weights from the contract object? (b) run_status.json is per-version single-file (a later --day smoke overwrites the month marker) — acceptable for NON_EVIDENTIARY replay bookkeeping, or should run history be append-only? (c) the .batch.lock covers the batch entry point (main/_run_batch); direct run_stock calls (smoke scripts) are not covered by the instance lock — they still go through attempt allocation locks and seal verification — acceptable? (d) LEGACY_CHAINS is a hardcoded platform constant — acceptable, or should legacy status live in each version's retro manifest?

REVIEW QUESTIONS
R1: Re-run your round-4 reproductions against 66a33e6: (a) delete each required seal field (incl. market_context) + re-seal -> rejected at engine reuse AND platform load; (b) mutate a prompt file after manifest creation -> executed prompts unchanged (grep the execution path for any disk prompt read); (c) missing-input month -> failed_preflight + exit 2; complete month asserts complete==expected; (d) sparse attempt numbering, forced publish failure (ledger must NOT say published), second batch instance blocked.
R2: New gaps introduced? Judge specifically the four residual concerns (a)-(d) and anything else you find.
R3: Is the completion-marker design (run_status.json + per-day attempts ledger) now sufficient to distinguish a crashed partial month from a complete one, on both the batch side and the platform side?
R4: Final gate — is chain_v2.4 fit to spend the 149x16 full-month replay (NON_EVIDENTIARY), with previously-agreed deferrals unchanged?
OUTPUT FORMAT
- Issues ranked Blocker / Major / Minor with offending line quoted + exact replacement.
- Answer R1-R4 explicitly.
- Final line: SHIP / REVISE, plus the single most important residual risk.
```
