# GPT 复审 #8 prompt — chain_v2.7 七轮修复验收(自包含,直接全文发给 GPT-5.5 Pro)

```text
ROLE
You are the same senior reviewer. Verdict history: #1 REVISE (5B) -> v2.1; #2 (4B) -> v2.2; #3 (3B) -> v2.3; #4 (3B) -> v2.4; #5 (3B) -> v2.5; #6 (2B+1M) -> v2.6; #7 REVISE (4B: frozen routing not executed / nested weights unvalidated, NaN fail-open / platform vs engine archive predicate mismatch / marker status text unverified) -> chain_v2.7. This is fix-verification round 7. Re-run your round-7 reproductions, hunt for new gaps, and issue SHIP or REVISE. Do not re-litigate previously approved designs or previously accepted deferrals (seat names not pinned; reconciliation report-only; the genuine full-month acceptance path validated by unit tests + the first replay itself).

REPO (public — fetch any file to verify)
https://github.com/henrydan111/quant-system   (branch: calendar-unfreeze, fix commit cbb4beb)
Raw form: https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/<path>
Key files (under workspace/research/ai_research_dept/ unless noted): engine/integrity.py, engine/analyst_chain.py, engine/llm_config.py, platform/server.py, tests: workspace/research/ai_research_dept/tests/{test_v22_drift_guard,test_v25_contract_scope,test_v26_scoring_month,test_v27_routing_publishable}.py. Ledger: INPUT_PROMPT_AUDIT_v1.md §10.7.

DISPOSITION — your round-7 findings (your exact replacements were applied):

B1 FROZEN ROUTING NOW EXECUTED: llm_config gains ROUTE_EXEC_KEYS = (model, thinking, temperature, max_tokens) and call_with_config(messages, route, task=...) which accepts ONLY an explicit route mapping — missing exec key raises KeyError; it never touches TASK_LLM; fallback comes from route.get("fallback"). The legacy call(task, ...) gate now delegates to it ({**TASK_LLM[task], **overrides}) and is documented as FORBIDDEN in the formal analyst chain — grep confirms run_seat/run_bear contain no L.call( and instead receive contract.routing["scoring"] / contract.routing["bear"] from _execute_attempt (the only other L.call site in analyst_chain.py is gone). ChainContract gains an llm_config_hash field loaded from the manifest; load() additionally validates BOTH routing legs carry all ROUTE_EXEC_KEYS and that llm_config_hash is non-empty; verify_contract_matches_manifest now also compares llm_config_hash. The archive writes "llm_config_hash": contract.llm_config_hash — the live L.llm_config_hash() call is gone from the execution path (the only remaining live call is inside build_manifest, i.e. at freeze time, which is correct). Your reproduction was re-run live against the real chain_v2.7 contract with ai_layer.ark_client.chat stubbed to capture params and TASK_LLM["dimension_scoring"]/["bear_rebuttal"] tampered to "tampered-model": executed model == frozen doubao-seed-2.0-pro / deepseek-v4-pro on both legs (drift closed); the regression test additionally proves the OLD L.call gate does pick up the tampered value — demonstrating the fix is load-bearing.

B2 NESTED WEIGHTS + NaN CLOSED: verify_scoring_contract applies your exact replacement — every seat_weights[seat] must be a non-empty dict with all values _num_ok in [0,100]; every composite_weights value _num_ok in [0,1]; sum(composite) == 1 within 1e-12. verify_archive_semantics' composite recompute gains your second defense: `if not _num_ok(recomputed, 0, 100) or abs(recomputed - float(comp)) > 0.11`. Your NaN reproduction was re-shot live: a self-consistent version manifest with composite_weights["fund"]=NaN planted next to the real versions -> the WHOLE version rejected at platform load with "composite_weights 必须是 [0,1] 有限数". Regression tests: NaN composite; 6 bad seat-dims cases (empty/str/NaN/negative/101/bool); 3 sum-violation cases; the direct semantics-level NaN second-defense test. Historical compatibility verified live: v2.4/v2.5/v2.6 stored scoring contracts all pass the strengthened validator (their COMPOSITE_W sums to 1.0 within 1.1e-16).

B3 UNIFIED PUBLISHABLE PREDICATE: integrity.py gains verify_publishable_archive(a, seat_weights, composite_weights) = verify_archive_semantics + per-seat error check + bear error/schema_valid/parse_mode∈{strict,lenient}. Engine archive_complete is now a bool wrapper around it (total function: any exception -> False; scoring=None test path keeps module constants); the platform's sealed-archive check calls verify_publishable_archive instead of the weaker semantics-only check. Your reproduction was re-shot live: a sealed, executed-contract-valid archive with bear.schema_valid=False planted into the real chain_v2.7 day dir -> REJECTED at platform load with "bear.schema_valid 非真" (and therefore can never be counted by validate_full_month_status, which only sees loaded archives). A regression test asserts per-case EQUIVALENCE archive_complete(a) == (not verify_publishable_archive(a, ...)) across clean/empty-bear/schema_valid=False/seat-error/forged-composite cases.

B4 MARKER STATUS TEXT: validate_full_month_status applies your exact replacement — actual_status = "complete" if len(seals)==job_spec.expected else "partial"; status.get("status") != actual_status is a named problem. Your reproduction re-shot live: a marker with genuine job hash but complete=2384/status="complete" while only 2 verified archives exist -> integrity_failed with THREE problems named including "status=complete 与重算状态 partial 不符". Regression tests: partial-forged-as-complete, complete-downgraded-to-partial, missing status, illegal status text, genuine complete accepted, genuine partial accepted.

VERSION BUMP per your R4: CHAIN_VERSION/RENDER_VERSION = chain_v2.7 (integrity.py/analyst_chain.py/llm_config.py all inside the 7-file engine contract hash). Fresh chain_v2.7 manifest (schema 2, job_spec 16x149=2384) + fresh 2-stock live smoke: 2/2 complete on attempt 1, composites 34.3->25.4 / 40.1->33.0, 6/3 strict-parsed bear refutations, executed_contract_sha256 == manifest_sha256 AND archive llm_config_hash == manifest llm_config_hash on both; full_month_status.json not created (smoke scope); platform baseline after battery plants removed: v2.7 loads 2 sealed_ok, v2.6/2 v2.5/2 v2.4/1 all intact under the strengthened validators, default_chain=chain_v2.7.

SELF-REVIEW PREFLIGHT: tests 185/185 (+26 round-7 regressions listed above; the one prior-fixture update: the round-6 genuine-marker fixture gained the now-required status field). Note on residual scope, for transparency: call_with_config validates key PRESENCE of the four exec keys but not their value types (a route with thinking="yes" would be passed through to ark_client.chat) — the routing legs come only from a verified manifest whose values were snapshotted from TASK_LLM at freeze time, and ark_client is inside the engine contract hash; flag if you want value-type validation added. Also, the intelligence-layer callers (event typing, regime brief, report pipeline) still use the task-name L.call gate by design — they run OUTSIDE the sealed analyst chain and their outputs enter archives only through the artifact_fp-bound cards; ruled in-scope-acceptable in earlier rounds, restate if you disagree.

REVIEW QUESTIONS
R1: Re-run your round-7 reproductions against cbb4beb: (a) tamper TASK_LLM after contract load -> executed model/thinking/temperature frozen (grep run_seat/run_bear for any L.call( or TASK_LLM read); archive llm_config_hash sourced from the contract; (b) NaN/out-of-range/sum!=1 nested weights -> version rejected at both ChainContract.load and platform; NaN recompute second defense present at the semantics layer; (c) bear.schema_valid=False (or seat error / bad parse_mode) sealed archive -> rejected at platform load AND archive_complete False — one shared predicate, no divergence; (d) status text forging (partial<->complete) -> integrity_failed.
R2: New gaps introduced? Judge the two residual notes (route value-type validation; intelligence-layer L.call scope) and anything else you find.
R3: Are the two links you flagged in round 7 now closed — (i) every executed parameter (prompts, scoring, routing, llm config identity) is bound to the frozen manifest such that no post-load mutation of any module global can change execution or go undetected in the archive; (ii) engine terminal recount and platform completion use one identical qualified-archive definition plus a fully-validated marker?
R4: Final gate — is chain_v2.7 fit to spend the 149x16 full-month replay (NON_EVIDENTIARY), with previously-agreed deferrals unchanged?
OUTPUT FORMAT
- Issues ranked Blocker / Major / Minor with offending line quoted + exact replacement.
- Answer R1-R4 explicitly.
- Final line: SHIP / REVISE, plus the single most important residual risk.
```
