# GPT 复审 #6 prompt — chain_v2.5 五轮修复验收(自包含,直接全文发给 GPT-5.5 Pro)

```text
ROLE
You are the same senior reviewer. Verdict history: #1 REVISE (5B) -> v2.1; #2 REVISE (4B) -> v2.2; #3 REVISE (3B) -> v2.3; #4 REVISE (3B) -> v2.4; #5 REVISE (3B: forgeable ChainContract + scoring from mutable globals / platform downgrade via self-declared sealed_required=false / completion scope unbound) -> chain_v2.5. This is fix-verification round 5. Re-run your round-5 reproductions, hunt for new gaps, and issue SHIP or REVISE. Do not re-litigate previously approved designs or previously accepted deferrals.

REPO (public — fetch any file to verify)
https://github.com/henrydan111/quant-system   (branch: calendar-unfreeze, fix commit 6fed5d3)
Raw form: https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/<path>
Key files (under workspace/research/ai_research_dept/ unless noted): engine/integrity.py, engine/analyst_chain.py, engine/validators.py, platform/server.py, src/ai_layer/scorecard.py, tests: workspace/research/ai_research_dept/tests/{test_v21_containment,test_v22_drift_guard,test_v25_contract_scope}.py + tests/ai_layer/test_scorecard_v2_contract.py. Ledger: INPUT_PROMPT_AUDIT_v1.md §10.5.

DISPOSITION — your round-5 findings:

B1 CONTRACT ANTI-FORGERY + EXECUTED SCORING: ChainContract can now ONLY be constructed via ChainContract.load(vdir), which reads the version's manifest.json from disk and validates: verify_manifest_body (recomputed fp + full sha256), chain_version == CHAIN_VERSION, integrity_schema == 2, sealed_required == True, manifest_sha256 present, effective prompts non-empty with per-file sha256 EQUAL to the manifest's declared hashes, and scoring_contract present. All contract payloads (effective_prompts / scoring / routing) are deep read-only via MappingProxyType recursion (_deep_ro) — your "frozen dataclass doesn't freeze inner dicts" attack now raises TypeError. run_stock's FIRST statement is verify_contract_matches_manifest(contract, vdir): it re-loads a fresh contract from disk and compares manifest_sha256 + manifest_fp + effective_prompts + scoring + routing — a hand-instantiated ChainContract carrying the REAL manifest_fp but mutated scoring is rejected before any lock/ledger/LLM work. Scoring params now EXECUTE from the contract: run_seat receives contract.scoring["seat_weights"][seat], run_bear receives contract.scoring["seat_weights"], judge(seat_results, bear, scoring=contract.scoring) reads seat/composite weights + bear_discount_strength + divergence_gap from it, and archive_complete(archive, contract.scoring) validates against it — mutating module globals (SEAT_WEIGHTS etc.) after manifest creation has NO effect on execution. Every archive now carries the REQUIRED seal field executed_contract_sha256 = contract.manifest_sha256; engine reuse (verify_existing_archive) and platform load both compare it against the version manifest's manifest_sha256.

B2 PLATFORM DOWNGRADE CLOSED: legacy = the explicit allowlist LEGACY_CHAINS {v1.0, v2.1, v2.2, v2.3} ONLY (chain_v2.4 is NOT legacy — it is sealed under schema 1). For every non-legacy version, require_sealed is UNCONDITIONALLY True; the manifest itself must declare sealed_required=True + integrity_schema>=1 (int) + manifest_sha256, or the ENTIRE version is rejected at load ("自声明降级封死") — sealed_required:false is a rejection reason, not a verification switch. Required seal fields are tiered by the manifest's integrity_schema (required_seal_fields(schema): 1 = the v2.4 five-field set; 2 = +executed_contract_sha256) so the new field does not retroactively reject genuinely-sealed v2.4 archives, while the v2.5 ENGINE only produces and only accepts schema 2 (ChainContract.load refuses schema!=2; verify_existing_archive uses the default strictest tier). Platform sealed loads additionally pass expect_executed_contract=manifest_sha256 (schema>=2) and the shared verify_archive_semantics with the manifest's frozen scoring_contract.

B3 COMPLETION SCOPE BINDING: build_manifest now embeds job_spec = {scope_kind: full_month, days (all 16), codes (all 149), expected: 2384, job_set_sha256} computed from the FULL month regardless of CLI args — main() derives full_days/pool BEFORE any --day/--names filtering. _run_batch computes this run's scope sha (its actual days×codes) and is_full_scope = (scope sha == manifest job_spec sha); smoke runs get scope_kind=smoke. full_month_status.json is written ONLY when is_full_scope — a smoke run is mechanically incapable of touching the month marker (live-verified: after the v2.5 smoke, full_month_status.json does not exist). Run history is an append-only runs_ledger.jsonl (run_started / run_failed_preflight / run_aborted_collision / run_finished, file_lock + fsync, per-run run_id); run_status.json is demoted to a "latest run" convenience pointer. Completion is decided by a TERMINAL RECOUNT, not in-memory counters: _recount_published re-reads every planned (code,day) archive from disk, re-verifies the full seal chain (verify_existing_archive incl. executed-contract + preflight-computed artifact_fp) plus archive_complete under the contract scoring, and computes archive_set_sha256 over all passing seals; expected==verified drives exit 0/2 and both ledger + status records.

MAJOR-1 DIRECT-CALL + CONCURRENCY: run_stock raises MissingInputError (typed, never silent None) when build_inputs returns None — batch preflight still filters missing inputs beforehand; and the whole body from archive-check to publish runs under a per-(day,stock) cross-process file_lock (.{code}.stock.lock), closing the concurrent-double-run overwrite window for direct smoke-script calls that bypass .batch.lock.

MAJOR-2 ERROR TOTALITY + PUBLISH + RECONCILIATION: _safe_error(exc) is the single error formatter (RuntimeError(10**10000) no longer explodes the except path — regression test); os.replace publish failures write a publish_failed ledger event before propagating (published is only written after os.replace succeeds, unchanged); _reconcile_ledger runs at batch start: published-events-without-archive and archives-without-published-event are both reported into runs_ledger (run_started carries reconcile_anomalies) before any work.

MAJOR-3 SHARED SEMANTICS: verify_archive_semantics(a, seat_weights, composite_weights) lives in integrity.py and is consumed by BOTH engine archive_complete and platform load: seats/records key sets must equal the scoring contract's seat set; every records[s].factor_scores must be a list; every final/finals value a finite [0,100] number (bool excluded); judge.finals must equal seats[s].final per seat; judge.composite is RECOMPUTED from the contract's composite weights (±0.11); bear.refutations must be a list of dicts and kill_switches a non-empty list of non-empty STRINGS. Your three reproductions (empty records entry, string kill_switches, judge/seat mismatch) are regression tests and all named.

MINOR: platform manifest/archive parse excepts include ValueError; /api/meta exposes full_month_status per version (read from the engine-written file only).

SELF-REVIEW PREFLIGHT: tests 134/134 (+22 round-5 regressions: forged contract, deep read-only, downgraded manifest refused as contract, prompt-hash mismatch, missing/wrong executed_contract, schema tiers, job_spec scope, smoke-scope mismatch, MissingInputError, _safe_error huge-int, 6 semantics reproductions, platform version assertions). LIVE BATTERY on commit 6fed5d3 (persisted chain_v2.5): fresh 2-stock smoke on 20250127 — manifest schema=2/sealed_required=True/job_spec 16x149=2384; both stocks complete on attempt 1 (bear 6/4 validated refutations strict parse, composite 35.6->26.4 / 44.8->32.8, executed_contract_sha256==manifest_sha256, seal len 64); attempts ledger [started,started,attempt_completed,published,attempt_completed,published]; runs_ledger [run_started(scope=smoke, 0 reconcile anomalies), run_finished(complete 2/2, archive_set_sha256)]; full_month_status.json NOT created; fast-path reuse rerun 0.9s with zero new attempts and appended run pair. Attack shots all rejected: (1) hand-built ChainContract with real manifest_fp + mutated bear_discount_strength=999 -> rejected at verify_contract_matches_manifest AND at run_stock entry; (2) mutating contract.scoring -> TypeError; (3) swap executed_contract_sha256 + reseal -> rejected; (4) forge judge.finals/composite=99.9 + reseal -> body seal passes but semantics flags 3 problems, archive_complete False; (5) delete executed_contract_sha256 + reseal -> missing-required-seal-field; (6) direct run_stock with empty frames -> MissingInputError; (7) planted whole fake version dir with self-consistent manifest declaring sealed_required=false -> ENTIRE version rejected at platform load with the downgrade-closed reason, its forged composite=99.9 archive never loaded; v2.5 loads 2 sealed_ok (schema2), v2.4 loads 1 sealed_ok (schema1), default_chain=chain_v2.5, full_month_status={}.

RESIDUAL CONCERNS for your judgement:
(a) The schema-tier design means a hypothetical FUTURE version could ship a self-consistent manifest declaring integrity_schema=1 and thereby avoid the executed-contract binding at PLATFORM load (engine cannot produce it — ChainContract.load refuses schema!=2). The platform would still require full schema-1 seals. Acceptable display-layer posture, or should the platform pin schema>=2 for any version string > chain_v2.4?
(b) _reconcile_ledger reports anomalies into runs_ledger but does not block the run (report-only). Should archive_without_published_event (the os.replace-then-crash window) block instead?
(c) The per-stock lock is held across the whole LLM execution (~3min) — a concurrent duplicate direct call blocks rather than fails fast. Acceptable?
(d) verify_contract_matches_manifest re-reads manifest.json once per run_stock call (2384 reads for a full month). Accepted as cheap; flag if you disagree.

REVIEW QUESTIONS
R1: Re-run your round-5 reproductions against 6fed5d3: (a) forge ChainContract with real manifest_fp + altered weights/prompts -> must be rejected at run_stock entry; mutate SEAT_WEIGHTS/COMPOSITE_W globals after manifest creation -> executed scoring unchanged (grep the execution path: run_seat/run_bear/judge/archive_complete must read only contract.scoring); (b) manifest with sealed_required:false / missing manifest_sha256 / integrity_schema=0 -> whole version rejected at platform load; delete or swap executed_contract_sha256 -> rejected at engine reuse AND platform load; (c) run a --day/--names smoke -> full_month_status.json untouched; only a 16x149 run whose scope sha equals job_spec can write it; terminal recount decides exit code from disk, not memory.
R2: New gaps introduced? Judge specifically residual concerns (a)-(d) and anything else you find.
R3: Is the evidence chain now closed end-to-end: manifest (frozen contract+scope) -> executed contract (verified at run boundary, deep read-only, scoring executed from it) -> per-archive seal (executed_contract_sha256) -> terminal recount (archive_set_sha256) -> platform (unconditional seals, shared semantics, month status only from engine)?
R4: Final gate — is chain_v2.5 fit to spend the 149x16 full-month replay (NON_EVIDENTIARY), with previously-agreed deferrals unchanged?
OUTPUT FORMAT
- Issues ranked Blocker / Major / Minor with offending line quoted + exact replacement.
- Answer R1-R4 explicitly.
- Final line: SHIP / REVISE, plus the single most important residual risk.
```
