# GPT 复审 #2 prompt — chain_v2.1 REVISE 修复验收(自包含,直接全文发给 GPT-5.5 Pro)

```text
ROLE
You are the same senior reviewer who issued the REVISE verdict on the LLM stock-scoring product's input upgrade (chain_v2.0). This is the FIX-VERIFICATION round: every Blocker you raised has been implemented as chain_v2.1. Your job is to verify each fix actually closes the hole (re-run your original reproductions mentally or against the code), hunt for NEW gaps the fixes introduced, and issue SHIP or REVISE. Do not rubber-stamp; do not re-litigate design decisions you already approved (three-section regime card, scheme B, returned-set quotas, fixed-ID grounding).

REPO (public — fetch any file to verify against live code)
https://github.com/henrydan111/quant-system   (branch: calendar-unfreeze, fix commit 47bb69c)
Raw form: https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/<path>

FILES THAT CARRY THE FIXES (all pushed):
- https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/src/ai_layer/scorecard.py  (B2)
- https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/workspace/research/ai_research_dept/engine/validators.py  (B3+B4, new module)
- https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/workspace/research/ai_research_dept/engine/analyst_chain.py  (B1 wiring, strict validation flags, falsifier registry, bear payload)
- https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/workspace/research/ai_research_dept/engine/cards.py  (B4 fixed-ID registries + M3/M4/M5/M6 card fixes)
- https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/workspace/research/ai_research_dept/engine/regime_brief.py  (B5 + limit-history-from-bins + margin-change percentile)
- https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/workspace/research/ai_research_dept/engine/pv_pack.py  (B5 + unit fixes)
- https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/workspace/research/ai_research_dept/engine/fact_table.py  (median/p90 benchmark cols + explicit-NaN rows)
- https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/workspace/research/ai_research_dept/engine/retrieval.py  (source/visible_at passthrough)
- https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/workspace/research/ai_research_dept/platform/server.py  (B1 version-aware platform)
- https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/tests/ai_layer/test_scorecard_v2_contract.py
- https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/workspace/research/ai_research_dept/tests/test_v21_containment.py
- Disposition ledger (§10 of the audit doc, authoritative):
  https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/workspace/research/ai_research_dept/INPUT_PROMPT_AUDIT_v1.md

SELF-REVIEW PREFLIGHT — completed before this request: all 5 Blockers + all feasible Majors implemented; tests 50/50 green (19 pre-existing scorecard + 31 new contract tests, full tests/ai_layer regression per house rule after touching a shared gate); persisted v2.1 smoke (SMIC 20250127) into the real version tree with manifest. Empirical fix confirmations: regime/pv universe now resolves 5455 instruments over the full lookback window (exactly your probe's number vs the survivor-filtered 5403); fact table gained +240 explicit-NaN valuation rows (matches your 12-no-PE/3-no-dividend count over 16 days); 20250127 max 连板 corrected 1 -> 10 after bins-based warm-up; margin 20d-change percentile flipped 79% (level, misleading) -> 9% (change series). The smoke itself surfaced TWO additional real model behaviors, both fixed and disclosed for your judgment: (a) a seat emitted what_could_weaken as a bare dict — engine-side normalization now wraps/filters/caps falsifiers to the valid subset (<=5) BEFORE strict validation, so a malformed auxiliary field no longer kills a whole seat; (b) the bear model embedded raw newlines inside a quoted whole line, breaking strict JSON — the bear path (only) falls back to json.loads(..., strict=False) on the extracted object; the shared parse_json_reply used by the frozen MVP product is untouched, and bear output is fully post-validated by validate_bear_record anyway. Residual concerns handed to reviewer: R-a) is silent-drop normalization of malformed falsifiers acceptable containment posture, or should it be counted/logged into the archive (it currently is not surfaced per-entry)? R-b) is the bear-only lenient parse acceptable given typed post-validation? R-c) in the smoke, ALL 8 bear refutations claimed strength 5 and were mechanically downgraded to 4 for lacking falsifier_id — guard works, but should the prompt more strongly steer the model to reserve 5 for genuine falsifier hits? R-d) deferred items (已披露至 via loader API extension; top_inst/block-trade/holder rows; class-①b readiness gate implementation per your Q10 spec; golden set per your Q12 protocol) — acceptable to defer past the #10 full-month replay?

WHAT CHANGED — Blocker-by-Blocker disposition (authoritative detail in the audit doc §10):
B1 VERSION ISOLATION: archives now live under analyst_chain/<chain_version>/<date>/ with a per-version manifest.json (chain_version, config_hash, llm_config_hash, prompt files + sha16, fact/pv/retrieval/regime versions, evidence_class). run_stock reuses an existing artifact ONLY when its chain_version equals the current CHAIN_VERSION. The 149 legacy archives were migrated to chain_v1.0/ with a retroactive manifest that states the v1 prompt files were later edited in place (not a frozen snapshot). The platform is version-aware: /api/archives + /api/dept + /api/reasoning take a chain param; non-current versions serve archive+raw with an honest no-render notice instead of re-rendered current cards; RENDER_VERSION==CHAIN_VERSION is asserted by test (platform may not import the orchestration module). A v2.1 smoke artifact + manifest are persisted in the real tree.
B2 DUPLICATE DIMS: validate_scorecard_record rejects duplicate factor/penalty names UNCONDITIONALLY (your 50->100 reproduction is a regression test). Strict extras are opt-in kwargs used by the chain (require_registered_exact: every registered dim exactly once, no unregistered names, score_0_5 finite in [0,5] hard-fail; falsifier_schema: bounded list of exactly {condition, observable_in in |-joined subset of fund/tech/news/market}) so the frozen MVP forward product's default validation semantics are unchanged.
B3 BEAR TYPED VALIDATION: validators.validate_bear_record — exact key set (falsifier_id optional), target_seat/target_dim must pair against SEAT_WEIGHTS, strength finite in [0,5] (your 999 exploit is a regression test), bounded text, counter_quote must be an EXACT whole card line (via the same line-ID matcher). strength==5 is kept only with a falsifier_id present in the run's falsifier registry (ids fund-0/tech-1/... assigned to each seat's validated falsifiers and fed to the bear in its payload); otherwise mechanically downgraded to 4. The judge consumes only validated refutations.
B4 MECHANICAL FENCES: validators.enforce_v2_evidence runs between strict validation and compute — every evidence span must be [ID] + the complete normalized line (substrings/paraphrases rejected); exclusivity is by line-ID set across factors AND penalties; news dims with any indirect/aggregate evidence are deterministically clamped to <=3; the disclosure line (FD1) is droppable from any dim except earnings_inflection. Line IDs moved to FIXED SEMANTIC REGISTRIES (FUND_FIELD_ID by field name F01-F17, SERIES_FIELD_ID FS1-4, FD1, FB positional-within-bounded-section; PV_ITEM_ID per item TA/TB/TC/TD/TE; news dynamic sections use deterministic importance->visible_at->event_id sort before ND/NDA/NX/NI/NIA numbering). A 160-char span cap in the matcher aligns enforce with scorecard._span_is_grounded so enforce/compute can never disagree (the adj>final class); news titles are truncated to 70 chars at render so legitimate whole lines stay under the cap.
B5 SURVIVORSHIP: regime_brief and pv_pack resolve D.list_instruments over the FULL lookback window (start_amt / mkt_start), aggregating each day with day-wise NaN exclusion; a layout regression test guards the archive-version structure and the rebuild log confirms 5455 instruments.
MAJORS: limit-temperature history (counts, 连板 streaks, prior-day limit-up premium) computed directly from up/down-limit bins over the full 400-day window with trading-calendar semantics (event-store dependency dropped — fixes the no-warm-up first-day means); margin percentile computed on rzye.pct_change(20) (regime + pv, [-21] base); netflow strength net/(amount/10) (was 100x off); margin buy ratio (rzmre元/1000)/amount(千元); red-flag absence via type+title predicates (the 董监高增减持|减持 case is covered); news indirect detail lines carry age+stars, aggregates grouped by channel x type with age ranges, direct same-source (event_type, source)<=2 with overflow aggregation (retrieval passes source through; config/snapshot unchanged); series section shows sample dates and computes true second-order labels (zero-crossing first, then accel/decel from successive diffs, consecutive-equal samples deduped); NaN valuation rows render explicit states (亏损/未披露 — never silently dropped); bear payload includes untruncated factor evidence, penalties with evidence, and market_context; fund card carries industry median + p90 benchmark values; event titles rounded to 1dp; market cap switches to 万亿 at 1e4亿; member-equal-weight return renamed 申万L1成分等权 (not "index").

SMOKE EVIDENCE (SMIC 688981.SH @ 20250127, persisted at analyst_chain/chain_v2.1/20250127/):
- 3 seats full dim coverage (4/4, 5/5, 4/4); fence stats all zero (models complied with the whole-line contract);
- bear: 8/8 refutations passed typed validation (0 dropped for keys/pairing/strength/quote); all 8 claimed strength 5 and were mechanically downgraded to 4 (no falsifier_id echoed); refutations are substantive (ROE 1.9%@35分位 vs profitability claim, net-profit -26.4% vs growth claim, etc.);
- composite 39.2 -> bear-adjusted 22.0 (dispersion 11.5).

REVIEW QUESTIONS
R1: For each Blocker B1-B5, is the fix complete against your original reproduction? Specifically re-check: (a) any archive reuse path that bypasses the version match; (b) any compute path that still sums duplicates (e.g. penalties, judge recount); (c) any judge input not passing through validate_bear_record; (d) any evidence-bearing card line WITHOUT a registered ID that a model could still cite via substring (and whether that fails closed); (e) any remaining aggregate computed on a decision-month universe (attention? event store market events?).
R2: Did the fixes introduce new holes? Judge specifically: the falsifier silent-drop normalization (R-a), the bear-only strict=False JSON fallback (R-b), the 160-char alignment + 70-char title truncation (could a truncated title collide with another line or break dedup?), fixed FB positional IDs within the biz section, and the exclusivity first-come policy (model ordering now determines which dim keeps a contested line — acceptable?).
R3: The strength-5 falsifier fast path saw 0 legitimate uses in the smoke (R-c). Keep as-is, strengthen the prompt, or change the anchor so 5 also covers mechanically-verifiable same-dim direct contradictions?
R4: Are the deferred items (R-d) acceptable to remain open through the #10 full-month replay (NON_EVIDENTIARY), given all are recorded in the §10 ledger with owners/phases?
R5: Final gate — is chain_v2.1 fit to spend the full-month replay LLM cost (149 names x 16 days), and is anything on your original Q8 minimum-set still missing?

OUTPUT FORMAT
- Issues ranked Blocker / Major / Minor, each with the offending line quoted and an exact suggested replacement.
- Answer R1-R5 explicitly.
- Final line: SHIP / REVISE, plus the single most important residual risk.
```
