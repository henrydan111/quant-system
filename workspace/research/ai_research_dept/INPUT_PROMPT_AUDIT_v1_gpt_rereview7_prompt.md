# GPT 复审 #7 prompt — chain_v2.6 六轮修复验收(自包含,直接全文发给 GPT-5.5 Pro)

```text
ROLE
You are the same senior reviewer. Verdict history: #1 REVISE (5B) -> v2.1; #2 REVISE (4B) -> v2.2; #3 REVISE (3B) -> v2.3; #4 REVISE (3B) -> v2.4; #5 REVISE (3B) -> v2.5; #6 REVISE (2B+1M: scoring contract fails open / full-month marker trusted unverified / future schema-1 downgrade) -> chain_v2.6. This is fix-verification round 6. Re-run your round-6 reproductions, hunt for new gaps, and issue SHIP or REVISE. Do not re-litigate previously approved designs or previously accepted deferrals (reconciliation report-only, 30s per-stock lock timeout, per-stock manifest reads were ruled acceptable in round 6).

REPO (public — fetch any file to verify)
https://github.com/henrydan111/quant-system   (branch: calendar-unfreeze, fix commit b34d290)
Raw form: https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/<path>
Key files (under workspace/research/ai_research_dept/ unless noted): engine/integrity.py, engine/analyst_chain.py, platform/server.py, tests: workspace/research/ai_research_dept/tests/{test_v22_drift_guard,test_v25_contract_scope,test_v26_scoring_month}.py. Ledger: INPUT_PROMPT_AUDIT_v1.md §10.6.

DISPOSITION — your round-6 findings (your exact replacements were applied):

B1 SCORING CONTRACT FAIL-OPEN CLOSED: integrity.py now defines REQUIRED_SCORING_KEYS = {seat_weights, composite_weights, bear_discount_strength, divergence_gap} and verify_scoring_contract(scoring) exactly per your replacement (dict check, four keys, seat_weights non-empty dict, composite_weights key-set == seat_weights key-set, bear_discount_strength _num_ok [0,5], divergence_gap _num_ok [0,100]). ChainContract._from_verified_manifest now calls it and rejects the contract on ANY problem — your reproduction (self-consistent manifest missing bear_discount_strength/divergence_gap) is a regression test and was re-shot live against the real chain_v2.6 manifest with the two keys stripped and the body re-fingerprinted: rejected with "scoring_contract 缺字段". judge() is exactly your replacement: scoring is None -> module constants (tests/legacy only); otherwise DIRECT INDEXING of all four params — no .get, no fallback; passing an incomplete dict raises KeyError (regression test), and mutating BEAR_DISCOUNT_STRENGTH/DIVERGENCE_GAP globals with a complete contract in hand leaves the verdict bit-identical (regression test asserts equality, including the strength-4 discount still firing under contract threshold 4 while the global says 999). archive_complete likewise direct-indexes scoring["seat_weights"]/["composite_weights"] inside its total-function try (malformed contract -> False, fail-closed). On the platform, verify_scoring_contract runs at VERSION level for every non-legacy chain — an invalid/missing scoring_contract rejects the whole version — and the per-archive semantics check is now UNCONDITIONAL for sealed archives (the `scoring.get("seat_weights")` gate is gone). Your empty-archive reproduction was re-shot live: a schema-2 archive carrying only the seal fields (valid seal, valid executed_contract) planted into the real chain_v2.6 day dir was rejected at load with "seats 键集与评分契约不符".

B2 MONTH MARKER VERIFIED: full_month_status.json loading moved to AFTER archive loading, and validate_full_month_status(status, manifest, version_archives) — a module-level pure function, unit-tested — requires exactly your five conditions: scope_kind=="full_month", job_set_sha256 == manifest.job_spec's, expected == job_spec.expected, complete == len(seals recomputed from the verified loaded archives over job_spec days×codes), archive_set_sha256 == sha256_json(sorted(seals)) with the same [day, code_underscore, archive_sha256] entry shape the engine's terminal recount writes. Valid -> exposed as-is; invalid -> {"status": "integrity_failed", "problems": [...]} plus an integrity_status reason and a warning log. Your forged-2384/2384 reproduction was re-shot live: planted marker with wrong job hash + forged set hash -> integrity_failed with all three mismatches named (job_set_sha256 / complete=2384 vs 2 on disk / archive_set_sha256). Regression tests also cover post-hoc archive deletion (complete-count mismatch), same-count archive swap (set-hash mismatch), smoke-scoped marker, and the genuine-marker acceptance path.

MAJOR SCHEMA-1 PINNED: SCHEMA1_CHAINS = frozenset({"chain_v2.4"}) and the version gate applies your exact replacement — schema==1 with any other name rejects the whole version. Live re-shot: planted chain_v9.9test with a self-consistent sealed_required=True schema=1 manifest (with a valid scoring contract) -> rejected: "integrity_schema=1 仅允许 chain_v2.4；后续版本必须 schema>=2".

VERSION BUMP per your R4: CHAIN_VERSION/RENDER_VERSION = chain_v2.6 (the fix changes integrity.py + analyst_chain.py, both inside the 7-file engine_contract_sha256, so v2.5 archives are now a sealed historical version — they still load sealed_ok under their own frozen v2.5 manifest, verified live). chain_v2.6 got a fresh manifest (schema 2, full scoring contract, job_spec 16x149=2384) and a fresh 2-stock live smoke: first run hit two TRANSIENT bear failures (one empty LLM reply, one JSON malformed beyond lenient parse) -> neither archive published, terminal recount honestly recorded run_finished status=partial complete=0 with both stocks named as no_archive; rerun allocated attempt 2 for both, healed, published, run_finished status=complete 2/2 (attempts ledger shows the full 10-event sequence started/attempt_completed(x2 incomplete) -> started/attempt_completed/published(x2)). full_month_status.json was never created (smoke scope). Composites 33.1->25.3 and 43.7->30.2 with 4/5 strict-parsed bear refutations; executed_contract_sha256 == manifest_sha256 on both.

SELF-REVIEW PREFLIGHT: tests 159/159 (+25 round-6 regressions: each REQUIRED_SCORING_KEYS key individually missing; bad value domains incl. bool/NaN/999/negative/string; load rejects incomplete contract; judge KeyError on incomplete contract — never falls back; globals mutation inert with complete contract; archive_complete fail-closed on malformed contract; month-marker forged/deletion/swap/smoke-scope rejections + genuine acceptance; SCHEMA1_CHAINS pinned). Platform live baseline after all plants removed: v2.6 loaded 2 sealed_ok, v2.5 loaded 2 sealed_ok, v2.4 loaded 1 sealed_ok, legacy chains browsable, default_chain = chain_v2.6.

RESIDUAL NOTES for your judgement:
(a) verify_scoring_contract accepts any non-empty seat_weights key set — it does not pin the seat names {fund, tech, news}. Archive-level semantics then enforces seats == the contract's key set, so a manifest with renamed/extra seats would produce internally-consistent-but-different books only via a NEW manifest fp (engine code hash unchanged only if cards.py untouched — SEAT_WEIGHTS lives in cards.py which is inside the 7-file hash). We judged pinning seat names redundant; flag if you disagree.
(b) validate_full_month_status counts seals over job_spec days×codes from archives that ALREADY passed seal+semantics verification — a corrupted archive is thus counted as absent (complete-count mismatch), not as a distinct "corrupt" state. The integrity_status reasons list separately names the corrupt file. Acceptable?
(c) The genuine full-month acceptance path has unit-test coverage but no live shot (it requires the full 2384-archive replay this gate is protecting). The first full replay will be its live validation. Acceptable sequencing?

REVIEW QUESTIONS
R1: Re-run your round-6 reproductions against b34d290: (a) self-consistent manifest missing bear_discount_strength/divergence_gap -> ChainContract.load rejects; with a complete contract, mutate module globals -> judge output unchanged (grep judge/archive_complete for any .get fallback on scoring); (b) schema-2 archive with no seats/records/bear/judge but valid seals -> rejected at platform load; version with missing/invalid scoring_contract -> whole version rejected; (c) forged full_month_status (wrong job hash / wrong counts / wrong set hash / post-hoc deletion / same-count swap) -> integrity_failed, never exposed as-is; (d) future version self-declaring integrity_schema=1 -> whole version rejected.
R2: New gaps introduced? Judge residual notes (a)-(c) and anything else you find.
R3: Is the scoring-contract boundary now closed (no executed scoring parameter can differ from the frozen manifest without changing manifest_fp), and is the terminal-recount -> platform completion link now closed (no marker can claim completion the on-disk verified seal set does not support)?
R4: Final gate — is chain_v2.6 fit to spend the 149x16 full-month replay (NON_EVIDENTIARY), with previously-agreed deferrals unchanged?
OUTPUT FORMAT
- Issues ranked Blocker / Major / Minor with offending line quoted + exact replacement.
- Answer R1-R4 explicitly.
- Final line: SHIP / REVISE, plus the single most important residual risk.
```
