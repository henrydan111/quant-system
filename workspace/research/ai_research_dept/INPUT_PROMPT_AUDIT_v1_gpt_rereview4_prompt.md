# GPT 复审 #4 prompt — chain_v2.3 三轮修复验收(自包含,直接全文发给 GPT-5.5 Pro)

```text
ROLE
You are the same senior reviewer. Verdict history: #1 REVISE (5 Blockers) -> v2.1; #2 REVISE (4 residual Blockers) -> v2.2; #3 REVISE (3 Blockers: scoring contract not fingerprinted / disk-claimed fingerprints trusted / structurally-broken bear could be marked complete) -> chain_v2.3. This is fix-verification round 3. Re-run your round-3 reproductions, hunt for new gaps, and issue SHIP or REVISE. Do not re-litigate previously approved designs (M-line asymmetry, returned-set quotas, strength anchors, deferred items you already accepted in R4).

REPO (public — fetch any file to verify)
https://github.com/henrydan111/quant-system   (branch: calendar-unfreeze, fix commit 67bc0fd)
Raw form: https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/<path>
Key files (all under workspace/research/ai_research_dept/ unless noted): engine/integrity.py (NEW pure shared module), engine/analyst_chain.py, engine/validators.py, engine/event_store.py, engine/regime_brief.py, platform/server.py, platform/static/{analysis,department,stock}.html, src/ai_layer/scorecard.py, tests/ai_layer/test_scorecard_v2_contract.py, tests under workspace/research/ai_research_dept/tests/. Ledger: INPUT_PROMPT_AUDIT_v1.md §10.3.

DISPOSITION — your round-3 findings:
B1 SCORING CONTRACT IN FINGERPRINT: build_manifest now binds (a) engine_contract_sha256 = byte-level sha256 over analyst_chain.py + cards.py + validators.py + src/ai_layer/scorecard.py (name-prefixed, NUL-separated); (b) effective_prompts = SYSTEM_C15 + prompt file text per file, plus effective_prompt_sha256_by_file; (c) scoring_contract = {seat_weights, composite_weights, bear_discount_strength, divergence_gap}; (d) routing snapshot (dimension_scoring + bear_rebuttal TASK_LLM entries). LIVE-VERIFIED: bumping SEAT_WEIGHTS.fund.profitability_quality 6->7 changes manifest_fp and ensure_immutable_manifest raises VersionCollisionError.
B2 VERIFY-NOT-TRUST: new pure module engine/integrity.py (stdlib json+hashlib only — importable by the platform without touching orchestration) provides sha16_json / manifest_core_fp / input_artifact_fp / verify_manifest_body / verify_archive_body / is_sealed; chain and platform use the SAME functions. ensure_immutable_manifest re-fingerprints the on-disk manifest body (your tamper-body-keep-fp reproduction now raises 疑似篡改) and first-write goes through file_lock (repo's portalocker-backed lock) + tmp file + os.replace. Archives now carry archive_sha256 (sha over the body excluding that key); verify_existing_archive re-derives the stored-input fp from the archived cards/market_context, re-verifies the body seal, checks chain/date/stem/manifest_fp AND current-input artifact_fp; missing seal (pre-v2.3 body dropped into the dir) is itself a hard failure. LIVE-VERIFIED: judge.composite=99.9 with all fingerprints preserved -> VersionCollisionError (archive_sha256 封印不符). The platform Data loader validates every manifest (recomputed body fp + chain_version==dir) and every archive (verify_archive_body with expect_chain/date/stem, and manifest_fp for sealed archives) at load, refusing mismatches with warnings; legacy (unsealed) archives are indexed only after structural checks and tagged _verify=legacy_structural.
B3 STRICT COMPLETENESS: validate_bear_record returns schema_valid = top-level refutations/kill_switches/blind_spots are all lists (your {'refutations': {'not':'a list'}} case -> schema_valid False); archive_complete now requires seats keys == records keys == exactly the registered seat set,每席 final is a non-bool finite Real and error-free, bear is a dict with no error + schema_valid + parse_mode in {strict, lenient} + at least one kill_switch, and judge.finals covers exactly the seat set. Your one-seat/no-bear and three-seat/bear={} reproductions are regression tests (both now False). Per-entry validation_dropped>0 still counts as technically complete, per your ruling.
MAJOR-1 OVERFLOW: float(10**10000) guarded in validators (individual strength drop; sibling survives — regression test) and in scorecard (strict path -> ScorecardViolation; non-strict _valid_score -> NO-SCORE). Fixing this exposed a secondary bomb we caught ourselves: repr(10**10000) in the violation MESSAGE also trips CPython's int->str 4300-digit limit — the error path now uses a guarded repr.
MAJOR-2 BATCH SEMANTICS: incomplete results count as failures; main returns 2 if any failures else 0; VersionCollisionError cancels all pending futures and propagates (run aborts non-zero); a full fingerprint PREFLIGHT over existing archives (using the same build_inputs renderer as run_stock) runs before any LLM submission.
MAJOR-3 ATTEMPTS AUDIT: every attempt gets day/attempts/<code>/attempt_NNNN/{raw/, archive.json, status.json}; failed raws are never overwritten; an append-only day/attempts_ledger.jsonl records code/attempt/complete/fingerprints/seat+bear errors/timestamp/published; only COMPLETE archives are published to day/<code>.json via tmp+os.replace. Platform raw access resolves through the published archive's attempt number (safe_raw_dir with attempt, still traversal-guarded); legacy layouts still resolve.
MAJOR-4 FRONTEND CHAIN + FROZEN CONTRACT: analysis page propagates chain into dept/stock links; department and stock pages read chain from the URL and pass it to /api/dept, /api/stock, /api/reasoning; /api/dept serves prompts (the frozen effective_prompts incl. SYSTEM_C15), routing, weights, composite_w from the version's FROZEN manifest with contract_source=frozen_manifest (legacy versions: 'unavailable', current-version-without-manifest fallback labeled current_process). cards_source chip shown on the analysis page. LIVE-VERIFIED: /api/dept for chain_v2.3 returns contract_source=frozen_manifest, weights from manifest, prompt containing SYSTEM_C15; chain_v1.0 returns 'unavailable(legacy版本无冻结契约)'.
MINOR-1 STRENGTH-5 DOMAIN: the fast path now requires the counter_quote's line-ID domain (F*/FB/FS/FD->fund, T*->tech, N*->news, M*->market) to be inside the falsifier's declared observable_in set, in addition to fid existence and seat binding (observable_in=fund quoting M01 -> downgraded; regression test).
MINOR-2 WARM-UP BOUNDARY + FIRST-DAY M14: event_days now include one extra open day before win_start (rebuilt: 9,291 market events, 20 warm-up trading days); regime initializes prev_top5 from the trading day before the first decision day (first-day card now has [M14]; verified on 20250106).

SELF-REVIEW PREFLIGHT: tests 92/92 (+19 round-3 regressions incl. tamper-body-keep-fp, forged-output-seal, one-seat-complete reproduction, huge-int). Rebuilt events -> retrieval -> regime (regime llm_ok 14/16). Live battery on the persisted chain_v2.3 smoke: contract-drift collision; fast-path reuse 0.02s; tampered-composite rejection; attempts/attempt_0001 + ledger present; SMIC complete=true on attempt 1 (bear 8 refutations, strict parse, composite 41.9 -> adj 31.1). Residual concerns for you: (i) engine_contract_sha256 hashes source FILES — a change to imported helpers outside the four files (e.g. ai_layer/ark_client.py parse, pit loaders) is bound only via llm_config_hash/config_hash/data-version fields, not byte-level — acceptable boundary or extend the file list? (ii) effective_prompts embedded in every manifest (~8KB) — fine? (iii) the platform skips (refuses to index) invalid archives with a warning rather than hard-crashing the read-only server — acceptable posture? (iv) preflight renders cards twice per existing archive (preflight + run_stock) — pure CPU ~20ms/name, accepted for correctness.

REVIEW QUESTIONS
R1: Re-run your round-3 reproductions against 67bc0fd: (a) SEAT_WEIGHTS drift -> manifest_fp change + collision; (b) tamper manifest body keeping manifest_fp -> rejected at ensure AND at platform load; (c) tamper archive judge/cards keeping fingerprints -> rejected on reuse AND refused by the platform loader; (d) {'refutations': non-list} -> schema_valid False -> archive incomplete; one-seat and bear={} archives -> incomplete; (e) 10**10000 strength/score paths.
R2: New gaps introduced? Judge specifically: the four-file boundary of engine_contract_sha256 (residual concern i); ledger append under a process-local threading.Lock (single-process batch assumption — is that documented/acceptable?); attempts numbering by directory count (could a crash between mkdir and write create a gap/collision?); the platform's skip-with-warning posture (iii).
R3: Batch exit semantics — preflight + cancel-on-collision + exit 2: does anything still let a partial month masquerade as success (e.g. run_stock returning None for missing-input names is NOT counted as failure — correct since those names have no inputs, or should they be reported separately)?
R4: Final gate — is chain_v2.3 now fit to spend the 149x16 full-month replay (NON_EVIDENTIARY), with previously-agreed deferrals unchanged?
OUTPUT FORMAT
- Issues ranked Blocker / Major / Minor with offending line quoted + exact replacement.
- Answer R1-R4 explicitly.
- Final line: SHIP / REVISE, plus the single most important residual risk.
```
