# GPT 复审 #3 prompt — chain_v2.2 二轮修复验收(自包含,直接全文发给 GPT-5.5 Pro)

```text
ROLE
You are the same senior reviewer. Round-1 verdict: REVISE (5 Blockers) -> chain_v2.1. Round-2 verdict: REVISE (4 residual Blockers + 4 Majors, all with adversarial reproductions) -> chain_v2.2. This is fix-verification round 2. Re-run your round-2 reproductions against the new code, hunt for gaps the fixes introduced, and issue SHIP or REVISE. Do not re-litigate previously approved designs.

REPO (public — fetch any file to verify)
https://github.com/henrydan111/quant-system   (branch: calendar-unfreeze, fix commit 6f7de83)
Raw form: https://raw.githubusercontent.com/henrydan111/quant-system/calendar-unfreeze/<path>
Key files: engine/validators.py, engine/analyst_chain.py, engine/cards.py, engine/event_store.py, engine/regime_brief.py, engine/pv_pack.py, platform/server.py (all under workspace/research/ai_research_dept/), src/ai_layer/scorecard.py, tests/ai_layer/test_scorecard_v2_contract.py, workspace/research/ai_research_dept/tests/test_v21_containment.py + test_v22_drift_guard.py. Disposition ledger: INPUT_PROMPT_AUDIT_v1.md §10.2.

DISPOSITION — your round-2 findings, item by item:
B1' SAME-VERSION DRIFT: ensure_immutable_manifest — first write freezes manifest_fp (created_at excluded from the fingerprint); a later build whose fingerprint differs under the same CHAIN_VERSION raises VersionCollisionError (the on-disk manifest is never overwritten). Per-artifact binding: artifact_fp = sha256_json({manifest_fp, input_snapshot={cards, market_context}}) computed BEFORE the reuse check; an existing archive with a different artifact_fp (or chain_version) is a hard VersionCollisionError — never silent reuse; an archive that is incomplete (any seat error/None final, or bear.error) is recomputed and overwritten. Archives persist the exact input snapshot (cards + market_context) and the validated per-seat records. The platform serves the ARCHIVED snapshot for completed artifacts (cards_source="archive_snapshot"); live re-render only as an explicitly-labeled preview when no archive exists for the current RENDER_VERSION.
B3' PER-ENTRY BEAR ROBUSTNESS: validate_bear_record — key set must be all-string keys and exact; isinstance(str) checks on target_seat/target_dim BEFORE membership; strength rejects bool and non-Real, then finiteness+domain (your list-valued and 999 and "4" and NaN cases are parametrized regression tests); falsifier_id must be a str before the set/dict lookup; kill_switches/blind_spots must be lists (a str no longer iterates chars). Combined with B1' completeness rule, a failed/empty bear can never be fossilized — verified live in the smoke (see EVIDENCE).
B4' CLAMP BY REGISTERED ID CLASS: _news_requires_cap(lid) = lid=="N00" or lid.startswith(("NI","NDA")) — covers NI relation-channel details, NIA aggregates, NDA direct aggregates, and the census line; ND direct details and NX absence lines uncapped. Your [NDA] score-5 reproduction plus NDA-with-age / relation-NI / N00 cases are regression tests.
B5' PV FULL-WINDOW UNIVERSE: pv_pack resolves D.list_instruments over the 420-day 'start' window (your measurement: decision-month=5,403, 110d=5,403, 420d=5,455); the 60d RS D.features read stays bounded by mkt_start (read window != universe resolution window).
MAJOR-1 TWO-PASS EXCLUSIVITY: enforce_v2_evidence counts line-ID claims across ALL factor+penalty entries first, then voids every occurrence of any contested ID (order-independent; your reversed-entries 10->5 reproduction is a regression test asserting both orders produce identical all-dropped results; contested_ids recorded in fence stats).
MAJOR-2 TRAVERSAL: safe_raw_dir(base, known_chains, chain, day, code) — chain must be in the known set, day matches ^\d{8}$, code matches ^\d{6}\.(SH|SZ|BJ)$, and the RESOLVED path must be relative to base/chain (your day=../chain_v1.0/... injection is a regression test and returns HTTP 400 live). chain is validated on /api/archives, /api/dept, /api/reasoning.
MAJOR-3 BEAR MARKET CONTEXT: the bear payload now includes market_context; regime card v0.4 carries fixed line IDs M01-M16 so market lines are quotable counter-evidence through the same whole-line matcher; the falsifier registry is {fid: {seat, observable_in}} and the strength-5 fast path additionally requires falsifiers[fid].seat == refutation.target_seat (cross-seat test included). R3 adopted: the bear prompt anchor now reads 5 = seat-bound mechanically-verified registered falsifier hit ONLY; 4 = same-dim direct contradiction or cross-evidence conflict.
MAJOR-4 EVENT WARM-UP: event_store computes open event_days from win_start (30 calendar days before the first decision day) through the last decision day and feeds them to the market/suspension/report_rc generators (rebuilt store: 16,480 events including 19 warm-up trading days; market events now start 2024-12-09, not 2025-01-07).
MINORS: scorecard entry names must be non-empty strings with seen-set dedup (NaN/non-string names close the unconditional-contract gap; parametrized tests); canonical _t70 title truncation at every news render site; event_id hashes the FULL normalized title + source (display truncation only); attention explicitly labeled 金股池内截面分位/非LLM输入 on the platform; falsifier normalization loss counters archived per seat (falsifier_norm); bear parse_mode recorded and a lenient-parse failure re-raises as ArkClientError. FB positional IDs retained per your acceptance (inputs now frozen per-archive).

SELF-REVIEW PREFLIGHT: tests 73/73 (was 50; +23 round-2 regressions). Rebuilt events -> retrieval -> pv -> regime (regime_v0.4). LIVE EVIDENCE of the completeness machinery working end-to-end in the persisted smoke (analyst_chain/chain_v2.2/20250127/688981_SH.json): run-1 bear hit a transport blip (empty reply) -> archive written with complete=false; run-2 detected the incomplete archive and RECOMPUTED (not reused) -> bear succeeded (parse_mode=strict, 5 validated refutations incl. a falsifier-derived S4 增收不增利 attack and a market-aware chip attack citing the D-1 margin-change percentile), composite 37.4 -> bear-adjusted 27.6, complete=true with full input snapshot; run-3 fast-path reuse returned in 0.02s with zero LLM calls; a tampered manifest_fp raised VersionCollisionError. Platform live: cards_source=archive_snapshot for the completed artifact; traversal and unknown-chain requests return 400. Residual concerns handed to you: (i) VersionCollisionError inside the day loop fails per-name (noisy but fail-closed) — acceptable? (ii) the reuse fast path re-renders cards each call to recompute artifact_fp (cost ~10ms/name; correctness first) — fine? (iii) market lines are quotable by the bear but seat evidence (three long seats) may NOT cite M-lines because each seat's line_map is its own card only — intended asymmetry (market context calibrates, never scores) — confirm.

REVIEW QUESTIONS
R1: Re-run your four round-2 Blocker reproductions against 6f7de83 — closed? Specifically: (a) change any card input under CHAIN_VERSION=chain_v2.2 and confirm no silent reuse AND no manifest overwrite path remains (including the smoke-script path which calls ensure_immutable_manifest directly); (b) feed the malformed bear entries (list seat/dim/fid, bool/str/NaN strength, str kill_switches) and confirm per-entry drops with the valid entry surviving; (c) NDA/NIA/relation/N00 spans above 3; (d) pv instruments count == 5,455.
R2: Any NEW gaps? Judge: the artifact_fp binds {cards, market_context} but NOT prompts/llm_config directly (they enter via manifest_fp — is that chain sound given manifest immutability?); archive_complete treats bear validation_dropped entries as complete (dropped-but-nonempty bear is a quality signal, not an error) — acceptable?; the incomplete-archive recompute path overwrites raw/ audit files from the failed attempt — should failed-attempt raws be preserved (audit trail) or is overwrite acceptable for NON_EVIDENTIARY replay?
R3: Is the M-line asymmetry (bear may quote M-lines; seats may not) the right containment posture, per residual concern (iii)?
R4: Final gate — is chain_v2.2 fit to spend the 149x16 full-month replay (NON_EVIDENTIARY), with the previously-agreed deferrals (loader extension, top_inst/block-trade/holder rows, ①b readiness gate, golden set) remaining open?
OUTPUT FORMAT
- Issues ranked Blocker / Major / Minor with offending line quoted + exact replacement.
- Answer R1-R4 explicitly.
- Final line: SHIP / REVISE, plus the single most important residual risk.
```
