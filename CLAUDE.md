# Claude Operating Rules

This file is the always-on contract for Claude Code working in this repository. It mirrors `AGENTS.md` (the Codex contract) and condenses the detailed rule set under [.agents/rules/](.agents/rules/) into Claude-specific guidance.

When the two contracts disagree, this file wins for Claude sessions and `AGENTS.md` wins for Codex sessions. The rule files under `.agents/rules/` remain the authoritative human-readable reference; if you need depth on a topic, read the matching file there.

---

## 1. Mandatory Context Refresh

Before any non-trivial implementation, investigation, refactor, or data operation, read these files in order:

1. [project_state.md](project_state.md) — durable system memory; "Last Updated" tells you what changed most recently
2. [config.yaml](config.yaml) — paths, risk limits, MLflow URI, broker placeholders
3. [src/system.md](src/system.md) — top-level src architecture
4. [data/data_dictionary.md](data/data_dictionary.md) — every column in every raw dataset
5. [data/data_tracker.md](data/data_tracker.md) — coverage, sync status, PIT serving conventions

These five files are the source of truth. Do not reason about architecture, factor coverage, data shapes, or PIT semantics from memory — re-read.

For scoped work, also read the matching `AGENTS.md` if one exists in the subtree (e.g. [src/data_infra/AGENTS.md](src/data_infra/AGENTS.md), [src/alpha_research/AGENTS.md](src/alpha_research/AGENTS.md), [src/backtest_engine/AGENTS.md](src/backtest_engine/AGENTS.md), [workspace/AGENTS.md](workspace/AGENTS.md)).

While you are refreshing context, also skim this file and [AGENTS.md](AGENTS.md) against `project_state.md` for drift. If the rule files contradict or omit something `project_state.md` describes, fix the rule files before proceeding (see §11.2).

---

## 2. System Architecture You Must Keep In Mind

Six modules, each with a fixed responsibility. Do not invent alternate boundaries.

- [src/data_infra/](src/data_infra/) — Tushare ingestion, Parquet storage, normalized canonical tables, PIT ledger, Qlib backend, verification
- [src/alpha_research/](src/alpha_research/) — factor library (191 catalog + composites), factor evaluation toolkit, theme strategy framework, model zoo, MLflow tracker, factor/candidate registries
- [src/backtest_engine/](src/backtest_engine/) — `VectorizedBacktester` (Qlib wrapper, fast screening) and `EventDrivenBacktester` (realistic A-share simulator with T+1, multi-tier limits, corporate actions)
- [src/portfolio_risk/](src/portfolio_risk/) — `PortfolioOptimizer` (cvxpy), cost models, risk models
- [src/result_analysis/](src/result_analysis/) — canonical metrics, `BacktestReport`, plotters
- [src/research_orchestrator/](src/research_orchestrator/) — DAG-based workflow scheduler, the 7 built-in profiles, the 5 typed registries

When unsure where logic belongs, prefer the existing module boundary over adding new top-level scripts or ad-hoc helpers. The orchestrator's scope is "data is ready → results are published"; raw downloads, normalization, and Qlib backend builds remain in `data_infra`.

---

## 3. Hard Invariants You Must Never Break

These are silent failures that have already burned the project. Treat them as load-bearing.

- **Tushare ↔ Qlib code format**: Tushare uses `000001.SZ`; Qlib uses `000001_SZ`. Convert with `ts_code.replace('.', '_')` before any join. Wrong format silently returns 0 matches with no error.
- **Benchmark codes** also use the underscore form: `'000300_SH'`, never `'SH000300'`. Qlib's built-in `CSI300_BENCH` only works with official Qlib downloads, not this project's custom backend.
- **Trading calendar**: [data/reference/trade_cal.parquet](data/reference/trade_cal.parquet) is the single ground truth. Never assume business days equal trading days.
- **ST authoritative source**: [data/qlib_data/instruments/st_stocks.txt](data/qlib_data/instruments/st_stocks.txt) (range form, covers the 2020-01-02 gap in `stock_st_daily.parquet`). Use this for any ST detection in backtests.
- **PIT for fundamentals**: always align on `ann_date`, never `end_date`; apply `shift(1)` after `merge_asof` to prevent same-day leakage; forward-fill across calendar gaps. Provider-side `pit_*` fields (`pit_or_yoy`, `pit_netprofit_yoy`, `pit_q_op_qoq`, …) are the canonical PIT-derived growth fields.
- **PIT visibility anchor** (`strictly_next_open_trade_day` invariant): `effective_date > disclosure_date` STRICTLY. The entire PIT guarantee depends on `next_open_trade_day` / `strictly_next_open_trade_day` returning a trading day that is strictly later than the disclosure date. Do NOT change that function without updating `tests/data_infra/test_pit_backend.py` invariant tests. Ref: `src/data_infra/pit_backend.py:strictly_next_open_trade_day`.
- **`f_ann_date` coverage is dataset-specific**: the 5 statement families (`income`, `income_quarterly`, `balancesheet`, `cashflow`, `cashflow_quarterly`) anchor visibility on `max(ann_date, f_ann_date)`. The 4 event/indicator families (`indicators`, `dividends`, `forecast`, `holder_number`) anchor on `ann_date` only because the raw Tushare schemas for those endpoints do not carry an `f_ann_date` column. If a future Tushare schema change adds `f_ann_date` to any of those 4 families, set `f_ann_date_column` in the corresponding `DATASET_SPECS` entry.
- **Delist / IPO-lag contract**: delisting and the 90-day IPO lag are enforced at the **instruments sidecar layer** (`data/qlib_data/instruments/all_stocks.txt`) via `provider_metadata.build_all_stocks_universe()`. Consumers of `D.features()` inherit the guard automatically. Consumers that read `data/pit_ledger/*.parquet` directly BYPASS the guard and MUST apply their own filter using `provider_metadata.stock_basic_bounds(ts_code)`. Regression test: `tests/data_infra/test_provider_boundary.py`.
- **Cumulative→quarterly late restatement**: when a prior quarter's cumulative value is restated AFTER the current quarter has already been disclosed, `derive_single_quarter_value` retroactively changes the derived current-quarter value at the restatement's effective date. This is intentional (use best-known state) but means research code that caches quarter values must invalidate on every ledger rebuild. See the worked example in the function docstring at `src/data_infra/pit_backend.py:derive_single_quarter_value`.
- **MultiIndex order**: Qlib `D.features()` returns `MultiIndex(instrument, datetime)` — *not* `(datetime, instrument)`. The factor_eval helpers normalize either order, but raw pandas code must be explicit. Use `groupby(level=0)` for per-instrument operations only when you have not swapped levels.
- **Negation in Qlib expressions**: `-Operator(...)` does not parse. Use `0 - Operator(...)`.
- **Qlib expressions for predictive factors**: prefer `Ref(..., 1)` over same-day values when the signal is used for next-day trading. Same-day leakage is the most common bug.
- **Factor library PIT-safety (post follow-up plan #1, 2026-04-11)**: every `$field` reference inside every Layer 1 operator in [src/alpha_research/factor_library/operators.py](src/alpha_research/factor_library/operators.py) MUST be wrapped inside a `Ref(...)` frame. The correct pattern is `Mean(Ref($close, 1), 20)`, NOT `Mean($close, 20)`. Use the `ADJ_*_T1` module constants for shifted adjusted prices. `forward_return` is the ONE allowlisted exception (prediction target, not a signal). Enforcement lives in [tests/alpha_research/test_factor_library_pit_safety.py](tests/alpha_research/test_factor_library_pit_safety.py) — a parser-based stack walk that fails if any `$field` lacks a `Ref` ancestor in its parenthesis stack. Per-operator expression lock tests at [tests/alpha_research/test_operator_expressions.py](tests/alpha_research/test_operator_expressions.py) pin exact post-fix strings. A behavioral test at [tests/alpha_research/test_operator_behavioral_pit.py](tests/alpha_research/test_operator_behavioral_pit.py) confirms factor[T] does not depend on close[T] using a tiny synthetic Qlib fixture.
- **Adjusted vs raw prices**: use adjusted prices for cross-day return / momentum computations; use raw values for PIT accounting ratios. Document the choice in any new factor.
- **Exchange cost source of truth (post follow-up plan #2, 2026-04-14)**: stamp tax, commission, and transfer fee (过户费) all flow through `exchange.compute_sell_cost_breakdown()` / `compute_buy_cost_breakdown()` as the single source of truth. The engine passes `breakdown.total` to `portfolio.sell()` / `portfolio.buy()`. Do NOT duplicate rate checks or date boundaries in the engine or portfolio. The `_STAMP_TAX_CHANGE_DATE` module constant in `exchange.py` is the only place the 2023-08-28 boundary lives. Enforcement: `tests/backtest_engine/test_exchange_costs.py`.
- **CostConfig defaults are JoinQuant (changed 2026-05-22)**: `CostConfig()` returns the JoinQuant `OrderCost` equivalent — `close_tax=0.001` constant (no 2023-08-28 cut), `open/close_commission=2.5/10000`, `min_commission=5`, `transfer_fee=0`. This is the deployment-medium default because all production strategies are verified on JoinQuant. To model the ACTUAL Chinese exchange (with the 2023-08-28 stamp-tax cut from 0.1% → 0.05% AND the 0.2 bps 过户费), use `CostConfig.realistic_china()` explicitly. Both presets must round-trip through `compute_*_cost_breakdown`. Enforcement: `tests/backtest_engine/test_exchange_costs.py::CostConfigPresetTests`.
- **Exchange default slippage is JoinQuant (changed 2026-05-22)**: `Exchange()` constructor defaults to `FixedSlippage(0.0003)` = 0.0003 ¥/share (≈ 0.3 bps on a ¥10 stock) — matching JoinQuant's standard `set_slippage(FixedSlippage(3/10000))`. The previous conservative default `PctSlippage(0.001)` (10 bps) is preserved as the named constant `CONSERVATIVE_SLIPPAGE_10BPS` for research that needs it explicitly: `Exchange(slippage_model=CONSERVATIVE_SLIPPAGE_10BPS)`. **Documentation-error protection**: `PctSlippage(0.0003)` ≠ `FixedSlippage(0.0003)` — they differ by ~10× for microcap prices. Always prefer the named constants `JOINQUANT_DEFAULT_SLIPPAGE` and `CONSERVATIVE_SLIPPAGE_10BPS` over inlining literals. Enforcement: `tests/backtest_engine/test_exchange_slippage.py::ExchangeDefaultSlippageTests`.
- **Limit prices use round-half-up**: `exchange.compute_limit_prices()` uses `Decimal.quantize(ROUND_HALF_UP)`, NOT Python's default banker's rounding. Enforcement: `tests/backtest_engine/test_exchange_limits.py`.
- **Event-driven suspension wiring (2026-04-24)**: `EventDrivenBacktester` must pass `data/market/suspension/suspension_ranges.parquet` into `Exchange` when the file exists. If absent, it must log the fallback and `Exchange.is_suspended()` uses the legacy `vol == 0` proxy. Enforcement: `tests/backtest_engine/test_event_driven_backtester_wiring.py`.
- **Event-like daily endpoint namespacing (2026-04-20)**: `_materialize_daily_dataset` in [src/data_infra/pit_backend.py](src/data_infra/pit_backend.py) writes one `.day.bin` per numeric column using the column name verbatim, AFTER `_run_dump_bin` has already written the canonical `$open/$high/$low/$close/$vol/$amount` bins from kline data. Several event-like daily endpoints ship numeric columns that collide with those canonical names — e.g., `top_list` has `close` and `amount`, `block_trade` has `vol` and `amount`. Without a namespace prefix, any event-day row would silently overwrite the canonical kline bin for that stock/date. Every dataset in `EVENT_LIKE_DAILY_DATASETS` (`top_list`, `top_inst`, `block_trade`, `cyq_perf`) MUST also appear in `EVENT_LIKE_DAILY_FIELD_PREFIX`, and its payload columns are written under `{dataset}__{column}.day.bin` (e.g., `top_list__close.day.bin`). Consumers access these via Qlib expressions like `$top_list__close`, `$block_trade__amount`, `$cyq_perf__winner_rate`. Enforcement: [tests/data_infra/test_event_like_daily_namespace.py](tests/data_infra/test_event_like_daily_namespace.py). When adding a new event-like daily endpoint: add it to `EVENT_LIKE_DAILY_DATASETS`, add a matching `EVENT_LIKE_DAILY_FIELD_PREFIX` entry, and extend `_SYNTHETIC_PAYLOADS` in the test so the rename-fires assertion exercises it.
- **Provider self-attestation (PR 1 of 2026-05-26 freeze plan)**: every host that publishes a Qlib provider MUST also emit `data/qlib_data/metadata/provider_build.json` — this file is the single source of truth for `provider_build_id`, `calendar_policy_id`, `event_endpoint_namespacing.status`, and calendar bounds. The Qlib provider tree itself is gitignored, so this manifest is what lets formal artifacts prove which provider produced them. Schema lives at [schemas/provider_build.schema.json](schemas/provider_build.schema.json); loader at [src/data_infra/provider_manifest.py](src/data_infra/provider_manifest.py); builder emission is wired into `StagedQlibBackendBuilder.publish()` in [src/data_infra/pit_backend.py](src/data_infra/pit_backend.py). The current 2026-04-21 build has a `retroactive_manifest=true` manifest with an evidence array; future builds emit non-retroactive manifests automatically. **Hard rule for formal runs**: missing manifest → fail; namespacing != enforced → fail; calendar mismatch → fail unless the calendar policy explicitly allows it. Enforcement: [tests/data_infra/test_provider_manifest.py](tests/data_infra/test_provider_manifest.py).
- **Artifact provenance contract (PR 1)**: every formal `BacktestResult.config`, validation-step output, and registry artifact MUST carry an `artifact_provenance` block (PR 1 = `provider_build_id` + `calendar_policy_id`; PR 3 will extend with `execution_profile_*`). Older artifacts missing this block are read back as `legacy_artifact=true` — they remain viewable for historical comparison but cannot pass the formal release gate. Schema: [schemas/artifact_provenance.schema.json](schemas/artifact_provenance.schema.json). Reader: [src/research_orchestrator/artifact_provenance.py](src/research_orchestrator/artifact_provenance.py). Gate: `evaluate_artifact_provenance` / `assert_formal_artifact_eligible` in [src/research_orchestrator/release_gate.py](src/research_orchestrator/release_gate.py). Enforcement: [tests/research_orchestrator/test_artifact_provenance.py](tests/research_orchestrator/test_artifact_provenance.py).
- **Calendar policy contract (PR 1)**: the 2026-02-27 trading-calendar freeze is recorded as an explicit policy YAML at [config/calendar_policies/frozen_20260227_system_build.yaml](config/calendar_policies/frozen_20260227_system_build.yaml). Formal runs that operate against a frozen calendar must pass `calendar_policy_id` explicitly (default sandbox behavior is `legacy_artifact=true`). When the freeze ends, flipping `frozen: false` and adding `max_calendar_lag_days` in the same YAML is the only change required — the loader at [src/research_orchestrator/calendar_policy.py](src/research_orchestrator/calendar_policy.py) already branches on `frozen`. Enforcement: [tests/research_orchestrator/test_calendar_policy.py](tests/research_orchestrator/test_calendar_policy.py).
- **Governance file home rule (PR 1)**: governance registries (calendar policies, field-status registry, execution profiles) live under `config/` and committed schemas under `schemas/`. Do NOT place governance files under `data/` — that directory is gitignored, so a registry placed there would silently become un-versioned. Runtime artifacts (the actual `provider_build.json`, registry status caches) DO live under `data/` because they are local per-host state, not shared governance.
- **ENGINE_REQUIRED_FIELDS contract (PR 2 of 2026-05-26 freeze plan)**: the canonical 8-field tuple (`$open`, `$close`, `$high`, `$low`, `$vol`, `$amount`, `$pre_close`, `$adj_factor`) lives at [src/backtest_engine/event_driven/constants.py](src/backtest_engine/event_driven/constants.py). `EventDrivenBacktester.run()` ALWAYS unions caller-supplied `preload_fields` with this tuple when `should_preload` is True, so the engine path never falls back to per-day `D.features` for OHLCV. Adding a new engine-required field means adding it here, not in `engine._fetch_day_data` alone.
- **`should_preload` condition (PR 2)**: `EventDrivenBacktester.run()` preloads when ANY of `preload_required=True`, `preload_fields is not None`, or `run_mode in FORMAL_RUN_MODES` ({`formal`, `oos_test`, `joinquant_replication`}). Passing `run_mode='formal'` auto-enables both `strict=True` and `require_preloaded=True`. The old `if preload_fields:` condition silently skipped preload when a formal run had no strategy-specific factor fields — that bug is fixed. Reference: the PR 2 negative-test suite at [tests/backtest_engine/test_preload_hardening.py](tests/backtest_engine/test_preload_hardening.py).
- **No-op preload removed (PR 2)**: `QlibDataFeeder.preload(start, end)` was a silent no-op that left `_cache_df=None` and forced the engine into a per-day fallback (the ~100x slowdown discovered in plan `snappy-buzzing-meerkat` v5). The method now raises `NotImplementedError`; the previous `BacktestEngine.run()` call site has been removed. Use `preload_features(index_name, fields, start_time, end_time, strict=True)` exclusively.
- **`require_preloaded` engine contract (PR 2)**: `BacktestEngine(require_preloaded=True)` makes the engine call `feeder.assert_preloaded(required_fields=ENGINE_REQUIRED_FIELDS, start=prev_trading_day(backtest_start), end=backtest_end, require_zero_fallback=True)` before the day loop starts. Auto-set to True when `EventDrivenBacktester.run(run_mode='formal'|'oos_test'|'joinquant_replication')`. Formal runs cannot silently degrade into per-day fallback.
- **`ExecutionProfile` is the formal contract (PR 3 of 2026-05-26 freeze plan)**: every formal backtest passes `execution_profile=<profile_id>` to `EventDrivenBacktester.run()` or `VectorizedBacktester.run()`. The profile lives in [src/backtest_engine/execution_profiles.py](src/backtest_engine/execution_profiles.py) as a frozen dataclass aggregating `fill_mode`, `cost_config_factory`, `slippage_preset`, `volume_limit`, `allowed_for_formal`, `deployment_target`, `backend`, plus a date-stamped `profile_version`. Built-in profiles: `joinquant_daily_sim` (formal, jq_daily_avg fill), `joinquant_open_close_replica` (formal, open_close fill), `realistic_china_stress` (event-driven but `allowed_for_formal=False`), `vectorized_screening_close` (vectorized, screening only). Adding a new profile means a new entry in `_BUILTIN_PROFILES` + a bumped `profile_version`. Enforcement: [tests/backtest_engine/test_execution_profiles.py](tests/backtest_engine/test_execution_profiles.py).
- **`profile_hash` is computed and self-excluding (PR 3)**: `ExecutionProfile.profile_hash` is a `@property` (NOT a dataclass field) that computes `sha256(json.dumps(execution-relevant-fields, sort_keys=True))`. The hash excludes itself — there is no recursion. Two ExecutionProfile instances with identical hashed fields produce identical hashes; changing `fill_mode`, `slippage_preset`, `cost_config_factory`, `volume_limit`, `notes`, or `profile_version` changes the hash. Every formal artifact records `execution_profile_hash` alongside `execution_profile_id` so reviewers can diff by hash without re-reading the profile registry.
- **Formal-no-override default (PR 3)**: when a caller passes `execution_profile=<formal_id>` AND also passes explicit `fill_mode=...` / `slippage=...` / `exchange_config=...` / `volume_limit=...`, the wrapper computes an `override_diff` and rejects the run with `OverrideRequiresReasonError` unless `override_reason='<text>'` is also supplied. Formal artifacts that DO carry overrides record `manual_override=true` + `override_reason` + `override_diff` in `artifact_provenance`; the release gate then surfaces those fields so reviewers can decide whether the override was justified.
- **`ArtifactProvenance` schema v2 (PR 3)**: bumped from PROVENANCE_SCHEMA_VERSION=1 to =2. v2 formal-eligible artifacts MUST carry `provider_build_id`, `calendar_policy_id`, `execution_profile_id`, AND `execution_profile_hash`; when `manual_override=True`, also `override_reason` and `override_diff`. v1 artifacts (PR 1 era, no execution_profile fields) remain readable but `is_formal_eligible()` returns False with reason `missing_execution_profile_id`. The release gate at [src/research_orchestrator/release_gate.py](src/research_orchestrator/release_gate.py) cross-checks `execution_profile_id` against the registry — unknown profile → fail; profile with `allowed_for_formal=False` → fail.
- **Cache + seal critical sections are file-locked (PR 4 of 2026-05-26 freeze plan)**: `HoldoutSealStore.claim_holdout_access` and `CacheManifestStore.record_cache_write` both wrap their **entire read-check-write sequence** in [file_lock](src/research_orchestrator/file_lock.py), not just the final `_atomic_write_dataframe`. Without lock coverage of the read-check window, two concurrent processes could each pass the `frame.empty` check (seal) or each `_load` a baseline frame (cache) and silently overwrite each other. Lock files: `<seal_root>/holdout_events.lock` and `<cache_root>/cache_events.lock`. Enforcement: [tests/research_orchestrator/test_lock_concurrency.py](tests/research_orchestrator/test_lock_concurrency.py) — 8 concurrent processes claiming the same `design_hash` produce exactly 1 success + 7 raised; 8 processes writing distinct cache rows produce 8 persisted rows. Touch either function without preserving the `with file_lock(...):` scope and the concurrency tests fail loudly.
- **Field-status registry is the formal data gate (PR 5 of 2026-05-26 freeze plan)**: every Qlib `$field` referenced in a factor expression is resolved through [config/field_registry/field_status.yaml](config/field_registry/field_status.yaml) (committed governance, NEVER under `data/`). Each dataset declares one of 4 statuses (`approved` / `pending_review` / `quarantine` / `deprecated`); each status declares per-stage allowed flags (`sandbox_screening`, `vectorized_screening`, `formal_validation`, `oos_test`, `registry_publish`). The runtime API lives at [src/data_infra/field_registry.py](src/data_infra/field_registry.py) — `FieldStatusRegistry.resolve_field(token, stage)` and `validate_expression(expr, stage)`; `extract_qlib_fields(expr)` parses tokens out of nested operators (`Mean(Ref($close, 1), 20)`). Schema lives at [schemas/field_status.schema.json](schemas/field_status.schema.json). Enforcement: [tests/data_infra/test_field_registry.py](tests/data_infra/test_field_registry.py) and [tests/research_orchestrator/test_field_dependency_gate.py](tests/research_orchestrator/test_field_dependency_gate.py).
- **Unknown-field conservative-fail policy (PR 5)**: when a referenced field matches NO registered dataset, the registry's `unknown_field_policy[<stage>]` decides. Default policy: `warn` in `sandbox_screening` / `vectorized_screening` (exploration not blocked); `fail` in `formal_validation` / `oos_test` / `registry_publish` (new untracked fields cannot quietly enter formal artifacts). Bumping a new provider field into formal use requires either listing it explicitly in `field_status.yaml` or letting it ride a registered `field_prefixes` entry (e.g. `$pit_*` is approved via the `pit_fundamentals` prefix).
- **Seed status assignments (PR 5)**: `approved` = `market_daily` + `daily_basic` + `pit_fundamentals` + `reference`; `quarantine` = `moneyflow` + `hk_hold` (northbound) + `margin_detail` + `stk_limit` (downloaded but pending anomaly review per project_state 2026-05-26); `pending_review` = `top_list` + `top_inst` + `block_trade` + `cyq_perf` (post-2026-04-20 namespacing fix; anomaly review WIP). Status transitions require an append-only entry in [config/field_registry/field_approval_log.jsonl](config/field_registry/field_approval_log.jsonl) AND a per-promotion YAML under [config/field_registry/approvals/](config/field_registry/approvals/) — diffable in Git, never binary.
- **Release-gate field-dependency check (PR 5)**: `evaluate_field_dependencies(fields=..., expressions=..., stage=...)` and the strict `assert_field_dependencies_eligible(...)` in [src/research_orchestrator/release_gate.py](src/research_orchestrator/release_gate.py) are the central choke point for refusing formal artifacts whose factor expressions touch any disallowed field. Returns a `FieldDependencyGateResult` with `disallowed_fields`, `unknown_fields`, and per-field reason strings.
- **`ResearchAccessContext` is the data-layer OOS enforcement primitive (PR 6 of 2026-05-26 freeze plan)**: a `contextvars.ContextVar` carrying `(run_id, step_id, stage, design_hash, allowed_start, allowed_end, provider_build_id, calendar_policy_id, holdout_context_id, holdout_seal_claimed, allowed_fields)`. Set via the `research_access_context(...)` context manager or via `SealedBacktestRunner.run_workspace_pipeline()` which builds the context from `time_split + holdout_context`. Lives at [src/research_orchestrator/research_access_context.py](src/research_orchestrator/research_access_context.py). The wrapper-level OOS backstop (`EventDrivenBacktester.run`) is preserved; this context closes the bypass path where a pipeline could read data via `qlib_windowed_features` directly.
- **`qlib_windowed_features` is the mandatory data-access chokepoint (PR 6)**: every Qlib `D.features` call in formal research must route through [src/research_orchestrator/qlib_windowed_features.py](src/research_orchestrator/qlib_windowed_features.py). Before the `D.features` invocation, the wrapper calls `get_research_access_context()` and invokes `ResearchAccessContext.validate_read(start_time, end_time, fields=...)`. Violations raise `HoldoutWindowViolation` (read outside window), `HoldoutSealViolation` (OOS without seal), or `FieldAccessViolation` (field outside `allowed_fields`). Sandbox / no-context calls are unchanged.
- **`SealedBacktestRunner.run_workspace_pipeline` signature is tight (PR 6)**: `time_split` and `pipeline_args` are required keyword args; the runner constructs a `ResearchAccessContext` from them and the holdout context, then wraps `pipeline_fn(...)` in `research_access_context(...)`. Pipelines receive `time_split` and `holdout_context` explicitly. The pre-PR-6 hole (pipeline could claim a seal and then request a broader window internally) is closed.
- **AST lint bans bare `D.features` outside the canonical wrapper (PR 6)**: [scripts/lint_no_bare_qlib_features.py](scripts/lint_no_bare_qlib_features.py) detects `D.features`, aliased `from qlib.data import D as X; X.features`, `qlib.data.D.features`, and the `getattr(D, "features")` trick. Allowed: [src/research_orchestrator/qlib_windowed_features.py](src/research_orchestrator/qlib_windowed_features.py) (the wrapper itself) and any line with the `# noqa: bare-qlib-features` marker (privileged admin calls like `provider_manifest.compute_canonical_kline_hash`). The lint runs inside `scripts/run_daily_qa.py` as the `no_bare_qlib_features_lint` audit block. Enforcement: [tests/research_orchestrator/test_lint_no_bare_qlib_features.py](tests/research_orchestrator/test_lint_no_bare_qlib_features.py).
- **Workspace `SCRIPT_STATUS` header contract (PR 7 of 2026-05-26 freeze plan)**: every Python script under [workspace/scripts/](workspace/scripts/) that the PR 2 audit classified carries a machine-readable header block. Keys: `script_status` (`formal_candidate` / `historical_investigation`), `formal_research_allowed`, `deployment_target`, `execution_profile` (when formal), `requires_provider_manifest`, `requires_preload_strict`, `pr2_audit_class` (A/B/C/D). Greppable via `rg "# script_status:"`. Enforcement: [tests/architecture/test_dormant_module_boundaries.py](tests/architecture/test_dormant_module_boundaries.py) `test_workspace_scripts_outside_archive_carry_script_status_header`.
- **Class-D scripts live in `workspace/scripts/archive/` (PR 7)**: 14 superseded JoinQuant mimic ladder scripts (v2-v15) were archived to [workspace/scripts/archive/p1_jq_g5a2_investigation_2026_05/](workspace/scripts/archive/p1_jq_g5a2_investigation_2026_05/). `src/` must NOT import or path-reference them — the archived scripts are historical investigation only. Enforcement: [tests/architecture/test_dormant_module_boundaries.py](tests/architecture/test_dormant_module_boundaries.py) `test_archived_workspace_scripts_not_referenced_from_src`.
- **Dormant `portfolio_risk` import boundary (PR 7)**: [src/portfolio_risk/](src/portfolio_risk/) is dormant per the 2026-05-26 audit — its `predict_portfolio_risk()` returns a hardcoded `0.05` and `MultiFactorRiskModel.fit()` is a no-op. No formal-path module (`release_gate`, `validation_steps`, `event_driven/`, `factor_library/`, `result_analysis/`) may import the dormant symbols `MultiFactorRiskModel`, `predict_portfolio_risk`, or `MarketImpactModel` until portfolio_risk is implemented for real (P2 → P0 promotion). Enforcement: [tests/architecture/test_dormant_module_boundaries.py](tests/architecture/test_dormant_module_boundaries.py) `test_formal_path_does_not_import_dormant_portfolio_risk` (parametrized across all formal paths).
- **`is_formal` considers BOTH run_mode AND profile.allowed_for_formal (PR 8 of 2026-05-26 freeze plan)**: `EventDrivenBacktester.run()` computes `is_formal = mode_is_formal or profile_is_formal` exactly once, before any preload decision. A caller passing `execution_profile='joinquant_daily_sim'` with `run_mode=None` is treated as formal — strict preload, `require_preloaded=True`, and `require_provider_manifest=True` all auto-enable. The pre-PR-8 bug computed `is_formal` twice and the second computation overwrote the profile-aware version with run_mode-only.
- **`strict_cache_only` mode fails on first mid-loop cache miss (PR 8)**: `QlibDataFeeder.set_strict_cache_only(True)` makes `get_features()` raise `PreloadCoverageError` on the first cache miss instead of silently falling back to per-day `D.features`. `BacktestEngine` flips this on automatically when `require_preloaded=True` so a formal run that passes the pre-loop `assert_preloaded` cannot then silently degrade mid-loop on a non-preloaded field. Enforcement: [tests/backtest_engine/test_pr8_runtime_enforcement.py](tests/backtest_engine/test_pr8_runtime_enforcement.py) `TestFix2StrictCacheOnly`.
- **Formal-runtime provider/calendar validation (PR 8)**: `EventDrivenBacktester.run()` calls `_validate_provider_at_runtime(manifest, calendar_policy_id, run_mode)` whenever formal mode is active. For a `frozen: true` policy, the observed Qlib calendar end-date MUST match BOTH `policy.calendar_end_date` AND `manifest.provider.calendar_end_date`; the pre-PR-8 `allow_mismatch = policy.frozen` blanket was too loose. Disallowed run_modes (mode not in `policy.allowed_modes`) also raise. Enforcement: [tests/backtest_engine/test_pr8_runtime_enforcement.py](tests/backtest_engine/test_pr8_runtime_enforcement.py) `TestFix3ProviderRuntimeValidation`.
- **Release gate flags `execution_profile_hash` mismatch (PR 8)**: `evaluate_artifact_provenance` now compares the artifact's stored `execution_profile_hash` against the current canonical `get_profile(id).profile_hash`. Mismatch → `execution_profile_hash_mismatch` added to reasons, `ArtifactGateResult.profile_hash_matches_current=False`. This catches the case where a profile was bumped (version+hash) after the artifact was produced; the artifact is no longer bit-for-bit reproducible against the current registry. Enforcement: [tests/backtest_engine/test_pr8_runtime_enforcement.py](tests/backtest_engine/test_pr8_runtime_enforcement.py) `TestFix5ProfileHashMismatch`.
- **Replayable override_diff for object overrides (PR 8)**: when a caller passes `exchange_config=CostConfig(...)` or `slippage=FixedSlippage(...)` on top of an execution profile, the provenance override_diff now records `{"from": {"factory": "joinquant_default"}, "to": {"class": "CostConfig", "params": {...}}}` so the override is reconstructible from the artifact alone. Pre-PR-8 we stored opaque `"<caller-supplied CostConfig instance>"` strings. Helpers: `_serialize_cost_config(cfg)` + `_serialize_slippage(slip)` in [src/backtest_engine/event_driven/__init__.py](src/backtest_engine/event_driven/__init__.py).
- **`require_research_access_context(stage)` for formal stages (PR 8)**: formal validation handlers should call this BEFORE touching feature data. Raises `MissingResearchAccessContextError` if `stage in FORMAL_STAGES = {formal_validation, oos_test, registry_publish}` and `get_research_access_context() is None`. Sandbox stages tolerantly return None. Lives at [src/research_orchestrator/research_access_context.py](src/research_orchestrator/research_access_context.py). Per-handler installs in `validation_steps.py` are tracked as the remaining follow-up after PR 8.
- **Formal runs MUST supply `calendar_policy_id` (PR 8a of 2026-05-26 freeze plan)**: `EventDrivenBacktester.run()` raises `RuntimeError` BEFORE doing any backtest work if `effective_require_provider_manifest=True` AND `calendar_policy_id is None`. The pre-PR-8a behavior silently skipped `_validate_provider_at_runtime` and stamped legacy provenance — formal-mode in name only. The policy is what authorizes a formal run to operate against the current calendar window; without it the manifest validator cannot run and the artifact cannot be formally attested.
- **Frozen policy allowed_modes include profile deployment targets (PR 8a)**: [config/calendar_policies/frozen_20260227_system_build.yaml](config/calendar_policies/frozen_20260227_system_build.yaml) lists `joinquant_daily` and `joinquant_open_close_replica` in `allowed_modes` so a profile-driven formal run with `run_mode=None` passes `policy.assert_run_mode_allowed(profile.deployment_target)`. Pre-PR-8a a `joinquant_daily_sim` profile + frozen policy + `run_mode=None` was rejected even though the profile is faithful to JoinQuant.
- **Daily QA enforces frozen-policy calendar equality (PR 8a)**: [scripts/run_daily_qa.py](scripts/run_daily_qa.py) no longer uses `allow_mismatch = policy.frozen`. Frozen policies require strict equality between `manifest.calendar_end_date`, `policy.calendar_end_date`, and the live `calendars/day.txt[-1]`. Mismatch raises with a precise diagnostic. Daily QA semantics now match the formal-runtime validator.
- **Formal runtime validator raises on Qlib read failure (PR 8a)**: `_validate_provider_at_runtime` in [src/backtest_engine/event_driven/__init__.py](src/backtest_engine/event_driven/__init__.py) now raises `RuntimeError` if the Qlib `D.calendar()` read fails. The pre-PR-8a code logged a warning and returned silently — too permissive for a formal validator.
- **Slippage serialization captures all `__dict__` attributes (PR 8a)**: `_serialize_slippage(slip)` uses `vars(slip)` filtered to JSON-safe types instead of a hardcoded attribute allowlist. Previously `FixedSlippage.spread` (the only field that class stores) was missed by the `(rate, value, bps, fixed_amount)` allowlist, producing an unreplayable `{"class": "FixedSlippage", "params": {}}`. Now `FixedSlippage(0.0005)` serializes as `{"class": "FixedSlippage", "params": {"spread": 0.0005}}`.
- **Strict cache-only raises on all-missing instruments (PR 8a)**: in `QlibDataFeeder.get_features`, when `_strict_cache_only=True` and the requested instruments have empty intersection with the cache's instrument index, the feeder raises `PreloadCoverageError` instead of returning an empty DataFrame. Closes a coverage-gap blind spot that the pre-PR-8a cache-hit short-circuit silently hid.
- **BacktestEngine restores strict_cache_only after run (PR 8a)**: `BacktestEngine.run()` snapshots the feeder's `_strict_cache_only` value before the day loop and restores it on the success path so test harnesses and direct-engine reuse don't see leaked formal-mode state.
- **Calendar policy + manifest validation fires BEFORE feeder/preload (PR 8b)**: `EventDrivenBacktester.run()` performs the `calendar_policy_id` presence check AND the full `_validate_provider_at_runtime` call BEFORE creating `QlibDataFeeder` or invoking `preload_features`. Pre-PR-8b the check fired after preload, so a formal run without a policy still touched Qlib and burned cache work before failing. Enforcement: [tests/backtest_engine/test_pr8b_ordering_modes.py::TestBlocker1OrderingBeforePreload](tests/backtest_engine/test_pr8b_ordering_modes.py).
- **Frozen policy `allowed_modes` covers canonical FORMAL_RUN_MODES (PR 8b)**: [config/calendar_policies/frozen_20260227_system_build.yaml](config/calendar_policies/frozen_20260227_system_build.yaml) `allowed_modes` includes `formal` and `oos_test` (the canonical strings from [src/backtest_engine/event_driven/constants.py::FORMAL_RUN_MODES](src/backtest_engine/event_driven/constants.py)) alongside the profile deployment targets. A run with `run_mode='formal'` and the frozen policy now passes `policy.assert_run_mode_allowed` literally — no string normalization shim required.
- **Provider manifest's own `calendar_policy_id` must match the run's policy (PR 8b)**: `_validate_provider_at_runtime` enforces `manifest.calendar_policy_id == calendar_policy_id`. Without this check, a caller could pass a different policy id with matching dates and silently stamp a mismatched policy into the artifact. Enforcement: [tests/backtest_engine/test_pr8b_ordering_modes.py::TestBlocker3ManifestPolicyMismatch](tests/backtest_engine/test_pr8b_ordering_modes.py).
- **Strict cache-only catches PARTIAL missing instruments (PR 8b)**: in `QlibDataFeeder.get_features`, when `_strict_cache_only=True` and ANY requested instrument is absent from the cache (not only the all-missing case), the feeder raises `PreloadCoverageError`. Pre-PR-8b the partial-missing path silently sliced and returned the subset present in cache, hiding a real coverage gap.
- **strict_cache_only restored on exception path (PR 8b)**: `BacktestEngine.run()` wraps the day loop in `try: ... finally: feeder.set_strict_cache_only(_prev_strict_cache_only)`. A module-level `strict_cache_mode(feeder, enabled)` context manager is also exported from [data_feeder.py](src/backtest_engine/event_driven/data_feeder.py) for callers that want the same guarantee in their own code. Pre-PR-8b restored only on the success path.
- **Runtime validator reads `calendars/day.txt` directly (PR 8c Blocker 1)**: `_validate_provider_at_runtime()` no longer depends on `qlib.init(...)` having run. PR 8b moved the validator BEFORE feeder construction (which is what calls `qlib.init`), so on a fresh process the prior `D.calendar(...)` call could fail with "Qlib not initialized" even when the provider on disk was fine. The new helper `_read_provider_calendar_end(qlib_dir)` reads `day.txt` directly, mirroring `scripts/run_daily_qa.py`. Enforcement: [tests/backtest_engine/test_pr8c_validation_wiring.py::TestBlocker1PreInitValidator::test_validator_runs_without_qlib_init](tests/backtest_engine/test_pr8c_validation_wiring.py) explicitly does NOT patch `qlib.data.D` and the validator must still succeed.
- **Validation handlers drive EventDrivenBacktester through the formal contract (PR 8c Blocker 2)**: `handle_validation_event_backtest_is` and `handle_validation_event_backtest_oos` in [src/research_orchestrator/validation_steps.py](src/research_orchestrator/validation_steps.py) now pass `execution_profile='joinquant_daily_sim'`, `calendar_policy_id='frozen_20260227_system_build'`, `run_mode='formal'`/`'oos_test'`, `preload_required=True`, and `require_provider_manifest=True`. `run_event_driven_window` in [workspace/research/alpha_mining/event_driven_strategy_research.py](workspace/research/alpha_mining/event_driven_strategy_research.py) gains those kwargs and forwards them. Pre-PR-8c the formal-runtime guardrails added in PR 8 / PR 8a / PR 8b worked but the validation path never invoked them; the release gate could reject a published artifact later, but the actual runtime had already executed without formal enforcement. Override reason is supplied verbatim because the prescription always provides an explicit `exchange_config` + `slippage_rate` that overrides profile defaults.
- **strict_cache_only enabled BEFORE warmup + strategy.initialize (PR 8c Blocker 3)**: `BacktestEngine.run()` flips `self.feeder.set_strict_cache_only(True)` immediately after `assert_preloaded(...)`, BEFORE `prev_day_data = self._fetch_day_data(prev_date)` and BEFORE `self.strategy.initialize(init_context)`. Pre-PR-8c the strict-mode enable happened right before the day loop, so warmup + initialize could still silently fall back on a non-preloaded field. Once `require_preloaded=True`, NO feeder access after the assertion may bypass the cache.
- **OOS validation handler claims the holdout seal explicitly (PR 8d Blocker 1)**: [src/research_orchestrator/validation_steps.py](src/research_orchestrator/validation_steps.py) `handle_validation_event_backtest_oos` now calls `_claim_holdout_access_if_needed(context)` from [src/research_orchestrator/steps.py](src/research_orchestrator/steps.py) BEFORE invoking `run_event_driven_window`. Pre-PR-8d the docstring claimed the seal claim happened "inside SealedBacktestRunner._claim_if_oos" but the handler called `_run_with_cache_context(... run_event_driven_window ...)` directly without going through `SealedBacktestRunner`. The `EventDrivenBacktester` OOS backstop would correctly refuse the run with "no seal claim found" — failing safely, but leaving the formal OOS validation path broken in production. The helper uses `design_hash=hypothesis.design_hash()`, `run_dir=str(context.run_dir)`, `step_id=context.step.step_id` — the exact identifiers the engine backstop later cross-checks.
- **Strict-cache try/finally covers benchmark + warmup + initialize (PR 8d Blocker 2)**: `BacktestEngine.run()` `try:` starts immediately after `set_strict_cache_only(True)`, so the `finally:` restoration fires even if benchmark load, warmup `_fetch_day_data`, or `strategy.initialize` raises. Pre-PR-8d the `try:` started at the day loop, so an init-time `PreloadCoverageError` (a strategy reading a non-preloaded field during initialize) leaked strict-mode state on the feeder. Combined with PR 8c Blocker 3, the engine now guarantees: (a) strict mode is on for every feeder access after the assertion, AND (b) restoration is guaranteed on every exit path.
- **Field-registry resolver enforcement (PR 9 of 2026-05-26 freeze plan)**: [src/research_orchestrator/validation_steps.py](src/research_orchestrator/validation_steps.py) `handle_validation_object_resolver` now calls `_validate_factor_field_dependencies(factor_names, stage='formal_validation', artifact_label=hypothesis_id)` AFTER the resolver succeeds. The helper looks up each prescribed factor's Qlib expression in `get_factor_catalog()` + `get_industry_relative_defs()`, then calls `assert_field_dependencies_eligible(expressions=..., stage='formal_validation')` from [src/research_orchestrator/release_gate.py](src/research_orchestrator/release_gate.py). Any factor whose expression touches a `quarantine` / `pending_review` / unknown `$field` raises `FieldApprovalError` BEFORE the IS leg starts — pre-PR-9 such candidates would only be caught at release-gate time after spending the full IS leg's compute budget. The `field_dependency_report` is also persisted into the resolver step's outputs for audit. Enforcement: [tests/research_orchestrator/test_pr9_validation_field_gate.py](tests/research_orchestrator/test_pr9_validation_field_gate.py).
- **Resolver helper fails closed on lookup gaps (PR 9a of 2026-05-27, GPT 5.5 Pro round-2 review)**: pre-PR-9a `_validate_factor_field_dependencies` recorded a `no_expression_found` source-tag and continued when a resolved factor lacked any factor-library expression, then submitted a possibly-empty expression list to `assert_field_dependencies_eligible` — the strict assert would return eligible because no `$field` tokens were extracted, silently defeating the gate. PR 9a raises `FieldApprovalError` at any formal stage (`formal_validation` / `oos_test` / `registry_publish`) in three previously-fail-open cases: (a) factor name in NEITHER `get_factor_catalog()` NOR `get_industry_relative_defs()`, (b) industry-relative composite whose base is missing from the catalog, (c) empty `factor_names` input. Sandbox / vectorized screening stages preserve the lenient note-and-continue behavior. PR 9a also reverses the lookup order: industry-relative composites are checked FIRST so a same-named stray catalog entry cannot bypass the base-inheritance PIT-safety contract. Enforcement: [tests/research_orchestrator/test_pr9_validation_field_gate.py::TestPR9aFailClosed](tests/research_orchestrator/test_pr9_validation_field_gate.py).
- **Behavioral resolver-handler test (PR 9a)**: `handle_validation_object_resolver` is now driven end-to-end with a mocked `ResolverHub.resolve_assets` to prove (a) `_validate_factor_field_dependencies` IS invoked after a successful resolver pass, (b) `field_dependency_report` is persisted into both step outputs and `registry_resolution.json`, and (c) a `FieldApprovalError` raised by the helper propagates out of the handler BEFORE any dataset/IS work begins. Enforcement: [tests/research_orchestrator/test_pr9_validation_field_gate.py::TestResolverHandlerBehavior](tests/research_orchestrator/test_pr9_validation_field_gate.py).
- **Formal-factor / field-registry compatibility test (PR 9a)**: a parametrized test iterates `get_factor_catalog(include_new_data=True)` (147 base factors; the 4 industry-relative composites + 20 Layer-2 composites inherit their field dependencies from their base factors and are covered transitively) and asserts every factor either passes `evaluate_field_dependencies(stage='formal_validation')` OR appears in `KNOWN_NON_FORMAL_FACTORS` (the explicit list of alpha-endpoint / quarantine-dependent factors expected to remain blocked). A new unexpected failure means a registry/catalog drift; a new unexpected pass means a dataset was promoted and the factor should leave the known-failing set. A representative basket (`val_bp`, `val_ep_ttm`, `qual_roe`, `qual_accruals`, `grow_netprofit_yoy`, `lev_debt_to_assets`, `liq_turnover_20d`, `mom_return_20d`, `size_ln_mcap`, `risk_vol_20d`) is also spot-checked separately. Pre-PR-9a a registry/catalog gap could silently block ~30 historically-formal factors without anyone noticing until production. Enforcement: [tests/research_orchestrator/test_pr9_validation_field_gate.py::TestFormalFactorCompatibility](tests/research_orchestrator/test_pr9_validation_field_gate.py).
- **Auditable KNOWN_NON_FORMAL_FACTORS contract (PR 9a round-3)**: the compatibility-test skip list is a `dict[str, dict]` mapping each non-formal factor name to `{reason, expected_status, expected_dataset, expected_fields}`, not a bare set. The companion test `test_known_non_formal_factors_still_block` iterates the mapping and verifies each entry: (a) the helper still raises `FieldApprovalError`, (b) actual `disallowed_fields` exactly match `expected_fields`, (c) the resolving dataset matches `expected_dataset`, (d) status matches `expected_status`. Catches three drift modes: dataset promoted → factor passes (`unexpected_passes`), factor expression edited → `wrong_fields`, registry coverage shifted → `wrong_dataset` / `wrong_status`. The skip list cannot silently become an escape hatch. Enforcement: [tests/research_orchestrator/test_pr9_validation_field_gate.py::TestFormalFactorCompatibility::test_known_non_formal_factors_still_block](tests/research_orchestrator/test_pr9_validation_field_gate.py).
- **Indicators-dataset PIT-lag contract (PR 9a round-3)**: the indicators approval YAML records `approved_usage_pattern: "Ref($field, 1) or stricter"` and `same_day_raw_usage_allowed: false`. `TestFormalIndicatorPITLagContract` ([tests/research_orchestrator/test_pr9_validation_field_gate.py](tests/research_orchestrator/test_pr9_validation_field_gate.py)) locks two invariants: (a) every YAML-listed indicator field resolves to `dataset_id=indicators` + `status=approved` in the live registry, and (b) every formal factor whose expression touches an indicator field wraps that field in a `Ref(...)` ancestor — reuses the parser-based `find_unwrapped_field_references` stack walk from [tests/alpha_research/test_factor_library_pit_safety.py](tests/alpha_research/test_factor_library_pit_safety.py). The approval YAML at [config/field_registry/approvals/2026-05-27_indicators_unlisted_to_approved.yaml](config/field_registry/approvals/2026-05-27_indicators_unlisted_to_approved.yaml) names these test IDs in its `evidence.tests` array so a reviewer can audit the live coverage from the YAML alone.
- **Per-family bare-name registry guardrails (PR 9a round-3)**: [tests/data_infra/test_field_registry.py::TestLiveRegistry](tests/data_infra/test_field_registry.py) carries one explicit test per quarantine / pending-review family that uses bare-named columns: `test_moneyflow_is_quarantined_for_formal` (`$net_mf_amount`, `$buy_sm_vol`), `test_hk_hold_bare_field_quarantined_for_formal` (`$ratio`), `test_margin_detail_bare_fields_quarantined_for_formal` (`$rzye`, `$rqye`, `$rzmre`, `$rzche`), `test_stk_limit_bare_fields_quarantined_for_formal` (`$up_limit`, `$down_limit`), `test_stk_holdertrade_bare_fields_pending_review_for_formal` (`$holdertrade_*`), `test_indicators_bare_fields_approved_for_formal` (`$roe`, `$or_yoy`, …). Each asserts dataset_id + status_id explicitly. Locks PR 9a's prefix→bare-name fix as a permanent guardrail — a future registry regression to the unmatched `$moneyflow_` style fails these tests with a precise diagnostic rather than the vague `unknown_field` outcome.
- **Universe raw-field gate at the resolver (PR 9b of 2026-05-28, GPT 5.5 Pro round-4 review)**: `handle_validation_object_resolver` in [src/research_orchestrator/validation_steps.py](src/research_orchestrator/validation_steps.py) now calls `_validate_prescription_universe_field_dependencies(prescription, stage='formal_validation', artifact_label=hypothesis_id)` immediately after the factor field gate. Pre-PR-9b the factor gate covered only `prescription.components`; `handle_validation_dataset_build` then independently constructed `raw_field_exprs` and turned `prescription.universe.broad_filters.profitability_field` into `Ref(${profit_field}, 1)` without any registry check. A formal prescription with only approved factor components could pass the factor gate AND still consume a quarantined `$ratio` (hk_hold) through the universe path. PR 9b enumerates the canonical universe-side `$field` set (`$close`, `$adj_factor`, `$total_mv`, `$amount`, plus `$<profit_field>` if set) and runs them through `assert_field_dependencies_eligible` at the resolver, BEFORE any IS work begins. The result is persisted as `universe_field_dependency_report` next to `field_dependency_report` so reviewers can audit which non-factor `$fields` the resolver authorized. Enforcement: [tests/research_orchestrator/test_pr9_validation_field_gate.py::TestPR9bUniverseFieldGate](tests/research_orchestrator/test_pr9_validation_field_gate.py).
- **Dataset_build raw-field defense-in-depth (PR 9b)**: `handle_validation_dataset_build` now runs `assert_field_dependencies_eligible(expressions=list(raw_field_exprs.values()), stage=field_gate_stage, ...)` BEFORE the second `QlibFieldProvider.load_named_expressions(raw_field_exprs, ...)` call. This is a belt-and-suspenders check: if a future change adds a new field to `raw_field_exprs` (e.g. starts loading `$ratio` to honor `northbound_required`, or `$revenue_q` to honor `revenue_floor`) and someone forgets to mirror the addition into `_validate_prescription_universe_field_dependencies`, the runtime check still fires before the Qlib load. The earlier hardcoded `provider.load_named_expressions({"market_cap": "Ref($total_mv, 1)"})` call inside the industry-relative composite branch is structurally constrained to an approved field and does not pass through the gate. Mirror contract: any addition to `raw_field_exprs` MUST also be reflected in the resolver-side helper. Enforcement: [tests/research_orchestrator/test_pr9_validation_field_gate.py::TestPR9bUniverseFieldGate::test_dataset_build_has_defense_in_depth_check](tests/research_orchestrator/test_pr9_validation_field_gate.py) (source-level ordering proof) plus the live FieldApprovalError tests in the same class.
- **Dataset_build IS-stage mapping (PR 9c of 2026-05-28, GPT 5.5 Pro round-5 review)**: `handle_validation_dataset_build` now maps the DAG-level stage to the field-registry stage explicitly — `"oos_test" → "oos_test"`, `"is_only" → "formal_validation"`, `"formal_validation"` and `"registry_publish"` pass through, anything else is treated as ungated. Pre-PR-9c the check used `if stage in {"formal_validation","oos_test","registry_publish"}` which silently skipped the IS leg because `_gate_stage(context)` returns `"is_only"` by default ([src/research_orchestrator/steps.py:132](src/research_orchestrator/steps.py#L132)). The IS leg of a `hypothesis_validation` run is itself a formal stage for the field-status registry — the dataset_build defense-in-depth must fire there too or the future-proofing claim does not hold. Enforcement: [tests/research_orchestrator/test_pr9_validation_field_gate.py::TestPR9bUniverseFieldGate::test_dataset_build_is_stage_maps_to_formal_validation](tests/research_orchestrator/test_pr9_validation_field_gate.py) pins the source-level mapping; `test_dataset_build_gate_fires_on_is_only_behavioral` proves the strict assert fires on a synthetic raw_field_exprs containing `$ratio` when the IS-leg mapping is applied.
- **Approval-evidence binding drift detection (PR 10 of 2026-05-28, post-PR-9c follow-up; hardened by PR 10a + PR 10b + PR 10c)**: every field-registry approval YAML under [config/field_registry/approvals/](config/field_registry/approvals/) that carries a `provider_build_id` + `calendar_policy_id` binding (PR 9a round-3 contract) is machine-validated against the live `provider_build.json` at the configured `storage.qlib_data_dir`. [src/data_infra/approval_evidence.py](src/data_infra/approval_evidence.py) provides `load_approval_bindings()`, `evaluate_approval_evidence_bindings()`, and the strict `assert_no_approval_evidence_drift()`. [scripts/run_daily_qa.py](scripts/run_daily_qa.py) wires this in as the `approval_evidence_binding` audit block (after `no_bare_qlib_features_lint`), so a future provider rebuild whose `provider_build_id` or `calendar_policy_id` differs from any approval YAML's binding fails the daily QA with a precise diagnostic. Pre-PR-10 the binding contract was operator-discipline only.
- **Approval-evidence fail-closed contract (PR 10a of 2026-05-28, GPT 5.5 Pro round-6 review)**: PR 10's original implementation had three fail-open paths that PR 10a converts to hard failures. (1) **Hardcoded manifest path** → `_approval_evidence_binding_check` and `_provider_manifest_check` now share `_resolve_qlib_dir_from_config()`, so both audit blocks validate against the same `storage.qlib_data_dir`. Pre-PR-10a the approval-evidence block hardcoded `data/qlib_data/metadata/provider_build.json`, creating a real divergence whenever the config pointed elsewhere. The helper uses a `project_root=None` sentinel (NOT `PROJECT_ROOT` as a default arg) so behavioral tests that reassign `run_daily_qa.PROJECT_ROOT` at runtime are correctly honored — Python evaluates default args at function-definition time, which would otherwise freeze the original value. (2) **Partial bindings** → `load_approval_bindings` raises `ApprovalEvidenceConfigError` when exactly one of `provider_build_id` / `calendar_policy_id` is declared. Pre-PR-10a a missing axis was treated as a wildcard match, silently reducing the contract from two dimensions to one. The post-PR-10a contract: both absent = legacy skip; both present = validated; exactly one = hard fail. (3) **Malformed / non-dict YAMLs** → also `ApprovalEvidenceConfigError`. Pre-PR-10a these were logged-and-skipped, a path that could silently disappear an approval from the drift check. The post-PR-10a `evaluate_approval_evidence_bindings` removes the wildcard-match logic entirely (both axes are now guaranteed non-None for every binding it sees). Enforcement: 21 tests at [tests/data_infra/test_approval_evidence.py](tests/data_infra/test_approval_evidence.py) covering matched binding, single-axis drift, both-axis drift, legacy skip, missing manifest raises, missing approvals-dir empty, strict-assert remediation guidance, daily-QA source-level wiring + PR 10a shared-resolver proof, PR 10a partial-binding raises (both directions + propagation), PR 10a malformed-YAML / non-dict / empty-YAML raises, live-registry smoke, AND two behavioral tests proving `_approval_evidence_binding_check(project_root=tmp_path)` consumes `config.yaml::storage.qlib_data_dir` (matched → `ok=True`; drifted → `ok=False` with diagnostic naming both declared and current values).
- **Approval-evidence explicit-exemption contract (PR 10c of 2026-05-28, GPT 5.5 Pro round-7 review)**: pre-PR-10c an approval YAML missing BOTH `provider_build_id` and `calendar_policy_id` was silently skipped as "legacy", which could not distinguish a true non-provider-bound record from a new approval that ACCIDENTALLY omitted both keys — the last fail-open path in the governance scanner. PR 10c requires an unbound record to declare `binding_exempt: true` (strict bool — a string `"true"` does NOT exempt) with a non-empty `binding_exempt_reason`; both-absent WITHOUT a valid exemption now raises `ApprovalEvidenceConfigError`. A contradiction guard also raises when both binding keys are present AND `binding_exempt: true`. The committed [2026-05-27_quarantine_prefix_fix.yaml](config/field_registry/approvals/2026-05-27_quarantine_prefix_fix.yaml) (a coverage/diagnostic fix, not a formal-use promotion) now carries the explicit `binding_exempt` marker; the [approvals README](config/field_registry/approvals/README.md) required-fields template now includes `provider_build_id` + `calendar_policy_id` and documents the exemption escape hatch. Enforcement: 39 tests total in [tests/data_infra/test_approval_evidence.py](tests/data_infra/test_approval_evidence.py) — PR 10c adds `TestBindingExemptContract` (both-absent-no-exemption raises, exempt-without-reason raises, exempt-blank-reason raises, exempt+reason skipped, `binding_exempt: false` raises, string-`"true"` raises, both-bound+exempt contradiction raises) and `TestCommittedApprovalsSatisfyContract` (the live committed approvals dir loads without raising; the quarantine_prefix_fix YAML is exempt-skipped not bound), plus flips the two pre-PR-10c both-absent-skip tests to expect a raise.
- **Approval-evidence null/blank-value fail-closed (PR 10b of 2026-05-28, GPT 5.5 Pro round-7 review)**: `load_approval_bindings` in [src/data_infra/approval_evidence.py](src/data_infra/approval_evidence.py) now distinguishes KEY ABSENCE from a key present with a null / empty / blank / non-string VALUE. Pre-PR-10b the code used `data.get("provider_build_id")`, which returns `None` for BOTH an absent key and a key written as `provider_build_id:` (null value). An approval YAML that kept the binding keys but blanked their values — e.g. during a manual provider rebuild or a botched edit — was therefore silently skipped as legacy, a fail-open path in the governance scanner. PR 10b uses `"provider_build_id" in data` for presence (both absent → legacy skip; exactly one present → raise) and then requires each present value to be a non-empty string after `.strip()` (null / `""` / whitespace / non-string → `ApprovalEvidenceConfigError`). Stored binding values are stripped so a trailing-space binding still matches a clean manifest value. Enforcement: 8 new tests in [tests/data_infra/test_approval_evidence.py::TestNullOrBlankBindingFailsClosed](tests/data_infra/test_approval_evidence.py) (both-null, both-empty, both-whitespace, one-null-one-absent, one-valid-one-null, non-string, true-legacy-still-skipped, valid-stripped-and-matches) — 29 tests total in the file.
- **Catalog count = 171, not 191 (PR 10 docstring refresh, post-PR-9c)**: the runtime factor universe is 171 named factors = 147 base catalog (`get_factor_catalog(include_new_data=True)`; 111 + 36 new alpha endpoints) + 4 industry-relative composites (`get_industry_relative_defs()`) + 20 Layer-2 composites (`get_composite_defs()`). Earlier docstrings throughout `src/` AND the root [README.md](README.md) six-module table referenced "191 factors" / "191-catalog" as a forward-looking design target; PR 10 (root README line fixed in the PR 10c round-8 pass) sweeps those to the verified 171. The PR 9a compatibility test `TestFormalFactorCompatibility::test_every_formal_factor_resolves_at_formal_validation` is the live source of truth for the breakdown. Live doc references confirmed clean: `grep -rn 191 README.md src/system.md src/alpha_research/README.md src/data_infra/pipeline/README.md data/data_tracker.md` returns nothing.
- **AGENTS.md sync with CLAUDE.md §3 (PR 10 sync, post-PR-9c)**: AGENTS.md §2a now mirrors every PR 1 → PR 9c hard-invariant entry from CLAUDE.md §3 verbatim, with markdown-link → backtick-path conversion for Codex tone. Pre-PR-10 AGENTS.md was missing every freeze-plan entry from PR 5 onward (CLAUDE.md §11.2 alignment contract was violated across the entire arc). Future rule changes MUST update both files in the same edit pass per the standing instruction.
- **`indicators` dataset is approved in field_status.yaml (PR 9a)**: PR 5 originally seeded only `$pit_*` aliases under `pit_fundamentals`, but the live formal factor catalog has always referenced bare Tushare `fina_indicator_vip` field names (`$roe`, `$or_yoy`, `$netprofit_yoy`, `$debt_to_assets`, `$current_ratio`, `$quick_ratio`, `$ocfps`, `$bps`, `$eps`, `$grossprofit_margin`, `$netprofit_margin`, `$assets_turn`, `$roa`, `$roic`, `$op_yoy`, `$basic_eps_yoy`, `$roe_yoy`, `$q_op_qoq`). These flow through the SAME PIT-anchored pipeline as `$pit_*` (per CLAUDE.md §3, the indicators family is anchored on `ann_date`). PR 9a adds an explicit `indicators` dataset entry with `status: approved` so the 30+ formal factors that depend on them (qual_*, grow_*, lev_*, val_bps_to_price, val_cftp) do not regress. The on-disk pipeline did not change — only the registry now reflects what the factor library has always consumed. Evidence: [config/field_registry/approvals/2026-05-27_indicators_unlisted_to_approved.yaml](config/field_registry/approvals/2026-05-27_indicators_unlisted_to_approved.yaml).
- **Quarantine prefix coverage fix (PR 9a)**: pre-PR-9a `field_status.yaml` declared `field_prefixes: [$moneyflow_, $hk_hold_, $northbound_, $margin_detail_, $margin_]` that matched NOTHING in the actual Qlib provider — moneyflow/hk_hold/margin_detail/stk_holdertrade are NOT in `EVENT_LIKE_DAILY_DATASETS`, so their numeric columns ship with BARE names (`$net_mf_amount`, `$buy_lg_amount`, `$ratio`, `$rzye`, `$rqye`, `$rzmre`, `$rzche`, `$holdertrade_*`). The fail-closed `unknown_field_policy[formal_validation]=fail` preserved safety, but the diagnostic message said "unknown_field" rather than the dataset-specific quarantine reason. PR 9a replaced the unmatched prefixes with explicit `fields:` lists drawn from on-disk verification. Also added a new `stk_holdertrade` dataset (`pending_review`, same status as its alpha-endpoint siblings). No status changes; pure coverage fix. Evidence: [config/field_registry/approvals/2026-05-27_quarantine_prefix_fix.yaml](config/field_registry/approvals/2026-05-27_quarantine_prefix_fix.yaml).
- **OOS seal is consumed on attempt; resume same run for recovery (PR 9, formalizing PR 8d behavior)**: `handle_validation_event_backtest_oos` calls `_claim_holdout_access_if_needed(context)` at handler entry (after the IS-gate-decision short-circuit), BEFORE reading the prescription schedule and BEFORE invoking `run_event_driven_window`. This means: (a) a failed schedule-read or provider-validation consumes the seal slot, and (b) recovery from any post-claim failure REQUIRES using the same `run_dir` + `step_id` so `allow_same_run=context.resumed` permits the second claim. Starting a fresh run with the same `design_hash` after a failed attempt is intentionally refused — the seal exists to make multiple-OOS-attempt overfitting impossible, including across crash-resume boundaries. To recover: use `research_orchestrator_cli.py resume --run-dir <same-run-dir>`, not a new run. Behavioral enforcement: [tests/research_orchestrator/test_pr9_validation_field_gate.py::TestOOSHandlerSealClaimBehavior](tests/research_orchestrator/test_pr9_validation_field_gate.py).
- **PIT-lookahead prevention architecture — "one PIT-semantics layer, two front doors" (2026-05-29/30 prevention plan, PRs #18–#21 + the promotion gate)**: a string-comparison lookahead bug in the hand-rolled `workspace/scripts/sandbox_v*` loaders (dashed `effective_date` lexically compared to compact `trade_date`) inflated the v31/v32/v33 + val_heavy "champions" by ~9 months of earnings foresight (OOS 188.7%→2.0%; val_heavy +81.9%→+9.6% with negative WF). The production backend was always clean; the bug lived only in research code that **bypassed** it. The standing contract that prevents recurrence:
  1. **Two sanctioned front doors only.** Research/sandbox code consumes PIT fundamentals through [src/data_infra/pit_research_loader.py](src/data_infra/pit_research_loader.py) (`load_pit_asof_panel` lag-0 / `load_pit_signal_panel` lag-1, default); formal code through [qlib_windowed_features](src/research_orchestrator/qlib_windowed_features.py). Both share the datetime-correct, **stateful-q0** kernel [src/data_infra/pit_alignment_core.py](src/data_infra/pit_alignment_core.py). NEVER hand-roll PIT alignment; NEVER read `data/pit_ledger/*` directly outside the loader/builder; NEVER string-compare date columns. The production provider is the **oracle** — the loader is bound to it by the live parity test [tests/data_infra/test_pit_loader_provider_parity.py](tests/data_infra/test_pit_loader_provider_parity.py) (lag-0 + lag-1, in `run_daily_qa`).
  2. **Loader is fail-closed.** Unknown/quarantined fields are refused even at `sandbox_screening` (positive field-registry match required); `duplicate_policy` is fail-closed (`provider_stateful_q0` for Case-A multi-period; raises on Case-C same-period conflict incl. mixed null/non-null); `apply_provider_bounds=True` closes the delist/IPO-lag leak.
  3. **PIT002 lint is a hard gate.** [scripts/lint_no_unsafe_pit_dates.py](scripts/lint_no_unsafe_pit_dates.py) (raw-`pit_ledger`-read = hard error, docstring-aware; PIT001 date-stringify = staged warning) runs in `run_daily_qa.py` over `src`+`workspace`; exemptions only via the schema-validated [config/lint/unsafe_pit_dates_allowlist.yaml](config/lint/unsafe_pit_dates_allowlist.yaml) (no inline noqa for PIT002). Only the sanctioned `ARCHIVE_SKIP_ROOTS` are skipped (root-specific).
  4. **Legacy lineage is contained.** The 73 invalidated `sandbox_v*` scripts are local-only (untracked, gitignored) under `workspace/scripts/archive/pit_lookahead_legacy_2026_05/` with a tracked README manifest; [tests/architecture/test_dormant_module_boundaries.py](tests/architecture/test_dormant_module_boundaries.py) forbids live code importing `sandbox_v*` or path-referencing the archive.
  5. **Promotion gate (decision layer — ENFORCED at the registry transition).** Promoting a `strategy_candidate` to the privileged registry **status `approved`** is gated inside `StrategyRegistryStore.set_status` ([src/research_orchestrator/registries/strategy_registry.py](src/research_orchestrator/registries/strategy_registry.py)): it **requires a `current_git_sha`** (mandatory for privileged transitions — binds the approval to a committed HEAD; omitting it raises) and raises `PromotionGateError` unless `promotion_evidence` passes `assert_promotion_artifact_eligible` ([src/research_orchestrator/release_gate.py](src/research_orchestrator/release_gate.py)) — i.e. an **independent PIT-correct reproduction** source ∈ `{qlib_windowed_features, joinquant_native_pit, audited_pit_source}` (a sandbox/loader panel, even the parity-verified `pit_research_loader`, is INSUFFICIENT; `audited_pit_source` additionally needs `source_name`+`audit_artifact`), PLUS `unsafe_pit_dates_lint`=="passed", `live_provider_parity` passed-or-legally-not-required (illegal if `pit_research_loader` was used), and a clean git tree / matching `git_sha`. The forward-looking deployment LABELS (`champion`/`deployment_candidate`/`live_candidate`) carry NO existing call site yet — any future promotion tooling that assigns them MUST call the **full** `assert_promotion_artifact_eligible` (NOT the source-only `assert_promotion_eligible` helper, which skips the lint/parity/clean-tree/git checks). This is the guard the val_heavy near-deployment lacked. Statuses split from labels per GPT review. Enforcement: [tests/research_orchestrator/test_promotion_gate.py](tests/research_orchestrator/test_promotion_gate.py) (incl. the `set_status` wiring).
- **Factor-lifecycle Phase-1 formal gates (2026-05-31, factor_lifecycle plan PRs P1.1+P1.2+P1.3; Codex round-4/5 reviewed, GPT cross-review P0-fixed)**: the formal factor registry now enforces a writer gate, a reader gate, AND a definition-binding gate so a `draft` factor stays usable for discovery research but can NOT be silently treated as formally-approved by a formal validation, and a formal validation can NOT run against a registry row whose definition has drifted from the catalog.
  1. **Writer gate (P1.1).** `FactorRegistryStore.set_status("approved")` ([src/alpha_research/factor_registry/store.py](src/alpha_research/factor_registry/store.py)) requires a MANDATORY `current_git_sha` + a `promotion_evidence` artifact passing `assert_promotion_artifact_eligible` (mirrors the strategy-registry promotion gate); otherwise `PromotionGateError`. A new `approval_validity` column (`valid`/`requires_revalidation`/`stale`) qualifies an approval; `_normalize_approval_validity` fail-closes any pre-existing `approved` row with a blank validity to `requires_revalidation` on load; a successful approve stamps `valid`. `export_current(status="approved")` excludes non-`valid` rows unless `include_invalid=True`. The registry CLI `set-status --status approved` ([workspace/scripts/factor_registry_cli.py](workspace/scripts/factor_registry_cli.py)) requires `--promotion-evidence-json` + a clean tree (exits 2 otherwise) so the CLI is not an unaudited approval door.
  2. **Reader gate "resolve-but-label" (P1.2, Codex round-5; SUPERSEDED the round-2 `draft→_unresolved` design — that would hard-fail the 3 discovery DAGs whose `object_resolver` step raises on unresolved at [steps.py:444](src/research_orchestrator/steps.py), since all registry rows are `draft`).** `resolver._resolve_formal_factor` ([src/research_orchestrator/resolver.py](src/research_orchestrator/resolver.py)) RESOLVES every current row (status stays `resolved`) but LABELS `source_layer` by registry status+validity: `approved`+valid→`formal`; `candidate`+valid→`factor_registry_candidate`; `draft`→`factor_registry_draft`; `approved`+non-valid→`factor_registry_stale`; `deprecated`→`factor_registry_deprecated`. A requested `definition_hash` is enforced as a REAL match filter (a name match with a mismatched hash does not resolve — closes same-name shadowing). The **SOLE** formal-permission point is the explicit allow-set in `handle_validation_object_resolver` ([src/research_orchestrator/validation_steps.py](src/research_orchestrator/validation_steps.py)): accept `formal` (+ `factor_registry_candidate` IFF `prescription.allow_candidate_components`), reject every other layer including plain candidate-registry `candidate`. Safe because discovery publish writes `candidate`/`observed` ([typed_store.py:452](src/research_orchestrator/registries/typed_store.py)), never `approved`. `resolve_assets` adds a `factor_registry_hits_by_layer` summary so non-formal resolved rows stay visible. Enforcement: [tests/alpha_research/test_factor_registry.py](tests/alpha_research/test_factor_registry.py), [tests/alpha_research/test_research_orchestrator.py](tests/alpha_research/test_research_orchestrator.py), [tests/research_orchestrator/test_pr9_validation_field_gate.py](tests/research_orchestrator/test_pr9_validation_field_gate.py) (`TestPR12FormalAllowSet`).
  3. **Definition-binding gate (P1.3).** `handle_validation_object_resolver` ([src/research_orchestrator/validation_steps.py](src/research_orchestrator/validation_steps.py)) calls `_assert_no_definition_drift` AFTER the allow-set and BEFORE the field gate / ANY compute: every resolved formal factor's STORED registry `definition_hash` must equal the CURRENT code catalog's hash, recomputed via `FactorRegistryStore.current_catalog_definition_hashes()` which REUSES `_build_catalog_snapshots` — the SAME algorithm `sync_catalog` writes, so base/composite/industry-relative all compare apples-to-apples (Codex round-4 parity requirement). A mismatch (registry row stale vs `catalog.py`, or factor absent from the current catalog) raises `FactorDefinitionDriftError` so a backtest never runs against a definition the registry can no longer attest. A factor-registry entry permitted into formal validation with NO stored `definition_hash` (or no canonical_id) — a malformed/legacy approved row — is itself treated as **drift** and refused (fail-closed, GPT cross-review); only non-factor-registry layers, which never reach the check, are skipped. Enforcement: [tests/research_orchestrator/test_pr9_validation_field_gate.py](tests/research_orchestrator/test_pr9_validation_field_gate.py) (`TestPR13DefinitionBindingGate`, incl. `test_missing_definition_hash_fails_closed`), [tests/alpha_research/test_factor_registry.py](tests/alpha_research/test_factor_registry.py) (`test_current_catalog_definition_hashes_round_trip`).
  4. **GPT cross-review P0 fixes (2026-05-31).** Two approval-bypass holes found in post-implementation review + fixed: (a) `set_status`/`StrategyRegistryStore.set_status` force-overwrite `artifact["promotion_status"]=status` (NOT `setdefault`) so a caller-supplied `promotion_status="draft"` can't downgrade the gate to non-privileged; (b) `set_approval_validity` refuses `validity="valid"` on an `approved` row (re-affirming an approval must go through the `set_status` promotion gate). Enforcement: the `cannot_be_bypassed_via_evidence_status` / `cannot_revalidate_approved_row` tests in [tests/alpha_research/test_factor_registry.py](tests/alpha_research/test_factor_registry.py) + [tests/research_orchestrator/test_promotion_gate.py](tests/research_orchestrator/test_promotion_gate.py).
  5. **TestingLedger file-lock (P1.5).** `TestingLedgerStore.record_event` / `record_verdict` ([src/alpha_research/testing_ledger.py](src/alpha_research/testing_ledger.py)) wrap the FULL read-check-write in a `file_lock` (lazy import, cycle-safe), mirroring `CacheManifestStore.record_cache_write` / `HoldoutSealStore.claim_holdout_access`. `record_verdict` holds ONE lock over its measurement lookup + prior-verdict supersedes lookup + append via an unlocked `_append_event_unlocked` helper (NOT the public `record_event`), so it does not re-enter the non-reentrant lock — no self-deadlock (Codex round-4). Lock file `<ledger_root>/testing_ledger.lock`. Enforcement: [tests/research_orchestrator/test_lock_concurrency.py](tests/research_orchestrator/test_lock_concurrency.py) (`TestTestingLedgerConcurrency`: 8 concurrent distinct `record_event` → 8 persisted; `record_verdict` no self-deadlock).
  6. **`seal_key` / FrozenSelectionSet OOS-seal migration (P1.4).** The holdout seal is now keyed by `seal_key` (defaults to `design_hash` for full back-compat) so the OOS budget is spent per FROZEN SELECTION SET, not per mutable `design_hash`. `HoldoutSealStore` ([src/research_orchestrator/holdout_seal.py](src/research_orchestrator/holdout_seal.py)) gained a `seal_key` column; `_load()` backfills `seal_key=design_hash` for pre-P1.4 rows (no mixed write-`seal_key`/read-`design_hash` window — every read routes through `_load`); `claim_holdout_access` + `list_events` accept `seal_key`. `HoldoutContext` ([sealed_backtest_runner.py](src/research_orchestrator/sealed_backtest_runner.py)) + `ResearchAccessContext` ([research_access_context.py](src/research_orchestrator/research_access_context.py)) carry `seal_key` with an `effective_seal_key` property (falls back to `design_hash`); `SealedBacktestRunner` + the two engine backstops ([event_driven](src/backtest_engine/event_driven/__init__.py) / [vectorized](src/backtest_engine/vectorized/__init__.py)) claim/check by `effective_seal_key`; `hypothesis_cli.py verify-seal` gained `--seal-key`. The new [frozen_selection_set.py](src/research_orchestrator/frozen_selection_set.py) `FrozenSelectionSet.frozen_set_hash` is the future `seal_key` producer — sha256 over `{schema_version, sorted selected (factor_id, version, definition_hash, expected_direction), candidate_pool_hash, selection_rule_hash, eval_protocol_hash, metric, portfolio_side, universe, time_split_window, rebalance, neutralization}`, EXCLUDING all pass/fail wording AND provider/calendar/build IDs (cosmetic/prose/enum-case edits don't change it; changing direction / pool / rule / protocol / metric / side / universe / window / rebalance / neutralization does). **No live caller passes `frozen_set_hash` yet** — everything defaults to `design_hash`; wiring a real `FrozenSelectionSet`-driven run is future work. Enforcement: [tests/research_orchestrator/test_frozen_selection_set.py](tests/research_orchestrator/test_frozen_selection_set.py).
- **Factor-lifecycle Phase-2 registry evidence schema (2026-05-31, factor_lifecycle plan PRs P2.1-P2.4; GPT cross-reviewed; PR #31)**: the factor registry now carries walk-forward/OOS + GROSS long-only evidence + signal-role metadata + provenance, populated by a definition-bound importer ([src/alpha_research/factor_registry/store.py](src/alpha_research/factor_registry/store.py)). **BOUNDARY:** Phase 2 writes EVIDENCE/metadata ONLY — never `status` / `approval_validity` / `definition_hash` — so the Phase-1 gates are unaffected. (1) **Schema (P2.1):** `factor_evidence` gains `is/oos_rank_icir`, `sign_consistency`, `oos_ls_sharpe`, `retain_pct`, `lo_excess_ann_gross`, `lo_sharpe_gross`, `lo_hit`, `evidence_class`, `formal_evidence_eligible`, `source_path`/`source_hash`, `provider_build_id`, `calendar_policy_id`; `factor_master` gains latest-mirrors + `long_only_viable_provisional`, `signal_role`(+`_suggested`), `expected_direction`, `requires_inverse_for_long_only`, `approved_uses`, `validation_scope`, `field_eligibility_snapshot_json`, `last_revalidated_at`, provenance. New columns have fail-closed load defaults (`_normalize_phase2_metadata`: `signal_role`→`unassigned`, `long_only_viable_provisional`→`non_viable`), mirroring P1.1. (2) **Viability (P2.2):** `long_only_viable_provisional` is DERIVED in `refresh_master_derived_fields` from the GROSS LO metric (fail-closed: missing→`non_viable`; sharpe≥1.0 ∧ excess>0 ∧ hit≥0.60→`viable`; sharpe<0.5 ∨ excess≤0→`non_viable`; sharpe≥0.5 ∧ excess>0→`review_only`). The metric is GROSS (`long_only_topbucket` has no cost) — the FORMAL cost-adjusted viability is a later-phase recompute; `review_only` is fail-closed for formal long-only use. (3) **Importer (P2.3):** `import_revalidation` attaches evidence ONLY to factors whose registry `definition_hash` == the current catalog hash (fail-closed via `current_catalog_definition_hashes`; drifted/unknown SKIPPED, never by name), labels rows `historical_investigation` / `formal_evidence_eligible=False` with the bound `source_hash`, writes evidence only, idempotent per `(run_id, factor_id, version)`. Real-data smoke: 171 attached / 0 skipped, 2 viable + 2 review_only + 167 non_viable, all `draft`. (4) **Surfacing (P2.4):** registry review HTML + `factor_registry_cli.py import-revalidation`. (5) **GPT PR-#31 cross-review hardening (2026-05-31):** post-implementation review (NO-GO → 5 findings verified + fixed; boundary re-confirmed) hardened `refresh_master_derived_fields`: it now (a) mirrors `latest_provider_build_id`/`latest_calendar_policy_id`/`last_revalidated_at` from the latest `run_type=='revalidation'` evidence ONLY (re-review fix: a plain `sync_catalog`'s `catalog_sync` row no longer stamps `last_revalidated_at`) + sets `signal_role_suggested='long_only_alpha'` for `viable` (never touching authoritative `signal_role`); (b) derives viability from the SINGLE latest LO-Sharpe-bearing evidence row's full `(sharpe,excess,hit)` tuple — a partial latest tuple → `non_viable`, NO cross-row metric mixing; (c) binds the P2 mirrors to the row's CURRENT `definition_hash` — evidence with a nonblank `source_hash` ≠ current hash (stale-definition / skip-drifted) is ignored even though it remains in `factor_evidence`; (d) populates `field_eligibility_snapshot_json` from the LIVE field-status registry at `formal_validation` — BASE factors resolve their `$fields` (`resolved:true`), COMPOSITE/INDUSTRY_RELATIVE pseudo-expressions + any registry-load error are fail-closed `resolved:false` (NEVER a false `all_allowed=true`; empty string is likewise fail-closed for Phase-3 consumers). `retain_pct` (finding 5) is schema-reserved/DEFERRED — absent from all current inputs, must not be read as populated until WF-retention inputs are wired. Enforcement: [tests/alpha_research/test_factor_registry.py](tests/alpha_research/test_factor_registry.py) (schema defaults, viability boundaries, definition-bound import skip-drift/skip-unknown, HTML/CLI, + the 5 PR-#31 regression tests — finding-2/finding-3 AND the re-review revalidation-scoping test proven to fail on the pre-fix logic; 28 passed). GPT re-review of the fixes (commit `75c2e45`) found one residual must-fix — `last_revalidated_at` was set from ANY bound row so a plain `sync_catalog` stamped all 171 "revalidated"; fixed by scoping the P2 evidence-mirrors to `run_type=='revalidation'` only.
- **Factor-lifecycle Phase-3 status-aware factor readers (2026-05-31, factor_lifecycle plan §4 Phase 3; design cross-reviewed → conditional-GO integrated → confirmed GO; branch `factor-lifecycle-p3`)**: an ADDITIVE sandbox/discovery read layer over the formal factor registry — [src/alpha_research/factor_library/selection.py](src/alpha_research/factor_library/selection.py), re-exported from `factor_library/__init__.py`. **BOUNDARY:** `get_factors`/`get_factor_selection` are NOT the formal gate — formal validation still resolves through the P1.2 resolver allow-set + P1.3 definition-binding. ZERO of the 42 `get_factor_catalog` call sites change; `get_factor_catalog` stays the authoritative computable definition source. (1) **`get_factors(*, stage, status_in, prioritize=None, include_new_data=False, registry_dir=None, on_drift='skip')`** → `OrderedDict{base_factor_id: qlib_expr}`, BASE only, compute-ready drop-in for `compute_factors`. Registry = status FILTER; CODE = definition SOURCE (registry `COMPOSITE(...)` pseudo-expressions NEVER returned for compute). `stage` REQUIRED + must be in `SANDBOX_STAGES`; a `FORMAL_STAGES` value raises `FormalStageNotAllowedError`. `status_in` REQUIRED + explicit (no default — all 171 rows are `draft`). `on_drift='skip'` default (a registry row whose `definition_hash` ≠ the current code hash is excluded; `code_warn`/`raise` opt-in). Empty registry raises `RegistryNotSyncedError` (NEVER `{}`); a true no-match returns `{}`. (2) **`get_factor_selection(...)` → `FactorSelection`** (`base_expressions` + `composite_defs` + `industry_relative_defs` + per-factor `records`): selected composites'/industry-relative base DEPENDENCIES are auto-unioned into `base_expressions` for compute-readiness, tagged `selection_role='dependency'` / `selected=False` / `dependency_included=True` so a draft base pulled in for compute can NEVER be confused with a status-matched selection; `get_factors` is the STRICT base-status filter (dependency-only bases excluded). (3) **`sync_catalog_to_registry(*, registry_dir=None, record_run=True, dry_run=False)`** — thin wrapper over `FactorRegistryStore.sync_catalog` + a PARITY diff (`new_drafts`/`new_versions`/`catalog_only`/`registry_only`/`parity_ok`); NEVER auto-run inside a read (reads stay reads); NEVER writes `approved` (Phase-1 writer gate stands). **Defense-in-depth bypass guard (design-review req):** the runtime `stage=` refusal PLUS an AST-USAGE architecture test ([tests/architecture/test_get_factors_boundary.py](tests/architecture/test_get_factors_boundary.py)) forbidding any formal-path module (`validation_steps`, `release_gate`, `resolver`, `steps`, `sealed_backtest_runner`, `prescription_runtime`, `event_driven/`, `vectorized/`) from importing/referencing `get_factors`/`get_factor_selection` — AST not text, because formal paths LEGITIMATELY import the sibling `get_factor_catalog`. Enforcement: [tests/alpha_research/test_factor_selection.py](tests/alpha_research/test_factor_selection.py) + [tests/architecture/test_get_factors_boundary.py](tests/architecture/test_get_factors_boundary.py) (21 tests: formal-stage refusal, name-parity, drift-skip-default, empty-raises, status-filter, off-status dependency contract, sync parity, AST aliased-import catch). **GPT PR-#32 post-impl review (NO-GO → 2 fixes → GO):** (a) `sync_catalog_to_registry` `parity_ok` now ALSO requires `not registry_only` — an orphan current row (a factor removed from code but still `is_current`) is a parity violation that the pre-fix formula (`not catalog_only and not drifted`) missed; regression test `test_sync_parity_ok_false_when_registry_only_orphan_present`. (b) `prescription_runtime` (hypothesis_validation Gate-C universe materialization, imported by `validation_steps`) added to the AST-boundary `FORMAL_PATHS`.
- **Factor-lifecycle Phase-4 walk-forward revalidation modules (2026-06-01, factor_lifecycle plan §4 Phase 4; design cross-reviewed → conditional-GO ×2 integrated → confirmed GO; branch `factor-lifecycle-p4`)**: the legacy walk-forward revalidation SCRIPTS are ported into a tested package [src/alpha_research/factor_lifecycle/](src/alpha_research/factor_lifecycle/) (`metrics` / `status_rules` / `revalidation` / `walk_forward_validation` / `report`). **TWO strictly-separated modes — the separation IS the leakage guard:** (1) **`run_is_walk_forward`** (FORMAL, IS-only, `draft→candidate` evidence) is structurally bounded to `TimeSplit.is_end`. Because the forward-return label is FUTURE-looking (`forward_return = Ref(ADJ_CLOSE, 0 - h)`, [operators.py:1044](src/alpha_research/factor_library/operators.py)), bounding the factor date alone is NOT enough — THREE belts enforce the **label-realization** boundary: (a) factors computed with `horizons=None` over `[is_start, is_end]`; (b) the label is built from adjusted close loaded ONLY to `is_end`, so `groupby(instrument).shift(-h)` NaNs (drops) the last `h` rows per instrument; (c) `IsWindowedPanel.__post_init__` raises `IsEndLeakageError` unless `max_factor_date` AND `max_label_realization_date` (the factor date shifted `+h` TRADING days via `data/reference/trade_cal.parquet`) are both `≤ is_end`. The IS-internal holdout uses a 3+1+1 rolling config (the default 5+2+1 yields ZERO folds for the 2014-2020 window); a generated factor with no valid heldout block RAISES `NoHeldoutBlockError` (fail-closed). The result carries NO `oos_*` field and labels `evidence_kind` (`generated_heldout` vs `a_priori`) on BOTH the result and every per-factor row. (2) **`run_historical_*`** (NON-formal, full-window IS+OOS) is a faithful port of the legacy scripts, labeled `historical_investigation` (Phase 2 imports it as `formal_evidence_eligible=False`); the `revalidate_*.py` scripts are reduced to thin wrappers over it. **Split status rules:** `assign_historical_status` (oos-based parity) vs `assign_candidate_status` (IS-only, consumes NO `oos_*`, only `candidate`/`draft`); long-only viability stays Phase-2 METADATA, not a lifecycle input. `metrics`/`report` reproduce the scripts' formulas + the exact CSV columns/rounding (round-trips through Phase-2 `import_revalidation`). Enforcement: [tests/alpha_research/test_factor_lifecycle_metrics.py](tests/alpha_research/test_factor_lifecycle_metrics.py) + [test_factor_lifecycle_revalidation.py](tests/alpha_research/test_factor_lifecycle_revalidation.py) + [test_factor_lifecycle_walk_forward.py](tests/alpha_research/test_factor_lifecycle_walk_forward.py) (30 tests: the label-realization leakage bound, the 0-fold pin, fail-closed, spy `horizons=None`, no-`oos_*`, metric parity, CSV round-trip). Scope: modules + tests + thin wrappers only — NO new revalidation RUN (existing CSVs stand; the formal candidate gate is not yet wired into the orchestrator — Phase 5).

---

## 4. Python Environment & Path Discipline

- Interpreter: `E:\量化系统\venv\Scripts\python.exe`
- Pip: `E:\量化系统\venv\Scripts\pip.exe`
- Pin exact versions in [requirements.txt](requirements.txt); align it with actual imports.
- Code in `src/` must derive paths from [config.yaml](config.yaml) or project-root-relative references. Hardcoded absolute paths are acceptable only in one-off `workspace/scripts/` or `scripts/` utilities.
- Use `pathlib` or `os.path.join`, not string concatenation, for paths.
- Never write persistent project artifacts to `%TEMP%`, `AppData`, `/tmp`, or any path outside `E:\量化系统\`.

---

## 5. Directory Hygiene

- **Project root** is for configuration, documentation, and top-level directories only. Do not accumulate ad-hoc logs, exports, audit dumps, or temporary guides at root — move them to `logs/` or `workspace/outputs/`.
- **`data/`** is for datasets and documentation only. No `.py` files. Raw and intermediate datasets are Parquet unless an existing subsystem requires another format.
- **`workspace/`** is the only place for active notebooks, prototypes, experiment scripts, and generated research artifacts. Organize as:
  - notebooks under `workspace/research/{topic}/`
  - helper scripts under `workspace/scripts/`
  - research configs under `workspace/configs/`
  - generated outputs under `workspace/outputs/`
- **`scripts/`** is for bootstrap and maintenance utilities that operate on durable datasets, not ad-hoc one-offs.
- **`logs/`** receives rotated console output from long-running pipelines.

Do not move or rename files in `data/qlib_data/` by hand — those are derived artifacts. Regenerate via the proper builder scripts.

---

## 6. Data Operations (read [.agents/rules/data-operations.md](.agents/rules/data-operations.md) for depth)

### 6.1 Tushare Safety (the rule that has bitten us most)

- **Never run parallel fetchers against Tushare Pro** under any circumstance. Account-level rate limits make it counterproductive and risk lockouts.
- All API calls must go through `TushareFetcher._safe_api_call()` with the built-in retry/backoff. Do not bypass it.
- If you hit repeated 429s or timeouts, **increase the sleep interval** instead of retrying more aggressively. The default `base_sleep=1.5` should not be reduced without evidence.
- Prefer `StorageManager.insert_*` helpers over manual Parquet writes.

### 6.2 Live Pipeline Entry Points

These are the supported entry points. Do not reintroduce or document deprecated legacy script names or the removed Airflow stub.

- [src/data_infra/pipeline/init_market_data.py](src/data_infra/pipeline/init_market_data.py) — Phase 1 bootstrap (prices, valuation, reference)
- [src/data_infra/pipeline/init_fundamentals_data.py](src/data_infra/pipeline/init_fundamentals_data.py) — Phase 2 bootstrap (statements, dividends, industry, index weights)
- [src/data_infra/pipeline/init_factor_data.py](src/data_infra/pipeline/init_factor_data.py) — Phase 3 (cashflow, forecast, moneyflow, hk_hold, margin_detail, holder, stk_limit)
- [src/data_infra/pipeline/refresh_indicator_history.py](src/data_infra/pipeline/refresh_indicator_history.py) — staged historical VIP indicator refresh
- [src/data_infra/pipeline/build_qlib_backend.py](src/data_infra/pipeline/build_qlib_backend.py) — staged PIT backend builder, supports `--stage full | upstream-only | provider-only`
- [src/data_infra/pipeline/update_daily_data.py](src/data_infra/pipeline/update_daily_data.py) — daily routine maintenance + incremental Qlib refresh
- [src/data_infra/pipeline/verify_database.py](src/data_infra/pipeline/verify_database.py) — raw integrity gate (now also runs the PIT live regression harness as a publish gate)
- [scripts/fetch_suspend_d_historical.py](scripts/fetch_suspend_d_historical.py) — one-time `suspend_d` historical bootstrap (P1-1; not wired into automation)
- [scripts/refresh_namechange.py](scripts/refresh_namechange.py) — idempotent refresh of `data/reference/namechange.parquet` (P1-2; not wired into automation)
- [scripts/run_daily_qa.py](scripts/run_daily_qa.py) — manual QA orchestrator (see §6.2a below)
- [scripts/fetch_new_alpha_endpoints.py](scripts/fetch_new_alpha_endpoints.py) — one-time bootstrap for 5 new alpha endpoints (top_list, top_inst, block_trade, stk_holdertrade, cyq_perf); not wired into automation

### 6.2a Daily QA Runner (manual)

Before any serious research session or after any data-infra change, run:

```bash
venv/Scripts/python.exe scripts/run_daily_qa.py
```

This orchestrates `DataAuditor.audit_daily_files` → `audit_qlib.py` smoke →
`tests/data_infra/test_provider_boundary.py` → `tests/data_infra/test_pit_live_provider.py`.
It writes a structured report to `logs/qa_report_<ts>.json` and exits
non-zero on any failure. It is intentionally NOT a scheduler / alerter
at this stage (because the trade calendar is intentionally frozen at
2026-02-27 while the system is being built).

### 6.3 Backend Rebuild Discipline

- A full Qlib rebuild (`mode="all"`) is expensive — use only for initial loads, corrected backfills, or schema-level changes.
- For daily updates use `mode="update"`.
- For sandbox validation baskets, prefer `--mode update --stage provider-only --touched-symbols <basket>` so the builder creates a minimal staged provider instead of copying the full published tree.
- After any rebuild, verify with [scripts/audit_qlib.py](scripts/audit_qlib.py), [tests/harnesses/qlib_smoke.py](tests/harnesses/qlib_smoke.py), [scripts/verify_phase2.py](scripts/verify_phase2.py), or targeted `D.features()` spot checks.
- **Publish atomicity (P0-6)**: `StagedQlibBackendBuilder.publish()` uses `os.replace()`, which is atomic ONLY when the staged provider and the target `data/qlib_data/` live on the same volume. `publish()` now hard-fails with a `BuildGateError` if the two paths have different `os.stat().st_dev` values. If you ever see that error, move the staged build onto the same drive as `data/qlib_data/` before publishing.
- **Deterministic rebuild (P0-4)**: `_normalize_periodic_dataset` injects `_src_file` and `_src_ordinal` hidden columns during raw ingest so the tie-break in `collapse_duplicate_versions` and `canonicalize_report_variants` is reproducible across machines. These columns are stripped before writing normalized parquet to disk, so the on-disk schema is unchanged. A WARNING log fires whenever the tail tie-break is actually needed.

### 6.4 Mutation Safety

- Scripts that modify existing `data/` Parquet files must log which files will be touched **before** they start.
- Support a `--dry-run` flag when practical.
- Never overwrite raw data in place without a backup path or a deduplication guarantee.
- After any bulk operation, update [data/data_tracker.md](data/data_tracker.md).

---

## 7. Research Integrity (read [.agents/rules/research-integrity.md](.agents/rules/research-integrity.md) for depth; the hypothesis workflow now lives in Section 10 of that same rule file)

These rules are non-negotiable. Violating them invalidates the research even if the code runs.

The detailed gated lifecycle now lives in Section 10 of `.agents/rules/research-integrity.md`. Use that section for the 10-stage workflow, the 5 human gates, pre-registration, sealed OOS, multiple-testing, and the pointers to `src/research_orchestrator/hypothesis.py`, `workspace/scripts/hypothesis_cli.py`, and the YAML factor spec schema.

Operationally, v3.1 formal non-audit runs now insert `gate_evaluation -> gate_concern_scoring -> gate_review` before publication. `gate_concern_scoring` is the `pause_for_input` step and now uses a typed `PauseForInputPayload` from `src/research_orchestrator/dag.py`; `gate_review` is the `pause_for_gate` step that writes the final gate report and accepts `approved`, `rejected`, or `quarantined`; seal-aware backtest execution flows through `src/research_orchestrator/sealed_backtest_runner.py`; the first pre-load window clamp now lives in `src/research_orchestrator/window_enforcement.py`; and cache/window safety is then reinforced through `src/research_orchestrator/cache_manifest.py` plus `src/research_orchestrator/qlib_windowed_features.py`. `workspace/scripts/hypothesis_cli.py verify-seal` now uses exit codes `0=untouched`, `1=OOS already touched`, `2=malformed hash`.

1. **No lookahead, ever.** Use PIT data, align fundamentals on `ann_date`, apply `shift(1)`, document PIT compliance for any new pipeline.
2. **Temporal splits only.** Never random splits. Walk-forward (`5y train / 2y validation / 1y test, step 1y`) is the project standard, used by [event_driven_strategy_research.py](workspace/research/alpha_mining/event_driven_strategy_research.py).
3. **Out-of-sample is sacred.** The final test set is run **once** per strategy variant. If iteration is driven by test results, that is no longer OOS — disclose it explicitly. Log the first test result before any follow-up adjustments.
4. **Factor evaluation standard**: IC, RankIC, ICIR, quantile spread, monotonicity, decay, and turnover before any factor is promoted.
5. **Factor documentation**: every factor in [src/alpha_research/factor_library/](src/alpha_research/factor_library/) should state its formula, data source, decay horizon, price basis (adjusted/raw/reported), and whether it is raw or neutralized.
6. **MLflow logging is mandatory** for substantive model-training or backtest runs. Use `ExperimentTracker` from [mlflow_tracker.py](src/alpha_research/mlflow_tracker.py).
7. **Survivorship bias**: use historical universes that include delisted names. Do not filter to currently listed stocks only.
8. **Centralized performance analysis**: all serious backtest evaluation must use [src/result_analysis/](src/result_analysis/). Do not reimplement Sharpe / MDD / turnover / win rate in a notebook. If a metric is missing, add it to [metrics.py](src/result_analysis/metrics.py).
9. **Phase 3 factor framework**: prefer `get_factor_catalog()`, `compute_factors()`, and `add_composites()` over bespoke factor pipelines. Prefer Qlib expressions over slow `groupby().apply()` whenever an operator exists.
10. **No hedge words in quantitative analysis.** When analyzing a strategy result, a discrepancy between two backtests, or any quantitative claim, do NOT use "likely", "possibly", "probably", "I think", "appears to", "seems to", "could be", "might be", or any other epistemic hedge. Either you have run the data and can state the exact cause with certainty, or you must explicitly mark the claim as unverified and propose the specific test that would resolve it. The unacceptable failure mode is presenting a plausible-sounding guess as if it were an answer. The acceptable failure mode is: "I have not verified X yet. The test that would resolve it is Y. Running it now." Quantitative finance does not allow plausibility — it requires the actual data or an explicit "I do not know, here is how I will find out". Reference the diagnostic in `Knowledge/temp_plan/` whenever a divergence is attributed to a specific mechanism: name the dataset/script/output that proves it. Suggestions are fine *only* when explicitly labeled as hypotheses with a stated falsification plan.

---

## 8. Signal Construction & Backtesting (read [.agents/rules/signal-backtesting.md](.agents/rules/signal-backtesting.md) for depth)

### 8.1 The four-layer pipeline (mandatory)

Every backtest follows: **factor computation → universe selection → signal construction → execution.** Keep concerns separated. Factor values in Layer 1, eligibility in Layer 2, desirability in Layer 3, tradability in Layer 4.

- **Layer 1 (factor)**: compute factors on the full market *before* sub-universe filtering. Lookbacks need full coverage.
- **Layer 2 (universe)**: represent membership as boolean masks, not row drops. Do not filter on tradability here — suspended names still need ranking context.
- **Layer 3 (signal)**: rank within the intended sub-universe, not the full market. Forward-fill within membership when continuity is required.
- **Layer 4 (execution)**: never encode tradability inside the signal.

### 8.2 Engine choice

- [VectorizedBacktester](src/backtest_engine/vectorized/) for rapid signal screening and multi-signal comparison.
- [EventDrivenBacktester](src/backtest_engine/event_driven/) for realistic execution, JoinQuant parity, corporate-action sensitivity. Use `preload_features()` — without it, a 1-year backtest is 5+ minutes; with it, ~24 seconds.

### 8.3 Production research execution defaults

For serious vectorized runs, set these explicitly. Do not rely on the convenience defaults:

- `deal_price='open'`
- `only_tradable=False`
- `forbid_all_trade_at_limit=True`
- realistic transaction costs
- `limit_threshold` aligned with the segment mix being tested

### 8.4 Banned anti-patterns

1. Filtering before factor computation
2. Dropping rows that should remain available for forward-fill
3. Encoding tradability inside the signal
4. Mixing ranking scope with execution filters
5. Omitting signal forward-fill when methodology requires continuity

---

## 9. Research Orchestrator Workflow

The orchestrator at [src/research_orchestrator/](src/research_orchestrator/) is the preferred way to run formal research. The 7 built-in profiles are: `factor_screening`, `theme_strategy`, `event_driven_signal_research`, `ml_signal_model_research`, `strategy_improvement`, `benchmark_audit`, **`hypothesis_validation`**.

**Discovery vs validation (added 2026-04-28, plan `jolly-seeking-lollipop`):** the first 5 profiles are **discovery** profiles — they auto-search a recipe space and pick the empirically best variant. `hypothesis_validation` is the **validation** profile — it runs a fully-prescribed recipe (universe + components + weights + topk + rebalance + cost model) verbatim through IS+gate+OOS+publish with no auto-search. Use validation when you have a specific recipe to test (e.g., from prior discovery research); use a discovery profile when you want to find the best recipe within a search space. The validation profile requires `hypothesis.prescription` to be set (a `PrescribedRecipe` defined in `src/research_orchestrator/hypothesis.py`); discovery profiles ignore the prescription field. Register validation hypotheses with `hypothesis_cli.py register --profile-id hypothesis_validation` to opt into profile-aware floor validation (default validates against ALL profiles).

CLI entry: [workspace/scripts/research_orchestrator_cli.py](workspace/scripts/research_orchestrator_cli.py)

```
research_orchestrator_cli.py profiles                   # list profiles
research_orchestrator_cli.py plan --request-file ...    # compile DAG only
research_orchestrator_cli.py run  --request-file ...    # execute
research_orchestrator_cli.py resume --run-dir ...       # safe resume
```

Resume is intentionally strict: same `run_dir` + matching `request_hash` + matching `plan_hash`. If the plan changed, resume is refused. Do not work around this — fix the request or start a new run.

Run artifacts live under the run directory: `dag_plan.json`, `dag_state.json`, `run_metadata.json`, `artifact_manifest.json`, `produced_objects.json`, `review_summary.json`, plus per-step subdirectories under `steps/<step_id>/`.

The five typed registries that the orchestrator publishes into:
- [data/factor_registry/](data/factor_registry/) — formal base + composite factors
- [data/candidate_registry/](data/candidate_registry/) — research candidates (factor, theme component)
- [data/signal_registry/](data/signal_registry/) — signal recipes (theme recipes live here, not in candidate_registry)
- [data/model_registry/](data/model_registry/) — trained models
- [data/strategy_registry/](data/strategy_registry/) — strategy candidates

Each is `master.parquet` + `evidence.parquet` + `run_index.parquet` + `status_history.parquet` + `review.html`.

---

## 10. Development Practices (read [.agents/rules/development-practices.md](.agents/rules/development-practices.md) for depth)

- **Logging, not print()**: use Python's `logging` module in any reusable code. Long-running scripts write to `logs/` with rotation.
- **Visible progress**: any script expected to run for a substantial time must show a progress tracker (`tqdm` or periodic log lines with completed/total/ETA). Operators should be able to tell from the console what stage is running.
- **Naming**: `snake_case.py`, `PascalCase` classes, `snake_case` functions, `UPPER_SNAKE_CASE` constants, factor names `{category}_{name}_{lookback}` when horizon-specific.
- **Module boundaries**: import through public interfaces, not by reaching into another module's internals. Finalized reusable logic belongs in `src/`, not `workspace/`.
- **Reuse before reinvent**: before adding a helper or dependency, check whether Qlib, MLflow, the factor library, factor_eval, or `result_analysis` already provides the capability.
- **Secrets**: `.env` for credentials. Never commit real tokens. Tushare token is loaded from `.env` via `${TUSHARE_TOKEN}` in [config.yaml](config.yaml).
- **Version control**: never commit `venv/`, `data/`, `mlruns/`, `logs/`, `*.log`, `*.pyc`, `__pycache__/`, `.env`, Parquet, Qlib bins, or large model artifacts.

---

## 11. Durable Memory & Rule-File Maintenance

### 11.1 project_state.md

[project_state.md](project_state.md) is durable system memory. Update it after meaningful work, including:

- new datasets, pipeline entry points, or coverage extensions
- major bug fixes or architecture changes
- backtester behavior changes or new research conventions
- data sync status changes
- rule migrations and registry-governance changes
- significant research milestones (formal screening completions, formal event-driven runs, etc.)
- **any update to [CLAUDE.md](CLAUDE.md), [AGENTS.md](AGENTS.md), or [.agents/rules/](.agents/rules/)** (rule changes are themselves significant work)

Keep "Last Updated", "Active Research Focus" / current priority, and data sync sections current when affected. Treat project_state.md as the canonical answer to "what is the system right now?" — if it disagrees with code, fix one of them rather than working around the gap.

### 11.2 Keep CLAUDE.md, AGENTS.md, and .agents/rules/ fresh (standing instruction)

This is non-optional. Stale rule files are worse than no rule files because they actively mislead future sessions.

**Revisit at the start of every non-trivial task.** During the §1 context refresh, also skim [CLAUDE.md](CLAUDE.md) and [AGENTS.md](AGENTS.md) against [project_state.md](project_state.md). If `project_state.md` describes architecture, entry points, registries, or workflow facts that the rule files contradict or omit, **fix the rule files before proceeding** with the task.

**Revisit at the end of every substantive change.** After any work that touches `src/`, pipeline entry points, registry layout, research workflow, naming conventions, hard invariants, or anti-patterns, check whether CLAUDE.md, AGENTS.md, and the matching `.agents/rules/*.md` still describe reality. If not, update them in the same session as the change — not "later".

**Drift signals to watch for:**

- A module exists in `src/` but is not listed in CLAUDE.md / AGENTS.md / [src/system.md](src/system.md). (Example: `src/research_orchestrator/` is the 6th top-level module; older copies of `src/system.md` and the rule docs may still describe a five-module layout.)
- A pipeline entry point listed in §6.2 no longer exists, or a new one in [src/data_infra/pipeline/](src/data_infra/pipeline/) is missing from the list.
- A "deprecated" name in the rule files has actually been deleted, or a name marked live has been deprecated.
- A scoped `AGENTS.md` inside a subdirectory contradicts this contract.
- A research convention recorded in `project_state.md` (e.g. PIT lag patches, factor framework changes) is not reflected in the rule files.
- A registry was added, removed, or moved (`candidate_registry`, `factor_registry`, `signal_registry`, `model_registry`, `strategy_registry`).
- A new built-in research profile was added to [src/research_orchestrator/profiles.py](src/research_orchestrator/profiles.py) but is not listed in §9.

**Alignment contract.** CLAUDE.md and AGENTS.md must agree on substance even though they differ in tone and audience. The §1 context-refresh list, the module list, the hard invariants, the live entry points, the data ops rules, the research integrity rules, the four-layer backtest pipeline, the banned anti-patterns, and the venv path must read the same in both files. The Claude-specific tool sections (§12) and the Codex-specific subagent matrix in `AGENTS.md` are the legitimate places to diverge. **If you update a rule in one file, update the matching rule in the other in the same edit pass.**

**Record rule changes in project_state.md** (see §11.1) so the audit trail of "how the agent contract evolved" lives next to the audit trail of "how the system evolved".

---

## 12. Tool Use Discipline (Claude-specific)

Claude Code has dedicated tools that supersede shell commands. Always prefer them — they provide the user a better review experience and respect permission boundaries.

| Use this | Not this |
|---|---|
| `Read` | `cat` / `head` / `tail` / `sed -n` |
| `Edit` / `Write` | `sed` / `awk` / `echo >` / heredoc |
| `Glob` | `find` / `ls -R` |
| `Grep` | `grep` / `rg` from Bash |
| `TodoWrite` | mental tracking for any task with 3+ steps |
| `Bash` | only for genuine system commands and script execution |

Specific to this repo:
- Use `venv/Scripts/python.exe` for any Python execution. Do not invoke system Python.
- Long-running scripts: launch with `run_in_background: true` and read output later. Do not block.
- This is a Windows machine — use forward-slash paths and bash-style redirects (`/dev/null`, not `NUL`).

### When to delegate to a subagent

- **`Explore` subagent** — for codebase exploration that will clearly need more than 3 search rounds, or when scanning for naming conventions across multiple module conventions. For directed lookups of a known symbol, use `Grep` directly.
- **`Plan` subagent** — when a non-trivial implementation task needs an explicit step-by-step plan before edits.
- **`general-purpose` subagent** — for multi-step research tasks that should not pollute the main conversation context with raw tool output. Always brief it as a smart colleague who has not seen the conversation.

Match subagent use to the spirit of the Codex `quant_*` agents in [AGENTS.md](AGENTS.md): use read-only exploration before write work, do not parallelize Tushare fetchers, and use validation gates after substantive changes.

### File references in Claude responses

When mentioning files or code locations, use markdown link syntax — never backticks for file names:

- File: `[file.py](src/file.py)`
- Line: `[file.py:42](src/file.py#L42)`
- Range: `[file.py:42-51](src/file.py#L42-L51)`
- Folder: `[src/utils/](src/utils/)`

Paths should be relative to the workspace root.

---

## 13. Risky-Action Discipline

Before any of the following, pause and confirm with the user (unless they have explicitly authorized it for the current task):

- `git push`, force-push, branch deletion, `git reset --hard`, amending published commits
- Removing or downgrading dependencies in `requirements.txt`
- Touching CI/CD configuration
- Running data-mutation scripts without `--dry-run` against production `data/`
- Full Qlib rebuilds (`mode="all"`)
- Anything that would modify a registry's master tables outside the orchestrator's normal publish path
- Any operation that touches Tushare while another fetch is suspected to be in flight

For everything else (local file edits, reading data, running tests, sandbox builds in `data/qlib_builds/`), proceed normally and report what you did.

---

## 14. Scope Override

More specific `AGENTS.md` files inside subdirectories refine these global rules for their subtree. When both apply, follow the more specific scoped file in addition to this root contract.

If you find a conflict between this file and the rule files under [.agents/rules/](.agents/rules/), the rule files are the deeper reference — bring it to the user's attention and propose a reconciliation rather than picking silently.
